# Rev5 public source-dependency supplement — preparation only

This finite supplement prepares the source dependencies of the stopped
`docs/rev5-public-2026-09-10/` package. It does **not** change any of that
package's 18 files, code, hashes, archived statuses or proof semantics.
It is not a new component comparison, primary-source review or publication.

## Contents and exact scope

- [`rows.json`](rows.json): **36 add operations** for existing source IDs
  absent from the frozen public baseline, plus **one qualified replacement
  proposal for DS-MTR-065**. Six existing BMI270 rows are retained unchanged.
- Twelve document-only metadata derivatives under
  `datasheets/public-references-rev5/`, with original basenames.
- [`manifest.json`](manifest.json): original/derived hash mappings,
  40-direct/43-total ID closure, metadata resolution, transformations and
  required final-public-tree checks.

The baseline is public commit
`f259608bd6d4e673393bb6ed4924ea7dc892cb6d`. Source rows come from the
separately frozen source record identified in the manifest. These are
**existing canonical IDs**, not new allocations or freshly verified facts.
All original technical values, conditions, categories, qualifications,
confidence, UNKNOWNs and limited verdict language are preserved.

The DS-MTR-065 replacement imports its already-recorded page correction and
application-reference qualification, rather than making a fresh technical
decision. Its exact prior public row/hash is retained in `rows.json`, bound
to the original public commit; history must not be presented as if the
qualification had always existed.

## Mechanical changes and provenance boundaries

Row metadata paths are remapped to the twelve public document references.
One source row, DS-IFACE-017, also contains a private provenance location.
Its public original-row representation explicitly omits that location,
retains the exact original digest, and provides the changed representation's
own digest. The complete original line and exact location-level operation
are retained in the private author handoff, not exported here. No numeric
value, source condition, correction identity/date or technical claim changes.
All other imported original-row bodies and the old public065 row are exact.

Metadata files project document identity, recorded official endpoints,
revision/date, retrieval/reuse provenance and recorded hashes only. Private
locations, receipt/coordination material, unrelated ID lists and historical
technical/application/geometry prose are excluded. The original metadata
and all its history remain unchanged. A recorded raw hash is **not** a new
hash verification, acquisition success or latest-version assertion.
Manufacturer PDFs, HTML, vendor source code, page text and diagrams are not
included. The changed derivative bytes inherit no old seal or independent
acceptance.

Old source/finding/change identifiers in claim prose are historical context,
not promises that every historical artifact or raw document is public.
That boundary does **not** excuse dangling source IDs: all **43 required
Evidence IDs** must resolve to canonical rows and existing public metadata.

## Required later import and actual public-tree checks

Only the designated serial publisher may apply these data under a new normal
contract. No canonical ledger is edited by this preparation.

1. Recheck the **actual** destination against the frozen baseline or stop
   for explicit reconciliation. Check old065's complete row/hash before
   replacement, reject collisions, and preserve its historical source record.
2. Add the 36 rows **inside the existing first matching eight-column
   Evidence-ID table**, immediately before its `## Rules` boundary. Replace
   065 in that same table. Do not append a new heading or separate table.
3. Preserve its header:
   `Evidence ID | Datasheet metadata record | Section | Table | Page |
   Parameter / Claim supported | Recorded by | Date`.
4. From the **real final public root**, run its existing ID checker and
   inspect that actual module's parsed Evidence-ID set. It must enumerate
   every one of the 37 operated IDs and all 43 required IDs exactly once,
   with the expected complete cells. **Exit0 alone is insufficient.**
5. Independently resolve all 43 IDs and every metadata-cell path against that
   final tree; require no missing metadata, duplicate IDs, wrong-width rows
   or unexamined table-boundary coverage. Check full source-row references,
   not just direct package references. Preserve the six retained rows.
6. Verify every installed derivative hash and all unchanged package hashes.
   Preserve the existing required checks. If shape or coverage is insufficient,
   hold the affected artifact rather than claiming it was checked.

The baseline checker is first-table-scoped. Its frozen text was inspected,
**not executed from a relocated copy**. The public baseline does not provide
`tools/tests/test_id_uniqueness.py`; this supplement adds neither that test
nor any checker/configuration/workflow changes. Private0d checks are not
claimed equivalent to publicf259 checks. Final public-root enumeration,
execution, import and publication remain **NOT_RUN / pending publisher**.

## Engineering and physical limits

Archived source claims remain source-scoped. The export does not resolve
actual rail/load/default/firmware state, controller authority, fresh-request,
fault/rejoin, numerical B/actual X or physical-conductance questions.
Digital states, readback or host tests are not physical-cut proof or
independent-power-survival evidence.

**NOT_INDEPENDENTLY_REVIEWED; approval=null; adoption=false.**
No new engineering PASS, review-status update, source acceptance or public
readiness claim. **NO-GO / P1 conditional /3C8H /REQ409 /oldfive /
foreign.pro strict and all physical/human holds remain.**
