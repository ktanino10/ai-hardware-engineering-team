# Rev5 public-safe host model and source update

**WIP / NO-GO. No physical operation or part, architecture, risk or B/X
adoption is authorized by this package.**

The [portable host-only sequencer](../rev5-public-2026-09-10/README.md)
provides runnable CLR/CLK/OE ordering and refusal examples, including fresh
synthetic rejoin and useful service. Its seven Python files and bound
technical context are unchanged from the reviewed adapter first cut.
Only current-status documentation and manifests were refreshed.

The [source supplement](../rev5-public-sources-2026-09-10/README.md) carries
36 missing canonical rows, a qualified replacement for an existing driver
row and 12 document-only metadata references. The separate
`g031-row-operations.json` carries two independently accepted source
corrections, one document metadata reference and the source-only ECO
projection. These are finite selected dependencies, not a bulk import of
private history.

`source-assessments.json` distinguishes the exact-original sequencer
correction PASS, the public adapter Code Review's limited finding report,
the G031 changed-source PASS and actual source-only canonical closure.
Original FAIL/CONDITIONAL epochs remain historical, rather than being
rewritten. C remains unimplemented/unadopted; the old option comparison is
still a NO_SELECTION record, not a decision made from the newer upload.

## Reproduce and audit

Run the commands in the portable package README with Python's standard
library. Every actual-operation/bus permission stays false. No SDK, device,
network, Git history or manufacturer PDF is required to execute those
synthetic examples. Runtime hash checks are consistency checks, not a
security or physical-proof mechanism.

The package-local manifest binds its current files. This directory's
`manifest.json` binds the complete selected export and its transformations.
Source-supplement manifests remain their original preparation snapshots;
their old package hash inventory is not a claim that later status documents
are unchanged. The exported source snapshots retain original status wording
where they serve as immutable technical inputs.

The designated public publisher must apply the row operations to the
existing supported canonical table and use the **actual public-tree**
checker to enumerate every imported/changed ID and all current dependencies.
A private projection or an exit code without coverage is insufficient.
Public configuration and existing required checks are not replaced.

This is an export handoff. Actual import, remote branch and draft-PR results
are recorded separately in the publisher's `import-readback.json` and
`publication-receipt.json` when those actions genuinely occur. This document
does not claim they have already occurred or that main was updated.

## Deliberate exclusions and remaining limits

No original PDF, uploaded attachment, vendor-code capture, private
conversation/operator/worker receipt, personal path, credential, native
design, sealed measurement firmware or old execution dump is included.
Original source paths/commits/hashes are archive identifiers in lineage,
not necessarily publicly retrievable links or runtime dependencies.

Remaining engineering gaps are actual independent-C/issuer enforcement,
source/power/load/edge and B/X qualification, and physical disconnect/
transaction/legacy-board premises. Command LOW is not pin LOW; high-Z and
FF state are not physical conductance evidence. No first-invalid-edge
protection, current/charge/timing budget or fault waiver is implied.

**P1 conditional / 3C8H / REQ409 / all existing physical and human holds
remain.** Public publication is not Design Complete, fabrication, flashing,
energization, measurement, motion or manufacturing approval.
