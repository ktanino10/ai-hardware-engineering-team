# Change Log — Engineering Change Orders (ECO)

Tracks what changed, why, and under whose approval, every time the design
is revised. This is what makes hardware revisions auditable after the fact —
"just editing" a schematic/PCB/BOM post-fabrication without a recorded ECO
is not acceptable practice here.

One entry per change. Design Complete requires an ECO entry for the current
revision (`docs/architecture.md` §8, condition 5).

## ECO Template

### `<ECO-XXX>` — `<short title>`

- **Date**:
- **Revision**: from `<rev>` to `<rev>`
- **Changed**: `<component / schematic / PCB / BOM — be specific>`
- **Why**: `<driving Finding ID(s) from open-issues.md, FMEA ID(s), or requirement change>`
- **Impact analysis**: `<affected subsystems; re-test needed? re-review needed?>`
- **Approved by**: `<human Chief Engineer name>` — `<date>`
- **Related IDs**: `<open-issues / FMEA / requirement / Evidence IDs>`

## Log

| ECO ID | Date | Revision (from -> to) | Changed | Why | Approved by | Related IDs |
|---|---|---|---|---|---|---|
| ECO-001 | 2026-08-30 | N/A (content-restoration event, not a design revision) | Nothing design-wise changed. `requirements/requirements.md`, `requirements/traceability-matrix.md`, `bom/component-selection.md`, `datasheets/evidence-log.md`, and 12 `datasheets/*.md` metadata records were re-written to disk verbatim from the Hardware Lead AI agent's own conversation context, after this session's worktree directory was accidentally deleted from disk during an independent audit (a human/creator-session operational error, not caused by this session). No requirement, component recommendation, or Evidence ID was altered during the rewrite. | Human-caused accidental deletion of the session worktree (confirmed independently by this session via 3 separate tool failures — bash process spawn, `view` "path does not exist", `get_changes_overview` git error — before any recovery action was taken) while the human/creator session was independently auditing the just-completed Component Selection phase (Checkpoint B). Uncommitted work at the time of deletion had never been committed to git (a process gap this ECO also serves to flag/close — see Notes). | Human Chief Engineer (via the creator/"General Chat" session), who both caused the incident and directed the recovery; restoration content independently spot-checked by the human against 5 key claims (ICM-20948 VDDIO range, ESP32-C3 I2C count, MCP1700 current limit, STM32G031K8T6 VDD AMR, BMI270 VDDIO range) prior to the incident, and the rewritten files were verified byte-count-identical to the pre-deletion versions by the Hardware Lead AI agent after rewriting. | Evidence IDs unaffected: DS-MCU-001..043, DS-IMU-001..073, DS-PWR-001..045 (all re-registered identically). Component Selection Checkpoint B approval (`bom/component-selection.md` Approval tables) unaffected — it was recorded via a tool call independent of the deleted files and remains valid. |
| ECO-002 | 2026-08-31 | N/A (conflict-mediation/evidence-correction event, not yet a circuit-design revision — the actual circuit rework this triggers is tracked as its own future ECO once the Circuit Engineer completes it) | Corrected the evidence record following a genuine factual disagreement between the parallel Hardware Reviewer and rubber-duck Independent Review passes on `hardware/schematic/bench-imu-01-design.md`. Corrected: (1) `validation/open-issues.md` ISS-003 — reclassified RESOLVED, the Hardware Reviewer's own θJA package-confusion claim was itself wrong (confused TI's DQN/X2SON-4 231.1°C/W row with the design's actual DBV/SOT-23-5 60.3°C/W row in the same datasheet table); original design was correct, no design change needed. (2) `validation/open-issues.md` ISS-006 — confirmed but root cause corrected: BOOT0 is muxed onto PA14 (already committed to SWCLK), not PB8 as both the design document and the Hardware Reviewer's citation trail assumed; PB8 does exist on the package (contra the design document's DS-MCU-046) but is unrelated to BOOT0. (3) Added `validation/open-issues.md` ISS-011 (new, HIGH) — confirmed via independent research that MCU pins PB10/PB11, labeled "I2C1" throughout the design document, actually map to the STM32G031K8T6's I2C2 alternate function, not I2C1 — a genuine defect the Hardware Reviewer's own checklist pass did not catch, first surfaced by the parallel rubber-duck pass. `datasheets/evidence-log.md` DS-MCU-050 through DS-MCU-053 added to carry the corrected facts; DS-MCU-045/046 annotated (not silently overwritten) with correction pointers. | Two independent Independent-Review passes (Hardware Reviewer checklist + rubber-duck premise review), run in parallel per `docs/architecture.md` §5.1, produced conflicting factual claims about the same MCU's pin/peripheral mapping and package thermal data. Per `docs/workflow.md` §3 (Conflict Resolution / Deadlock Escalation Protocol), the Hardware Lead did not simply defer to either side — it independently re-verified every disputed claim via real-time web research against primary/authoritative sources (TI's own datasheet thermal table, ST's own reference-manual/pinout/AF-table data) before recording an outcome. | Hardware Lead (AI agent), acting as mediator per its designated role in `docs/workflow.md` §3 — not a human-approval item itself (no requirement/component/design decision changed by this ECO; it corrects the evidence trail feeding the next Circuit Engineer rework cycle, which will carry its own ECO and its own approval record). | `validation/open-issues.md` ISS-003, ISS-006, ISS-011; `datasheets/evidence-log.md` DS-MCU-045, DS-MCU-046, DS-MCU-050, DS-MCU-051, DS-MCU-052, DS-MCU-053; `validation/design-review.md` addendum. |
| ECO-003 | 2026-09-01 | Rev 1 → Rev 2 (`hardware/schematic/bench-imu-01-design.md`) | Circuit Engineer rework closing 3 open HIGH findings from Independent Review Cycle 1: (1) ISS-011 — relabeled MCU I2C bus from I2C1 to I2C2 throughout (no PCB/wiring change, peripheral-instance-label/firmware-target correction only); (2) ISS-001 — removed a hedge on the LDO (U3) EN pin, now a firm, unconditional EN→VIN net (zero-cost, one trace); (3) ISS-002 — added §3.5 analysis of the LDO's thin Vin ROC margin against the real USB-C vSafe5V ceiling, with a recommended ACCEPTED-RISK disposition. Also folded in corrections for ISS-006 (BOOT0/PA14, not PB8) and ISS-010 (MCU VDD AMR lower bound resolved). A fresh Hardware Reviewer Cycle 2 pass (2026-09-01) independently re-verified all 3 HIGH fixes against primary sources and confirmed ISS-001/ISS-011/ISS-006/ISS-010 fully closed; ISS-002's technical analysis was confirmed sound but its disposition required human sign-off (this ECO's own approval — see next row). | ISS-001, ISS-002, ISS-011 (and folded-in ISS-006, ISS-010) findings from `validation/design-review.md` Cycle 1, per the Circuit Engineer's own loop-back obligation (`.github/agents/circuit-engineer.agent.md`: "address every CRITICAL and HIGH finding explicitly"). | Circuit rework itself: no human approval needed (routine loop-back fix, not an architecture/major-component/safety-critical change). ISS-002's ACCEPTED-RISK disposition specifically: **Human Chief Engineer, 2026-09-01**, via `ask_user` — "Accept the risk as-is — no damage risk, bench-use context." Written rationale: this is a first-cycle, bench/prototype design (REQ-201 desk/lab use, REQ-502 paper-design exercise, no mass production); the failure mode is bounded (possible reduced functionality/reliability at a rare worst-case input per TI's own ROC-exceedance footnote, not device destruction — 0.5V/9% of real headroom remains under the LDO's 6.0V Absolute Maximum even at the worst-case 5.5V input); re-selecting the already-approved LDO would reopen an approved Component Selection decision for a non-damage-risk edge case. Revisit if this design is ever mass-produced or its input-source population becomes less controlled. | `validation/open-issues.md` ISS-001 (RESOLVED), ISS-002 (ACCEPTED-RISK), ISS-006 (RESOLVED), ISS-010 (RESOLVED), ISS-011 (RESOLVED); `validation/design-review.md` Cycle 2 entry; `datasheets/evidence-log.md` DS-MCU-012 (corrected). |
| ECO-004 | 2026-09-03 | N/A (validation-closeout event, not a circuit/mechanical design revision — Rev 2/Rev 2 unchanged) | Design Complete Gate evaluation closeout: (1) `requirements/traceability-matrix.md` fully dispositioned, all 25 rows (24 `Verified`, 1 `Pending` — REQ-501 BOM rollup, see Notes); (2) closed a real, previously-unflagged evidence gap for REQ-201: only the IMU candidates' operating-temperature ranges had been checked during Component Selection — the MCU (STM32G031K8T6), LDO (TLV75533PDBVR), and ESD IC (USBLC6-2SC6) ranges were confirmed for the first time this pass (all comfortably cover 0–40°C), added as DS-MCU-054/DS-PWR-047/DS-PROT-003; (3) `validation/fmea.md` populated for the first time with 7 real, design-specific entries (RPN 30–126), 5 tied to existing open-issues.md findings (ISS-002/004/006/011, MISS-007) and 2 new systemic-risk observations (IMU supply-chain fragility; paper-design-vs-real-PCB-layout gap); (4) `validation/bring-up-procedure.md` populated with this design's real expected values (rail voltages, current limits, boot-mode check, recommended bench current-limit) for the human's future physical build — procedure itself not executed this session. | Human's explicit direction to "proceed to whatever remains for Design Complete Gate (FMEA review, traceability matrix closeout, final ECO if any)" following Checkpoint C (Mechanical Design) approval. | Hardware Lead (AI agent) — this ECO documents routine validation-artifact closeout, not an architecture/component/safety-critical design change, so it does not itself require new human approval per `docs/architecture.md` §10. **The Design Complete verdict itself is a separate, still-pending human decision** — this ECO is submitted alongside that request, not a substitute for it. | `requirements/traceability-matrix.md` (all 25 rows); `validation/fmea.md` (FMEA-001–007); `validation/bring-up-procedure.md` §0; `datasheets/evidence-log.md` DS-MCU-054, DS-PWR-047, DS-PROT-003. |
| ECO-005 | 2026-09-03 | **Bench-IMU-01 Rev 2 (Electronics) / Rev 2 (Mechanical) — DESIGN COMPLETE GATE GRANTED** | Closed the 2 remaining open items from ECO-004's gate report, both presented to the human as an explicit choice: (1) `requirements/traceability-matrix.md` REQ-501 (BOM cost ≤~$15) changed `Pending` → `Waived` — a full price rollup is structurally blocked because J1 (USB-C connector) and D1 (LED) never received formally-selected MPNs during Circuit Design, so no real price can be cited for them without fabricating a number; the 3 priced primary ICs already total $7.51 (≈50% of the soft target), and REQ-501 is a "Should," not a "Must". (2) `validation/open-issues.md` ISS-004 (reverse-polarity protection, MEDIUM) changed `OPEN` → `ACCEPTED-RISK`; `requirements/traceability-matrix.md` REQ-402 changed from a flagged partial-gap note to `Verified — human-accepted disposition`. With both items closed, **all 5 Design Complete conditions (`docs/architecture.md` §8) are now met**: (1) zero unresolved CRITICAL; (2) all 5 HIGH findings RESOLVED (4) or ACCEPTED-RISK w/ human sign-off (1, ISS-002); (3) traceability matrix 100% `Verified`/`Waived` across all 25 rows; (4) FMEA reviewed this revision (ECO-004); (5) this entry, human-signed. | ECO-004's Design Complete Gate report, presenting REQ-501 and REQ-402/ISS-004 as the 2 remaining open items with the Hardware Lead's recommendation on each. | **Human Chief Engineer, 2026-09-03**, via `ask_user` — "Waive REQ-501 as proposed, and accept REQ-402's mechanical-keying-only protection as-is — grant Design Complete now." This single decision both dispositions the 2 remaining traceability rows and constitutes the human sign-off required by Design Complete condition 5. | `requirements/traceability-matrix.md` REQ-501 (Waived), REQ-402 (Verified); `validation/open-issues.md` ISS-004 (ACCEPTED-RISK); supersedes ECO-004's "still-pending" note — Design Complete is now GRANTED for this revision. |
| ECO-006 | 2026-08-31 | Rev 2 (Electronics), corrected — post-Design-Complete pin fix, plus this repository's first real KiCad project | This repository's first real KiCad project (`hardware/schematic/bench-imu-01/`) was created for the already-Design-Complete Bench-IMU-01 Rev 2 (PR #6). Building it — independently verifying real KiCad symbol/footprint availability against ST's own official pin database before wiring anything — surfaced **ISS-014 (CRITICAL, pending independent Hardware Reviewer confirmation)**: the STM32G031K8T6's real LQFP-32 package has no PB10/PB11 pins at all, so the IMU I2C2 bus as documented (already "corrected" once by ISS-011) cannot be physically wired. Fixed in `hardware/schematic/bench-imu-01-design.md` (§0, §2.3, §4.1, §4.3, §5.2, §5.3, §6, §11, §12, §13, §14, §16, new §19): I2C2_SCL/SDA reassigned to the real, physically-existing PA11/PA12 (same peripheral instance, different physical pins — minimizes Firmware Bring-up blast radius); also corrected, same primary source: no separate VDDA pin (combined with VDD at pin 4), no separate VBAT pin at all, only one VSS pin not two (pin 5), and NRST shares a pad with PF2 (pin 6, not pin 4). The real KiCad project itself was then built on these corrected pins and independently tool-verified (`extract_schematic_netlist`/`analyze_schematic_connections`/`find_component_connections`/`identify_circuit_patterns`/`analyze_bom` — see `validation/design-review.md`). | ISS-014 (renumbered to **ISS-027** on 2026-09-11 when this ECO's own branch was merged into a repository whose own, unrelated ISS-014 already existed — see ECO-014), discovered by the Hardware Lead during independent KiCad tooling verification (not a checklist/premise review pass) — see `validation/open-issues.md` ISS-027 for the full finding. | Circuit-rework portion (the pin reassignment itself): no new human approval required (routine loop-back fix, mirroring ECO-003's own precedent for ISS-011 — same severity-driving logic, zero new hardware topology). **Flagged for human awareness, not a new HITL gate**: this reopens a fact about an already-Design-Complete, already-merged revision — the human Chief Engineer should be aware Design Complete does not mean "provably correct forever," only "correct under the review rigor available at the time." | `validation/open-issues.md` ISS-027 (was "ISS-014" — see this row's own Why-column note); `datasheets/evidence-log.md` DS-MCU-064/065/066/067; `datasheets/stmicroelectronics_stm32_open_pin_data_stm32g031k4-6-8tx.md` (new metadata record); `hardware/schematic/bench-imu-01-design.md` (Rev 2, corrected); `hardware/schematic/bench-imu-01/` (new real KiCad project). **Not fixed by this ECO**: `firmware/bench-imu-01/` (PR #7, already merged) still initializes GPIOB pins 10/11 for I2C2 — needs a follow-up fix (GPIOA pins 11/12), explicitly flagged, not performed here (Firmware Bring-up does not gate Design Complete, `docs/architecture.md` §14). |
| ECO-007 | 2026-08-31 | Rev 2 → Rev 3 planning artifacts (`hardware/power-architecture.md`, `hardware/power-budget.md`) | First Power Engineer pass for the Motor Driver + Reaction Wheel addition: populated `hardware/power-architecture.md` with 3 real source-topology options (dual-input dedicated motor supply, single-connector USB-C PD sink, battery-backed motor rail), documented why the existing 3V3/USB rails cannot absorb the new subsystem, and updated `hardware/power-budget.md` to add a separate pending 12V-class motor rail row per REQ-109. No source architecture was selected; file intentionally records a recommendation only, pending the required REQ-108 / `docs/architecture.md` §10 human HITL gate. | Component Selection's recommended motor+driver pairing (T-Motor MN2206-13 KV2000 + TI DRV10983) introduced a structural new-rail requirement: driver VM min 8V and target motor current ≈1.05A make the existing Rev 2 3V3 logic rail insufficient in both voltage and current scale. Hardware-design instructions require `hardware/**` changes to carry ECO traceability, and this pass creates the human-facing architecture-decision record the rest of Rev 3 depends on. | Pending human Chief Engineer decision at the dedicated Power Architecture gate; Hardware Lead relays the options and recommendation, but no architecture is approved by this ECO alone. | REQ-108, REQ-109, REQ-503; DS-PWR-003; DS-MTR-017, DS-MTR-020, DS-MTR-032, DS-MTR-034, DS-MTR-040; `hardware/power-architecture.md`, `hardware/power-budget.md` |
| ECO-008 | 2026-08-31 | **Rev 3 component + power architecture — BOTH HITL GATES GRANTED** | Closed the 2 open decisions ECO-007 and `bom/component-selection.md`'s Motor/Motor Driver IC sections were both waiting on: (1) Component approval — T-Motor MN2206-13 KV2000 + TI DRV10983 approved as recommended, human independently re-verified the specs (fresh web search matching this repo's citations) and the Kt/current-margin arithmetic (independently recomputed in Python: Kt=4.77 mN·m/A, 1.05A needed, 17.2× motor margin, 1.9× driver margin — all matching) before approving. Directive attached: the BEMF-sensing-degrades-below-500–1500-RPM caveat must appear as a tracked item in the Firmware bring-up plan and/or `validation/fmea.md`, not just the comparison report — carried forward as an open action for those phases. (2) Power architecture — Option A approved (keep USB 5V logic-only exactly as Rev 2, add a second dedicated ~12V-class motor input, e.g. barrel jack) over Options B (USB-C PD) and C (battery), on lowest-integration-risk/best-fault-isolation grounds, consistent with the already-approved bare-bench-rig mounting context. `bom/component-selection.md` and `hardware/power-architecture.md` Decision/Approval tables updated in place with this sign-off; `hardware/power-budget.md`'s motor-rail row updated from "pending architecture decision" to "Option A approved," with the specific connector/converter part still to be sourced by Component Engineer during Circuit Design. | ECO-007's own still-open decision, plus `bom/component-selection.md`'s own two "architecture-defining/major component decision" escalation flags — both required human Chief Engineer sign-off per `docs/architecture.md` §10 before Circuit Engineer could proceed. | **Human Chief Engineer, 2026-08-31**, via the creator/"General Chat" session (this cycle's established HITL-gate channel, since `ask_user` proved unreachable — see `requirements/requirements.md` §9c/§10a). Both decisions granted in a single reply after independent re-verification, with the Firmware/FMEA tracking directive attached to the component approval. | `bom/component-selection.md` Motor + Motor Driver IC Approval tables (both Chief Engineer rows now Approved); `hardware/power-architecture.md` Decision table (Chief Engineer row now Approved — Option A); `hardware/power-budget.md` motor-rail row; supersedes ECO-007's "still-pending" note for both decisions — Circuit Design (Rev 3) may now proceed. |
| ECO-009 | 2026-09-04 | Rev 2 → Rev 3 (`hardware/schematic/bench-imu-01-design.md`) | Circuit Engineer extended the Design-Complete Rev 2 schematic-equivalent document to Rev 3, integrating the human-approved motor+driver pairing and Option A power architecture: new J4 power input (Same Sky PJ-102AH barrel jack, chosen the same way Rev 2 justified J1/the ESD IC — a real, datasheet-grounded single supporting part, no full comparison needed), new MCU pin allocation (PA8=PWM speed command, PA6=FG tach input, PB1=direction, PB6/PB7=I2C1 commissioning bus — consuming 5 previously-free GPIOs), explicit shared-ground-reference treatment across the two power domains (checklist item 15, addressing REQ-010/REQ-204's noise-isolation concerns), a reverse-polarity Schottky diode (D2, STPS3L60) on the new motor input (paralleling REQ-402's existing USB-side treatment), and checklist item 18 (Mechanical/Thermal co-design) addressed for real for the first time in this project's history (a rotating body now genuinely exists) rather than the Rev 2 "N/A" determination. `hardware/power-budget.md`'s motor-rail row finalized with real J4/DRV10983 numbers, no placeholders remaining. **Folded-in correction**: found and flagged (not self-resolved) that `bom/component-selection.md`'s DS-MTR-037 citation mis-attributed the DRV10983's genuinely-configurable, auto-retry Lock Detection feature's behavior to its actually-fixed, non-retry OCP mechanism — Hardware Lead independently re-verified via web search (corroborating TI's own §8.3.2.3/§8.3.2.4 datasheet section structure) before annotating `datasheets/evidence-log.md` DS-MTR-037 (not silently overwritten — correction pointer added, mirroring the Rev 2 DS-MCU-045/046 precedent) and correcting `bom/component-selection.md`'s own table text; REQ-111/404's substantive "protection exists" conclusion is unaffected, only the mechanism-to-feature-name attribution was wrong. **New finding requiring Hardware Reviewer attention**: adding D2 causes a 2S-class input (~7.4–8.4V) to sag to/below the DRV10983's 7–8V undervoltage-lockout threshold after the diode's forward drop, while 3S-class input (~9–12.6V) stays comfortably clear — practical operating recommendation is 3S-only, a consequence of the added protection diode, not a flaw in the original motor/driver selection (M1 itself remains rated 2S–3S at the motor terminals). Self-checked against the full 16-item Hardware Reviewer checklist, explicitly re-examining items 1–9, 11, 13, 15 for the new motor domain. Ready for Hardware Reviewer's fresh review (not a re-review of Rev 2's already-passed content, a first review of the Rev 3 addition specifically). | Human's Rev 3 component + power architecture approvals (ECO-008) unblocked Circuit Design per `docs/workflow.md` Phase 4; `.github/agents/circuit-engineer.agent.md`'s own mandatory 18-item checklist required addressing items 15/18 for real now that a motor exists. | Circuit Engineer rework itself: no separate human approval needed for this routine design-integration pass (not yet reviewed by Hardware Reviewer, so no loop-back trigger applies). The evidence-trail correction (OCP/Lock Detection): Hardware Lead (AI agent) mediated per `docs/workflow.md` §3, independently re-verified before annotating — not itself a human-approval item (corrects a citation, does not change any design decision or protection-adequacy conclusion). The 2S/3S UVLO finding is flagged for Hardware Reviewer to classify a severity for, not yet dispositioned. | `datasheets/evidence-log.md` DS-MTR-052 through DS-MTR-070 (new), DS-MCU-064–066 (new), DS-CONN-005, DS-PROT-004/005 (new), DS-MTR-037 (annotated, not deleted); `hardware/power-budget.md` (finalized); `bom/component-selection.md` DS-MTR-037 table-text correction; `requirements/requirements.md` REQ-007/008/108/109/110/111/112/404/204/307 (all now have real design-artifact references pending Hardware Reviewer sign-off). |
| ECO-010 | 2026-09-06 | Rev 3 → Rev 4 (`hardware/schematic/bench-imu-01-design.md`) | Circuit Engineer loop-back rework closing/dispositioning all 5 open HIGH findings from Independent Review Cycle 3 (Hardware Reviewer + parallel rubber-duck, both reviewing the Rev 3 motor-domain addition): **(1) ISS-014 (2S/3S UVLO margin) — FIXED/sharpened**: rewrote §7.5.2 with a corner-by-corner analysis using D2's own min/typ/max VF and U5's own min/typ/max VUVLO_R (DS-MTR-057); found 2S actually fails UVLO at the **typical** corner (6.87V vs. 7.4V typ), not merely a rare worst case as Rev 3 had characterized it, while 3S clears every corner (≈0.38V margin, narrowing to ≈0.32V once F1's own added series drop is included). 3S-only is now stated as this design's **binding constraint**, not a soft recommendation; the 2S/3S conclusion itself is unchanged, only stated more precisely. **(2) ISS-015 (SPEED pin uncommanded-motion risk, REQ-403) — FIXED (partial) + FLAGGED (remainder)**: new evidence (DS-MTR-071, decoding TI's own default-EEPROM register 0x2B and §8.4.5.2) supersedes the Rev 3 "functionally inert" characterization of the DRV10983's factory-default SPEED analog mode — it is fully active, not inert, out of reset. Added new **R10** (1kΩ, 0603, SPEED→GND, §7.5.5) as a real external pulldown holding SPEED near zero-command whenever U1's PA8 is not actively driving it, reversing Rev 3's deliberate non-addition stance. **Flagged, not fixed this revision**: a supervisory load-switch IC gating U5's VCC to the MCU domain's own power state (would hardware-enforce power-up ordering) is specified functionally (§7.5.10) but not part-selected — routed to Component Engineer via Hardware Lead as an architecturally-significant part choice; REQ-403's human-review/sign-off gate for the uncommanded-motion risk class itself is not something a schematic fix alone can close. **(3) ISS-019 (J4 unbounded source envelope) — FIXED**: new **F1** (Littelfuse 30R500U, radial-leaded PTC resettable fuse, Ihold=5.00A/Itrip=10.00A/Vmax=30Vdc/Imax=40A, new **DS-PROT-006**) added in series ahead of D2/D3 (§7.5.9), and this design's source class is now explicitly stated as a **binding 9.0–13.0V envelope** (3S near-cutoff to 3S full-charge +~3% headroom), not an open-ended "up to 24V/5A connector rating" as Rev 3 left it. **Flagged, not fixed**: no continuous/steady-state OVP exists to actively enforce the envelope's 13.0V upper bound in real time (F1/D3 protect against fault/surge conditions, not steady-state overvoltage); `validation/bring-up-procedure.md` should record the 9.0–13.0V envelope as an explicit bench-check item (flag only, file not edited this cycle per this ECO's own scope constraints). **(4) ISS-020 (no maximum-speed/overspeed envelope, REQ-403) — FLAGGED ONLY, no schematic fix**: the "≥3000 RPM" target was always a functional floor; no maximum/safety ceiling for commanded speed, acceleration, or overspeed exists anywhere in this design's artifacts, and none of the Hardware Reviewer's 16 checklist items map to this gap (explicitly noted in §15 to avoid the appearance of silently dropping it from self-check scope) — this is a **requirements-and-firmware-level gap, not a schematic-level defect this revision can close by adding a part**; a firmware requirement is now explicitly specified (an overspeed/max-commanded-speed shutdown policy, enforced in firmware since U5 itself has no hardware speed ceiling) but this is a new *explicit requirement being raised*, not an implemented fix. **(5) ISS-021 (REQ-404 shutdown behavior not actually satisfied) — FLAGGED ONLY, no schematic fix**: confirmed via DS-MTR-058/059/060 that all 3 of U5's protection mechanisms (OCP, Lock Detection, Thermal Shutdown) **auto-recover; none latch** — Lock Detection's own 5-second auto-retry (already on record via DS-MTR-059/ECO-009) does not, by itself, satisfy REQ-404's "Should"-priority intent to prevent *sustained* overheating from repeated retry-and-reattempt cycling. A firmware requirement is now explicitly specified (count consecutive Lock Detection events via I2C1, force SPEED to a safe/stopped state and require deliberate re-arm past a threshold) — again a new explicit requirement, not a schematic fix, since latching is not a capability U5's silicon exposes as a hardware pin/strap option per its own datasheet. `hardware/power-budget.md` reconciled throughout (3V3 subtotal/margins recomputed with R10's ≈0–3.3mA addition to ≈93.5%/≈96.1% margin; VM_MOTOR Supply Capability/Subsystem Load/Rail Margin Summary rows updated with F1, the sharpened 9.0–13.0V/3S-only conclusion, and the ISS-021 non-latching caveat). New datasheet metadata file `datasheets/littelfuse_30r500u_rev-unknown.md` created for F1, including a same-family sibling comparison (30R400U/500U/600U) and 3 honestly-flagged known gaps. Self-checked against the full 16-item Hardware Reviewer checklist for the reworked areas (items 1, 3, 4, 6, 7, 10 re-examined; item 10/power-sequencing explicitly newly checked this cycle, a gap Rev 3's own self-check had not covered). Ready for Hardware Reviewer's re-review, focused on the reworked areas (§7.5.2/§7.5.5/§7.5.9–§7.5.12), not a full re-review of already-passed Rev 1–3 content. | Driving Finding IDs (all HIGH, `validation/design-review.md` Independent Review Cycle 3, both the Hardware Reviewer section and the parallel rubber-duck section): ISS-014, ISS-015, ISS-019, ISS-020, ISS-021 — per the Circuit Engineer's own loop-back obligation (`.github/agents/circuit-engineer.agent.md`: "Address every CRITICAL and HIGH finding explicitly — do not silently drop one."). | Circuit Engineer rework itself: no separate human approval needed for the 3 schematic-level fixes (ISS-014/015/019 — routine loop-back fixes using the same "single supporting part, no full comparison" class already established for D2/D3/J4 in Rev 3, not an architecture/major-component change), consistent with ECO-009's own precedent for this same design. **ISS-015's supervisory-load-switch remainder, and ISS-020/ISS-021 in full, are explicitly NOT self-dispositioned by the Circuit Engineer** — flagged for Hardware Lead mediation per `docs/workflow.md` §3 and the Circuit Engineer's own out-of-scope constraint ("you can flag a part-level problem... resolution goes back through the Hardware Lead"); any resulting RESOLVED/ACCEPTED-RISK disposition tied to REQ-403/REQ-404 requires human Chief Engineer sign-off per `docs/architecture.md` §8 before Design Complete could apply to this subsystem — **NOT YET OBTAINED, still open pending Hardware Reviewer re-review and Hardware Lead mediation**. | `validation/open-issues.md` ISS-014, ISS-015, ISS-019, ISS-020, ISS-021 (this ECO documents the Circuit Engineer's own fix/flag disposition for each; `validation/open-issues.md` itself is out of scope for the Circuit Engineer to edit directly and is not touched by this ECO — its own status-field update for ISS-014/015/019/020/021 is the Hardware Reviewer's/Hardware Lead's action upon re-review, still pending); `datasheets/evidence-log.md` DS-PROT-006 (new); DS-MTR-071, DS-MTR-068, DS-MTR-069, DS-MTR-057, DS-MTR-058/059/060 (all pre-existing, re-cited); `hardware/power-budget.md` (reconciled); REQ-403, REQ-404 (new firmware-level requirements flagged against these, not yet formally amended in `requirements/requirements.md` by this ECO). |
| ECO-011 | 2026-09-06 | Rev 4 mediation + Motor-Rail Supervisory Controller Component Selection | Hardware Lead mediation of ECO-010's flagged/unresolved items, plus a follow-on Component Selection pass: (1) Propagated ISS-014's confirmed 3S-only binding constraint into `bom/component-selection.md` (Motor Driver IC Recommendation section) and `requirements/requirements.md` (REQ-108's own Notes column) — both previously silent on this point, per rubber-duck's ISS-014 cross-reference finding that it had not been propagated. (2) Added two new explicit firmware-level safety requirements to `requirements/requirements.md`, discovered at Independent Review and not previously stated anywhere: **REQ-405** (firmware shall enforce a maximum commanded flywheel speed via the already-wired FG tachometer feedback, ISS-020) and **REQ-406** (firmware shall implement a latched-fault policy on repeated motor-driver Lock Detection events, ISS-021) — both initialized as `Pending` rows in `requirements/traceability-matrix.md`. (3) Dispatched Component Engineer for a real, >=3-candidate comparison (not a single-part shortcut, given safety relevance) for the architecturally-significant supervisory motor-rail controller ECO-010 specified functionally but did not select (serves ISS-015's remainder, ISS-019's residual continuous-OVP gap, and provides the physical enforcement point for ISS-021's firmware-declared latch) — recommended **TI TPS26631PWPR** (HTSSOP-20, ~$4.7-4.8, confirmed EVM, all 3 required functions met, one required schematic addition flagged: SHDN's native bias floats ON and needs an external pull-down to invert to the required default-OFF/fail-safe direction). New `bom/component-selection.md` "Motor-Rail Supervisory Controller" section (3 real candidates: TPS26631PWPR recommended, TPS25982 [best R_DS(on) but VQFN-only], LM5069MM-1+IRLZ44NPBF discrete combo [meets every function but highest cost/part-count]); 17 new Evidence ID rows (`DS-CONN-006`, `DS-PROT-007` through `DS-PROT-022`, using the `PROT` category rather than `PWR`/`MTR` — Component Engineer's own reasoned deviation, flagged as its own escalation item for Hardware Lead confirmation, and confirmed here: `PROT` is the more consistent choice, matching the existing ESD-IC/TVS-diode/reverse-polarity-diode precedent for protection-class parts); 4 new datasheet metadata records. Whole-board committed BOM subtotal now ~$37.0 (motor+driver+J4/D2/D3/F1+this controller), leaving ~$38-53 headroom against REQ-503's $75-90 ceiling. **Also surfaced, not yet actioned**: Component Engineer flagged that F1 (Littelfuse 30R500U, added at ECO-010) may be an obsolete/EOL part number, needing its own follow-up verification before this subsystem's Design Complete. | ECO-010's own explicitly-flagged mediation items (ISS-014 propagation gap; ISS-015 remainder/ISS-019 residual/ISS-021 enforcement-point all needing an architecturally-significant part search Circuit Engineer's own scope correctly declined to self-select; ISS-020/ISS-021 both needing new explicit firmware requirements neither previously existed in `requirements/requirements.md`). | Hardware Lead (AI agent) for the mediation/propagation actions themselves (routine evidence-trail/requirements-currency corrections, not an architecture/component/safety-critical decision on their own). The supervisory controller **recommendation** is explicitly **not self-approved** by Component Engineer or Hardware Lead — flagged as both architecture-defining (new component class) and safety-relevant (REQ-403/404/405/406), requiring human Chief Engineer sign-off before Circuit Engineer implements it, **not yet obtained**. | `requirements/requirements.md` REQ-108 (Notes updated), REQ-405/406 (new); `requirements/traceability-matrix.md` REQ-405/406 (new, Pending); `bom/component-selection.md` Motor Driver IC section (3S-only note) + new Motor-Rail Supervisory Controller section; `datasheets/evidence-log.md` DS-CONN-006, DS-PROT-007–022; `validation/open-issues.md` ISS-014 (propagation gap closed), ISS-015/019/020/021 (still open, pending Hardware Reviewer re-review + human sign-off). |
| ECO-012 | 2026-09-08 | Rev 4 → Rev 5 (`hardware/schematic/bench-imu-01-design.md`) | Circuit Engineer loop-back rework implementing ECO-011's own Component-Engineer-recommended, human-approved supervisory motor-rail controller: wired in **U6** (TI TPS26631PWPR, HTSSOP-20) between the existing F1→D2→D3 protection stage and U5 (DRV10983) VCC, implementing the load-switch function (U6's own internal series-pass FET, controlled by SHDN) as the physical default-OFF/fail-safe enforcement point REQ-403 requires. **MCU pin commitment finalized**: **PA9** (confirmed still free; `bench-imu-01-design.md` §7.5.10's own tentative earmark from ECO-010 is now committed) drives U6's SHDN pin through a new **R11 = 10kΩ external pull-down** (SHDN held low = OFF whenever PA9 is not actively driven high) — sized against U6's own guaranteed ≥10µA SHDN sinking-capability spec (DS-PROT-024), not against either internal pull-up figure found this session (1MΩ per this design's own Figure 8-1 citation, DS-PROT-013, vs. ≈440kΩ per a fresh independent web search corroborated by the human) — R11 dominates by 44–100× under either figure, so the discrepancy does not change R11's sizing (flagged as a residual open item, not resolved, `bench-imu-01-design.md` §16 item 27). **New OVP/UVLO resistor divider** (**R12 = 887kΩ / R13 = 60.4kΩ / R14 = 88.7kΩ**, E96 1%, topology IN_SYS—R12—UVLO pin—R13—OVP pin—R14—GND per TI's own §9.2.2.2 application circuit, DS-PROT-025/029) referenced to the existing 9.0–13.0V J4 envelope (`bench-imu-01-design.md` §7.5.9): using TI's own Eq9/Eq10 and the datasheet's own V(UVLOR)/V(OVPR) reference-tolerance windows, UVLO trips rising at 8.1721/8.3388/8.5056V (min/typ/max, reference-tolerance-only, DS-PROT-025) — 8.6527V at the full-stack worst case (reference + both resistors' 1% tolerance adverse) — comfortably below the 9.0V floor at every corner; OVP trips rising at 13.7368/14.0171/14.2975V (min/typ/max, reference-tolerance-only) — 13.4881V full-stack worst case — comfortably above the 13.0V ceiling at every corner (though only 3.4–3.75% below a hypothetical 4S pack's 14.8V nominal, flagged as a residual margin nuance for Hardware Reviewer, §16 item 28). This closes ECO-010's own flagged ISS-019 residual ('no continuous/steady-state OVP exists to actively enforce the envelope's 13.0V upper bound') for the first time — D3's TVS clamp remains a transient/surge protector only (26.0V threshold), U6's divider is the first continuously-active enforcement mechanism at either envelope boundary. **New adjustable overload current limit**: **R15 = 3.57kΩ** (per TI's own §9.2.2.1 Eq8, DS-PROT-027) sets I(OL) ≈ 4.69/5.04/5.40A (min/typ/max), deliberately biased to the nearest E96 value above TI's own tested 4.02kΩ table row so U6's own overload trip sits between U5's existing OCP (3-4A, DS-MTR-058) and F1's Itrip (10A) — completing a 3-tier protection hierarchy without pre-empting U5's own OCP as the primary mechanism. **New MODE/PGTH/PGOOD/FLT/IMON strapping**: MODE tied Open (latch-off overload response, reset via SHDN toggle — consistent with REQ-403's fail-safe intent and providing the physical actuation point ISS-021's still-undecided firmware latch policy will eventually need, `bench-imu-01-design.md` §7.5.10/§7.5.12); PGTH tied GND (forces the slower, dV/dt-controlled turn-on ramp unconditionally, a conservative choice given the shared rail feeding U5); PGOOD/FLT/IMON left floating (sanctioned — matches TI's own standard Electrical Characteristics test condition per DS-PROT-016, and this design does not currently consume exact fault-class telemetry, a residual informational gap flagged not blocking, §16 item 29). New **C16 = 1µF** IN/IN_SYS bypass (general ceramic-bypass guidance, DS-PROT-030) and **C17 = 22nF** dV/dt-setting capacitor (per TI's own §8.3.1 Eq1-3, DS-PROT-021/022, sized against this design's own real C(OUT)=10µF — explicitly NOT against TI's own unrelated 1mF/30mF worked examples in §8.3.1/§9.2.2.3, a sizing-basis distinction flagged for Hardware Reviewer to avoid a future 'copy error' misreading, §16 item 29). PowerPAD soldered to the GND plane in addition to the GND pin per TI's own layout instruction (DS-PROT-023). **F1 swap**: replaced the now-obsolete **Littelfuse 30R500U** with the **30R500UF** (independently web-search-confirmed this session: identical Ihold=5A/Itrip=10A/Vmax=30Vdc/Imax=40A/package/footprint, RoHS-compliant construction only, Active status, Littelfuse's own manufacturer-recommended direct replacement) throughout the parts list, net list, and `datasheets/littelfuse_30r500u_rev-unknown.md` (renamed via `git mv` to `littelfuse_30r500uf_rev-unknown.md`, both MPNs cross-referenced in a new Rev 5 note rather than duplicating the file) — new **DS-PROT-032/033** citations added, DS-PROT-006 retained (unaffected by the swap, same electrical facts). `hardware/power-budget.md` updated throughout: new 3V3 subsystem-load row for R11 (≈330µA continuous whenever PA9 is high), new VM_MOTOR subsystem-load row for U6's own IQ(ON)/IQ(OFF)/divider-bias current (combined worst case ≈1.76mA, <0.06% of J4's rating, immaterial), Rail Margin Summary's own final row rewritten from 'no continuous OVP exists' (Rev 4) to 'CLOSED by U6' (Rev 5) with the real trip-point numbers, and a new Thermal cross-check (U6) section (RθJA=32.2°C/W, DS-PROT-031; conduction-loss-based TJ≈53°C at 40°C ambient, ≈72/97/112°C margin below ROC/AMR/T(TSD) respectively). Self-checked the new U6 stage against the full 16-item Hardware Reviewer checklist specifically (`bench-imu-01-design.md` §15), all 16 items addressed for this stage. **Explicitly NOT attempted**: no circuit-level resolution of ISS-020 (overspeed envelope) or ISS-021 (latched-fault policy) — both remain genuine firmware-policy gaps per this Circuit Engineer's own Rev 4/ECO-010 analysis; U6 gives firmware a physical enable/disable actuation point (PA9/SHDN) for ISS-021's eventual latch, it does not implement that policy itself. | ECO-011's own human-approved Component Selection outcome (`bom/component-selection.md` Motor-Rail Supervisory Controller section, Approval table: "Approved — 'TPS26631PWPR confirmed.'") authorized this implementation; also closes ECO-010's own flagged items — ISS-015's supervisory-load-switch remainder, ISS-019's residual continuous-OVP gap, and provides ISS-021's eventual firmware-latch physical enforcement point (`.github/agents/circuit-engineer.agent.md`: "Address every CRITICAL and HIGH finding explicitly"). F1's swap is independently driven by this session's own web-search re-confirmation that the 30R500U is obsolete/NRND and the 30R500UF is Littelfuse's own recommended Active/RoHS3 direct replacement — a like-for-like part-currency correction, not a new finding. | Circuit Engineer implementation itself: no separate human approval needed beyond ECO-011's own already-obtained sign-off on the TPS26631PWPR component-selection decision (implementing an already-approved part per its own datasheet's recommended application circuit is routine Circuit Design work, consistent with ECO-009's own precedent for D2/J4/F1). The F1 30R500U→30R500UF swap is a like-for-like manufacturer-recommended replacement (same electrical/mechanical spec, RoHS-construction-only difference) — not an architecture/component-class change requiring a fresh Component Selection gate. **ISS-020/ISS-021 remain explicitly NOT dispositioned by this ECO** — both remain open firmware-policy gaps per this Circuit Engineer's own explicit scope decision (task constraint: "Do not attempt to resolve ISS-020 or ISS-021 with a new circuit trick"); any eventual RESOLVED/ACCEPTED-RISK disposition for those still requires human Chief Engineer sign-off per `docs/architecture.md` §8, unchanged from ECO-010's own still-open status for both. | `datasheets/evidence-log.md` DS-PROT-023 through DS-PROT-033 (new); DS-PROT-006 (retained, F1 facts unaffected by the swap); `datasheets/texasinstruments_tps26631pwpr_unknown-rev.md` (Rev 5 addenda, known-gaps annotated); `datasheets/littelfuse_30r500uf_rev-unknown.md` (renamed from `littelfuse_30r500u_rev-unknown.md`, Rev 5 rename/swap note added); `hardware/power-budget.md` (U6 current draw, F1 MPN, Rail Margin Summary, new Thermal cross-check section); `validation/open-issues.md` ISS-015 (remainder addressed — not itself edited by this ECO, Hardware Reviewer's/Hardware Lead's action on re-review), ISS-019 (residual OVP gap closed — same caveat), ISS-021 (physical enforcement point provided, policy itself still open — same caveat); ISS-020 (unchanged, still open); REQ-403 (fail-safe default-OFF behavior now hardware-implemented, not just specified). |

| ECO-013 | 2026-09-09 | No document revision bump — disposition/process change only (`validation/open-issues.md`, `validation/bring-up-procedure.md`) | **Hybrid ACCEPTED-RISK disposition for ISS-020 and ISS-021**, following the Hardware Reviewer's Cycle 4 re-review (confirming ISS-014/015/019 RESOLVED, ISS-020/021 correctly remaining OPEN as genuine firmware-policy gaps neither hardware-fixable, per that review's own conclusion). The human Chief Engineer explicitly declined to apply Bench-IMU-01's own "Firmware Bring-up does not gate Design Complete" precedent here without modification, reasoning that the precedent was safe for Rev 1/2 specifically because that firmware only read sensors and never drove physical motion — this subsystem stores real rotational energy, so a stronger compensating safeguard is required before the same non-gating treatment can apply. Resulting hybrid: (1) `validation/open-issues.md` ISS-020 and ISS-021 both changed from OPEN to **ACCEPTED-RISK**, each explicitly conditioned on Firmware Bring-up actually implementing its governing requirement (REQ-405 for ISS-020, REQ-406 for ISS-021) — neither finding is treated as truly RESOLVED until that firmware exists and is independently re-confirmed against its requirement in a future review cycle; both Notes columns updated with the full sign-off text and date. (2) `validation/bring-up-procedure.md` §1 (Pre-Power-On Checklist) gains a new, prominently-formatted, MANDATORY first checklist item naming ISS-020/ISS-021/REQ-405/REQ-406 explicitly by ID, requiring confirmed REQ-405/406 firmware implementation before the motor/reaction-wheel subsystem (U6/U5/M1) is ever energized — explicitly not satisfied by U5/U6's own auto-recovering protections alone (per ISS-021's own rationale) — added as a targeted advance addition ahead of that file's full Rev 3 rewrite (still tracked separately at ISS-023, unaffected by this ECO). Net effect: hardware/mechanical Design Complete for Rev 3 may now proceed without an unresolved-HIGH block, while the actual physical risk stays hard-blocked at the one gate (first power-on) that governs real energy ever reaching the motor. | Hardware Reviewer's Cycle 4 re-review left ISS-020/ISS-021 as the last 2 open HIGH findings blocking `tools/check_open_issues.py`'s Design Complete gate check (`docs/architecture.md` §8, condition 2) — this ECO is the human decision that resolves how those 2 sit relative to that gate without inventing a fake hardware fix for a firmware-policy question. | **Human Chief Engineer, 2026-09-09**, via the creator/"General Chat" session (this cycle's established HITL-gate channel). Independently re-verified F1's 30R500UF replacement claim (unrelated, carried over from the prior report) before addressing this disposition. Explicit reasoning recorded above distinguishing this case from the Bench-IMU-01 precedent, not a bare "accepted." | `validation/open-issues.md` ISS-020, ISS-021 (Status: OPEN → ACCEPTED-RISK; Resolved date 2026-09-09; Notes appended); `validation/bring-up-procedure.md` §1 (new mandatory checklist item); REQ-405, REQ-406 (now the explicit, named unblocking condition for both findings' true resolution); cross-ref `validation/design-review.md` Cycle 4 (Hardware Reviewer re-review this ECO acts on). |
| ECO-014 | 2026-09-11 | Merge `origin/main` (PR #9) — adopt a more complete MCU pin-identity fix, superseding this branch's own narrower "Rev 6"/ISS-026 fix | A parallel session built this repository's first real KiCad project for the Rev 2 baseline (PR #9, merged to `main`), independently surfacing a more complete version of the same pin-nonexistence defect class this branch's own ISS-026 (Rev 6) addressed only partially: PB10/PB11 nonexistence (matching this branch's own finding), plus 3 further errors this branch's own fix never caught — no separate VDDA pin (combined with VDD at physical pin 4), no VBAT pin at all on this package, one combined VSS/VSSA pin (pin 5, not two), and NRST sharing physical pin 6 with GPIO PF2. That branch's own Hardware Reviewer independently verified the fix using real `kicad-cli` ERC/netlist-export tooling. Before merging, the Hardware Lead independently re-verified both branches' core claims directly against ST's own primary datasheet (DS12992 Rev 4) and ST's own machine-readable CubeMX pin database — every fact on both sides confirmed correct. **This branch's own Rev 6 changelog entry was retired (deleted), not kept alongside the adopted fix** — describing the same underlying defect twice with two different fixes would have been confusing/contradictory; ISS-026 stays RESOLVED in `validation/open-issues.md` with a note that it is superseded by the more complete fix, not silently erased. **Three genuine ID collisions resolved** (both branches independently invented the same numbers for unrelated content after diverging from the same base): ECO-006 through ECO-012 (this branch's own) renumbered to ECO-007 through ECO-013 (main's ECO-006 stays canonical); main's ISS-014 (the pin-identity finding) renumbered to **ISS-027** (this branch's own, unrelated ISS-014 — a 2S/3S UVLO-margin finding, already RESOLVED — stays unchanged, no relation between the two); this branch's own DS-MCU-064 through DS-MCU-068 renumbered to DS-MCU-069 through DS-MCU-073 (main's DS-MCU-064–067, the more complete citations, stay canonical). This branch's own Rev 3–5 motor-driver-subsystem content (schematic §7.5 and all related pin/net/checklist additions) preserved in full — main has no knowledge of the motor subsystem. A real KiCad project (`hardware/schematic/bench-imu-01/`) and its supporting datasheet metadata record are newly present from `main` — pure additions, no conflict. | Both branches independently extending the same already-merged Rev 2 baseline in parallel is an expected consequence of this repository's own multi-session workflow; a genuine, previously-undetected defect discovered by one branch in an already-Design-Complete revision must be reconciled into any other branch still building on that same revision, not left to diverge silently. | Circuit rework itself: no separate human approval needed (adopting an already-independently-reviewed upstream fix for an already-merged baseline defect is not a new architecture/component/safety-critical decision — mirrors this project's own precedent that a routine loop-back/correction fix doesn't require a fresh HITL gate). Merge/ID-renumbering/content-reconciliation mechanics: Hardware Lead (validation-artifact bookkeeping, within this role's own Critical Issue register ownership) and Circuit Engineer (dispatched sub-agent, schematic-document content reconciliation, within that role's own domain) — both independently re-verified the adopted facts before touching anything, per this project's own established discipline. | `validation/change-log.md` (this entry); `validation/open-issues.md` ISS-026 (superseded-by note added), ISS-027 (new, renumbered from main's ISS-014, RESOLVED); `datasheets/evidence-log.md` DS-MCU-064–067 (main's, canonical), DS-MCU-069–073 (this branch's own, renumbered); `hardware/schematic/bench-imu-01-design.md` (Rev 6 retired, corrected pin-identity content adopted, motor-subsystem content preserved); `hardware/schematic/bench-imu-01/` (new, from `main`); `datasheets/stmicroelectronics_stm32_open_pin_data_stm32g031k4-6-8tx.md` (new, from `main`). |
| ECO-015 | 2026-09-11 | Rev 3 enclosure rework (`hardware/mechanical/bench-imu-01-dimensional-spec.md`, `bench-imu-01-enclosure.scad`) | Mechanical Lead loop-back rework closing all 3 gating findings from Independent Mechanical Review Cycle 1 (CONDITIONAL verdict): **(1) MISS-008 (CRITICAL) — FIXED**: `fw_disk_bottom` formula was missing `fw_hub_collar_h` (6mm), causing a genuine ~145mm³ manifold overlap between the flywheel disk and its own hub collar that would have made physical assembly (the design's own Step 5) impossible; fixed by adding the missing term, cascading +6.0mm through the downstream Z-stack (`fw_disk_bottom` 31.5→37.5, `fw_disk_top` 36.0→42.0, `fw_clearance_top` 39.0→45.0, containment cap height 43.0→49.0mm) — independently re-verified via an isolated `intersection()` render reporting "Current top level object is empty". **(2) MISS-009 (HIGH) — FIXED**: the motor phase-wire duct void was only subtracted inside `motor_wire_bridge()`'s own local cube, never globally in `base()`, leaving the duct path solid plastic at 2 locations; fixed by restructuring `base()` into a proper `difference()` with a dedicated `motor_wire_duct_void()` module (also fixed an independently-discovered 1mm shortfall at the duct mouth) — re-verified via a 7-point probe sweep. **(3) MISS-010 (HIGH) — FIXED**: the pre-existing Rev 2 base-tab/lid-skirt interference (~190mm³), previously self-disclosed but left unfixed as out-of-scope, fixed now that the geometry is already being reworked — new `tab_relief_margin` (1.0mm) + relief notches at each of the 4 `tab_positions` — re-verified via `trimesh` volume computation (watertight, 0.0mm³ overlap). REQ-403's containment envelope re-derived end-to-end against the corrected geometry (not left at pre-fix values): new `fw_clearance_top`=45.0mm, containment cap height=49.0mm, flange/skirt Z-range=[37.0,45.0]mm; topology claims (continuous wall, no rotation-plane opening, bolted cap) re-confirmed still valid against the new Z-positions. MISS-011 (MEDIUM, non-gating) carried forward as an explicitly disclosed limitation, not resolved this cycle, per the review's own non-blocking classification. Full-assembly regression check after all 3 fixes: NoError, Genus 8, 4618 vertices, 9264 facets — matches documented figures, no regressions. | Driving Finding IDs (all from `validation/design-review.md` Mechanical Reviewer — Cycle 3, Independent Review of the Rev 3 enclosure): MISS-008, MISS-009, MISS-010 — per the Mechanical Lead's own loop-back obligation, mirroring the Circuit Engineer's own established "address every CRITICAL and HIGH finding explicitly" precedent for the Electronics discipline. | Mechanical Lead rework itself: no separate human approval needed (routine loop-back fix closing findings from an already-completed Independent Review cycle, not an architecture/major-component/safety-critical decision on its own — mirrors ECO-009's own precedent for the equivalent Electronics loop-back). The REQ-403 containment proposal's own final safety disposition remains a separate, still-pending human sign-off (per REQ-403's own explicit human-review requirement) — this ECO documents the geometry now being credible enough to re-review, not a safety approval in itself. | `validation/open-issues.md` MISS-008, MISS-009, MISS-010 (this ECO documents the Mechanical Lead's own fix disposition for each — `validation/open-issues.md` itself is out of scope for the Mechanical Lead to edit directly and is not touched by this ECO; its own status-field update is the Mechanical Reviewer's/Hardware Lead's action upon re-review, still pending); REQ-306, REQ-403 (containment envelope re-derived); `hardware/mechanical/bench-imu-01-dimensional-spec.md` §8/§12/§15/§16 (updated throughout). |
| ECO-016 | 2026-09-11 | No document revision bump — citation-hygiene correction only (`datasheets/evidence-log.md`, `datasheets/stmicroelectronics_stm32g031k8t6_rev-unknown.md`, `validation/open-issues.md`, `validation/design-review.md`, `validation/bring-up-procedure.md`) | **Stale/dangling citation cleanup following ECO-014's ID renumbering.** ECO-014 renumbered this branch's own colliding DS-MCU-064–068 to DS-MCU-069–073, and updated the primary citing locations (`datasheets/evidence-log.md` itself, and `hardware/schematic/bench-imu-01-design.md`'s own citations) — but 8 further live citations across 5 files were missed at the time and left dangling (pointing at an Evidence ID, DS-MCU-068, that no longer exists in `evidence-log.md`) or stale (pre-renumbering ECO number, or pre-ISS-027 pin facts): (1) `datasheets/stmicroelectronics_stm32g031k8t6_rev-unknown.md` — its own "Used for Evidence IDs" list and 2026-09-09 Correction note's cross-reference, both fixed to DS-MCU-073 with a new dated Renumbering note appended (original note text left as originally written, per this file's own convention). (2) `validation/open-issues.md` ISS-026's own Datasheet Source text, fixed to DS-MCU-073 with a renumbering annotation. (3) `validation/design-review.md`, 2 places within the Cycle 5 finding record, same fix. (4) `datasheets/evidence-log.md` DS-MCU-062 — still stated "PB10/PB11 = AF6" as a live, uncorrected fact; annotated (not deleted) with a 2026-09-11 correction note distinguishing the still-correct AF6 *value* from the wrong *pin pair* (real pins are PA11/PA12, DS-MCU-064/073). (5) `datasheets/evidence-log.md` DS-MTR-062/063 — both cited "ECO-007" for the BEMF/FG-degradation directive, stale after ECO-014's renumbering shifted the actual event (component+power HITL gates) from old ECO-007 to new ECO-008; both citations corrected. (6) `validation/bring-up-procedure.md` — 2 places still instructed a future physical bring-up to check "I2C2/PB10-PB11 wiring"; both corrected to PA11/PA12 with inline ISS-027 correction notes. All fixes independently found (not merely reported): items 1–3 flagged directly by the Hardware Lead's own follow-up repo-wide grep after the human's spot-check request; items 4–6 flagged by the Firmware Engineer's own independent pin-traceability check during the Firmware Bring-up task (ECO-017) and confirmed by the Hardware Lead before fixing. | ECO-014's own renumbering was incomplete — a real process gap (missed cross-references), not a new design defect, caught by two independent follow-up checks (a human spot-check request, and the Firmware Engineer's own traceability discipline) rather than left to accumulate silently. | No human approval needed — pure citation/documentation-hygiene correction, no design/requirement/component change, mirrors the routine-correction precedent already established for DS-MCU-045/046 and DS-MTR-037. | `datasheets/evidence-log.md` DS-MCU-062 (annotated), DS-MTR-062/063 (ECO citation corrected); `datasheets/stmicroelectronics_stm32g031k8t6_rev-unknown.md` (Renumbering note added); `validation/open-issues.md` ISS-026 (citation corrected); `validation/design-review.md` (2 citations corrected); `validation/bring-up-procedure.md` (2 lines corrected). |
| ECO-017 | 2026-09-11 | Firmware Bring-up (Rev 3) — `firmware/bench-imu-01/` | Firmware Engineer extended the existing Bench-IMU-01 bring-up firmware to drive and characterize the Rev 3 Motor Driver + Reaction Wheel subsystem, strictly **open-loop**, plus fixed the pre-existing IMU I2C GPIO defect (ISS-027) at the firmware layer. **(1) IMU I2C pin fix**: `gpio.c`/`gpio.h`/`clock.c`/`clock.h`/`i2c2.h` corrected from GPIOB pins 10/11 to **GPIOA pins 11/12** (same AF6, same I2C2 peripheral base address 0x40005800 — a GPIO-mux fix only, not a peripheral-instance change), matching the corrected schematic; independently re-confirmed by the Hardware Lead via direct code inspection (no remaining register-write code anywhere in `firmware/` targets PB10/PB11) before committing. **(2) Motor subsystem bring-up**: new `i2c1.{c,h}` (U5 commissioning/status bus, PB6/PB7), `tim1_pwm.{c,h}` (PA8, PWM speed command), `tim3_fg.{c,h}` (PA6, **interrupt-driven** FG tachometer capture — deliberately not polling, to avoid silently missing edges at high speed), `drv10983.{c,h}` (register-level EEPROM commissioning + Lock-Detection status reads), `motor.{c,h}` (host command grammar over the existing UART: SPD/DIR/STOP/REARM, plus CSV telemetry) — a test/characterization routine, not a permanent controller. **(3) REQ-405 (overspeed shutdown)**: ceiling set to **6000 RPM** (2.0× margin above the ≥3000 RPM floor, ~3.3–3.7× below the motor's own ~20,000–22,200 RPM no-load speed), continuously enforced via FG feedback; SPEED→0 and U6 SHDN→low on trip. **(4) REQ-406 (latched fault policy)**: 3 consecutive Lock-Detection events within a rolling 30s window trips a latch (SPEED→0, U6 SHDN→low) requiring a deliberate `REARM` host command — never auto-resumes on its own, unlike U5/U6's own hardware auto-recovery. **(5) BEMF/FG degradation caveat** (~500–1500 RPM, ECO-008) disclosed via boot-banner output, deliberately not filtered/masked in the Lock-Detection path (would undermine REQ-406's own purpose). **(6) IMU/motor independence bug fix**: found and fixed a real coupling bug in the pre-existing `main.c` — a permanent-blocking loop on BMI270 init failure would have silently prevented the independent motor subsystem from ever initializing; replaced with a non-blocking `bmi_ok` flag. Scope discipline (no PID/attitude-control/sensor-fusion code) and IMU/motor independence both independently re-verified by the Hardware Lead via direct grep/code inspection before committing, not accepted on the Firmware Engineer's own report alone. | REQ-007/REQ-009 (open-loop motor characterization scope), REQ-405, REQ-406 (new firmware safety requirements from `validation/open-issues.md` ISS-020/ISS-021's ACCEPTED-RISK disposition, ECO-013); ISS-027 (the firmware-layer half of the pin-identity fix, schematic-layer half already closed by ECO-014). | Firmware rework itself: no separate human approval needed — implementing already-approved requirements (REQ-405/406) and an already-corrected schematic pin assignment (ISS-027) is routine Firmware Bring-up work, not an architecture/component/safety-critical decision on its own, consistent with the Firmware Engineer discipline's own established no-independent-reviewer precedent (self-check against `.github/skills/firmware-bringup/SKILL.md` stands in, `docs/architecture.md` §14). Firmware Bring-up does not itself gate Design Complete, mirroring the Rev 1/2 precedent for this same repository, subject to `validation/bring-up-procedure.md`'s own strengthened pre-power-on gate (ECO-012) requiring confirmed REQ-405/406 implementation before real power ever reaches the motor — now satisfied at the firmware-source level; hardware-in-the-loop confirmation remains a separate, future, physical-build-time step. | `firmware/bench-imu-01/src/{gpio,clock,i2c2}.{c,h}` (pin fix); new `i2c1.{c,h}`, `tim1_pwm.{c,h}`, `tim3_fg.{c,h}`, `drv10983.{c,h}`, `motor.{c,h}`; `firmware/bench-imu-01/src/main.c` (IMU/motor independence fix); `firmware/bench-imu-01/{README.md,bench-imu-01-firmware-design.md}` (design rationale); `datasheets/stmicroelectronics_cmsis_device_g0_master.md`, `datasheets/texasinstruments_drv10983_slvscp6h.md` (new I2C1/TIM1/TIM3 register citations, DS-MCU-074 added); REQ-405, REQ-406 (implemented, pending traceability-matrix closeout). |
| ECO-018 | 2026-09-08 | N/A (firmware-only follow-up fix — no Electronics/Mechanical hardware revision; Firmware Bring-up does not carry its own revision number, `docs/workflow.md` Phase 11) | Closed the firmware follow-up ECO-006 explicitly flagged but did not perform: `firmware/bench-imu-01/` (PR #7, merged before ISS-014 was discovered) still initialized GPIOB pins 10/11 for the IMU's I2C2 bus, matching the schematic's pre-ISS-014, physically-nonexistent pin assignment. Fixed: `src/gpio.c`/`src/gpio.h` now configure **GPIOA pins 11/12** (AF6, open-drain), matching `hardware/schematic/bench-imu-01-design.md`'s corrected PA11/PA12 assignment; `src/clock.c`/`src/clock.h` no longer enable `RCC_IOPENR_GPIOBEN` (GPIOB is not used by any pin this board's firmware configures); `src/stm32g031_regs.h` removed the now-dead `GPIOB_BASE`/`GPIOB`/`RCC_IOPENR_GPIOBEN` definitions, consistent with that header's own stated scope ("only the peripherals this board's schematic actually uses"). The I2C2 *peripheral instance* and register base address (0x40005800) were already correct and are unchanged — only the GPIO port/pin/AF-mux target changes. Independently re-verified the pin/AF facts from primary sources before changing code (deliberately not trusting an unverified "DS-MCU-068" citation asserted by the task framing that requested this fix, which turned out not to actually exist in `datasheets/evidence-log.md` prior to this ECO — re-confirmed PA11/PA12=I2C2_SCL/SDA against ST's own pin database, DS-MCU-067, and additionally sourced the AF6 value — which the pin database alone does not encode — from two further independent official ST sources new to this repository, DS-MCU-068/077: STM32CubeG0's own NUCLEO-G031K8 (identical MCU) I2C2 example and the `stm32g0xx-hal-driver` header's `GPIO_AF6_I2C2` numeric definition). Rebuilt with `arm-none-eabi-gcc` 16.2.0 this session: succeeds, zero warnings under `-Wall -Wextra`, byte-identical image size to the pre-fix build (10,804 `.text`/`.rodata`, 4 `.bss`, 0 `.data`); `arm-none-eabi-objdump -d` on the rebuilt `.elf` confirms the old GPIOB base address (`0x50000400`) no longer appears anywhere in the compiled binary. **Independent verification (2026-09-08, ad hoc, not a formal Firmware Reviewer gate)**: a separate independent pass re-derived every fact from primary sources itself (not trusting this fix's own citations), independently re-fetched the same official ST sources and confirmed matching commit SHAs, independently rebuilt the firmware and confirmed byte-identical results via three separate methods (objdump/grep, raw ELF byte-pattern scan, and a native non-ARM re-execution of `gpio.c`'s actual bit-manipulation helpers against a mock register struct), full-text-scanned the updated docs for stale PB10/PB11 claims (found none), and verified the evidence-log/change-log entries. **Verdict: CONFIRMED CORRECT, zero findings.** Full report retained in this session's record; summary in `firmware/bench-imu-01/bench-imu-01-firmware-design.md` §10. | ISS-014's own Notes (`validation/open-issues.md`) and ECO-006's own "Not fixed by this ECO" disclosure, both explicitly flagging this firmware follow-up as outstanding. | Routine firmware loop-back fix correcting an already-flagged, already-scoped follow-up to an already-approved schematic correction (ECO-006) — no new architecture/component/safety-critical decision, so no new human sign-off required per `docs/architecture.md` §10. Per `docs/workflow.md` Phase 11, Firmware Bring-up does not gate Design Complete and this fix does not reopen or alter Bench-IMU-01's Design Complete status (already GRANTED, ECO-005) or ISS-014's own RESOLVED/CRITICAL disposition (which was, and remains, about the schematic/hardware pin assignment, already fixed by ECO-006). | `validation/open-issues.md` ISS-014 (Notes updated, firmware follow-up closed); `firmware/bench-imu-01/src/gpio.c`, `gpio.h`, `clock.c`, `clock.h`, `i2c2.h`, `stm32g031_regs.h`; `firmware/bench-imu-01/bench-imu-01-firmware-design.md` (Changelog, §4, §7, §8, §9, new §10); `firmware/bench-imu-01/README.md`; `datasheets/evidence-log.md` DS-MCU-068 (new), DS-MCU-077 (new, renumbered from "DS-MCU-069" to avoid colliding with this branch's own existing DS-MCU-069), DS-MCU-062 (annotated, not silently overwritten); `datasheets/stmicroelectronics_stm32cubeg0_master.md`, `datasheets/stmicroelectronics_stm32g0xx-hal-driver_master.md` (new metadata records). |

| ECO-019 | 2026-09-11 | No document revision bump — new finding logged, not resolved (`validation/open-issues.md`, `validation/design-review.md`) | **MISS-012 (HIGH) logged**: while reviewing the REQ-403 containment-cap proposal directly, the human Chief Engineer identified that `containment_wall_t`=4.0mm is a CAD-geometric solid-wall claim only — whether the physically 3D-printed part actually contains 4.0mm of solid material, or a sparse internal lattice (typical FDM slicer defaults are 15-25% infill, mostly air), depends on manufacturing/print-process parameters (infill %, pattern, wall/perimeter count, print orientation) never specified anywhere in this repository (independently re-verified: repo-wide `grep -rn -i "infill"` returns zero hits). Neither Mechanical Lead's own self-check nor 2 Independent Mechanical Review cycles caught this. Logged as MISS-012 in `validation/open-issues.md`, classified HIGH (a realistic, not remote-corner-case, risk for a part whose entire purpose is impact/fragment containment, though distinguished from CRITICAL since the design is genuinely incomplete on this parameter, not demonstrably wrong on a fully-specified one) — distinguished from the existing MISS-011 (MEDIUM): MISS-011 questions whether an already-agreed geometry has been load-verified; MISS-012 questions whether the CAD geometry will even correspond to the as-built part at all, a more fundamental gap. Corresponding addendum added to `validation/design-review.md`. **Explicitly not resolved this ECO** — a separate session has been dispatched to introduce a new Manufacturing Engineer discipline (agent + skill + Mechanical Reviewer checklist addition) as a standalone framework PR against `main`, mirroring the Power Engineer precedent; not yet merged. This containment cap's own Manufacturing Engineer pass will follow once that framework lands, as an additional prerequisite for a complete REQ-403 sign-off on top of (not instead of) the human's own separate safety-topology review. | Human Chief Engineer's own direct review of the REQ-403 proposal, relayed via the cross-session HITL channel — a genuine premise-challenge finding neither review lens caught. | Finding-logging itself: no separate human approval needed (routine backlog bookkeeping, mirroring how ISS-026 was logged directly by the Hardware Lead after human-relayed discovery). The finding's eventual resolution requires the not-yet-merged Manufacturing Engineer framework, explicitly not to be improvised around ad hoc. | `validation/open-issues.md` MISS-012 (new, OPEN, HIGH); `validation/design-review.md` (new addendum after the Mechanical Reviewer Cycle 4 entry); `hardware/mechanical/bench-imu-01-dimensional-spec.md` line 372 (`containment_wall_t`, cited not modified); REQ-403 (an additional prerequisite surfaced, not yet a new requirement of its own). |

| ECO-020 | 2026-09-11 | No document revision bump — new supporting-material document + finding closures, not a REQ-403 resolution (`hardware/mechanical/bench-imu-01-manufacturing-spec.md` new; `validation/open-issues.md`, `validation/design-review.md` updated) | **MISS-012's Manufacturing Engineer framework pass, completed end-to-end.** (1) Manufacturing Engineer (fallback invocation, framework merged PR #12) produced `hardware/mechanical/bench-imu-01-manufacturing-spec.md`: a real, evidence-grounded FDM process specification (material, infill %/pattern, wall/perimeter count, top/bottom solid layers, print orientation) for the containment structure, reasoned against REQ-403's own disclosed 121.60J/69.74m/s radial-ejection load case, honestly marked predominantly `ESTIMATE`, and correctly concluding FDM cannot be presented as an adequate/validated containment process without physical testing — recommending escalation to the human Chief Engineer rather than self-certifying. (2) Mechanical Reviewer's first independent cross-check (checklist item 11, first-ever exercise) found the spec covered `containment_cap()` only, not `fw_bay_wall()`/`base()` — independently re-derived from the live `.scad` source to be the actual primary radial containment surface in the fragment's path — plus a confidence-marking overclaim (derived KE/rim-speed/threat-mechanism rows labeled CONFIRMED despite ESTIMATE/ASSUMPTION inputs) and a one-sided infill rationale that didn't disclose its own cited source's counter-evidence (a 40%-infill sample beating a 100%-infill sample for impact-energy absorption in the cited MDPI dataset). Logged as MISS-013 (HIGH), MISS-014 (MEDIUM), MISS-015 (MEDIUM). (3) Manufacturing Engineer revised the document to extend real, independently-reasoned process coverage to `fw_bay_wall()`/`base()` (not a copy-paste of the cap's own numbers — a genuine orientation trade-off analysis for `base()`'s multi-feature bundling), fix the confidence labels on the 3 originally-named rows, and add the literature-nuance/Z-axis-residual-limitation disclosures. (4) Mechanical Reviewer's second independent re-verification confirmed MISS-012/013/015 genuinely RESOLVED (re-deriving geometry from source, not trusting prose) but found MISS-014 only partially fixed — 2 further rows in the same table ("Credible worst-case flywheel speed," "Angular velocity") still mislabeled CONFIRMED despite the RPM figure being a project-derived estimate per `datasheets/evidence-log.md` DS-MTR-018 ("not directly published by the manufacturer"), not caught by the first fix pass. (5) Hardware Lead independently re-confirmed the DS-MTR-018 citation directly, then applied this last, narrow, already-fully-specified correction directly (relabel 2 cells + add lineage, matching the pattern already correctly used elsewhere in the same table) rather than a third full round-trip, disclosed transparently in MISS-014's own Notes rather than claimed as independently re-reviewed. `tools/check_open_issues.py` confirms a clean gate (42 findings checked, zero unresolved CRITICAL/unsigned-off HIGH) following this ECO. **This ECO does not resolve REQ-403 or declare Design Complete** — it completes the manufacturing-process supporting material the human Chief Engineer asked for (see MISS-012's own origin) and closes the findings that material itself generated; REQ-403's own containment-topology-and-manufacturing-adequacy decision, and the overall Design Complete Gate, remain the Chief Engineer's own reserved decision, per `docs/architecture.md` §10, `traceability-matrix.md`'s REQ-403 row (still `Pending`), and this repository's own repeatedly-reaffirmed HITL discipline throughout this design cycle. | MISS-012 (HIGH, now RESOLVED), MISS-013 (HIGH, now RESOLVED), MISS-014 (MEDIUM, now RESOLVED), MISS-015 (MEDIUM, now RESOLVED); REQ-403 (new supporting material, disposition itself still Pending). | Manufacturing-spec creation, review, and finding-closure work itself: no separate human approval needed — this is ordinary Mechanical-discipline design-and-review work under an already-approved framework (Manufacturing Engineer, PR #12), not itself a new architecture/component/safety-critical decision. REQ-403's own disposition remains explicitly withheld pending the human Chief Engineer's own review of this material — not granted by this ECO, not implied by the clean findings-gate state, and not to be conflated with "Design Complete" per `docs/architecture.md` §8's 5 independent conditions. | `hardware/mechanical/bench-imu-01-manufacturing-spec.md` (new); `validation/open-issues.md` MISS-012/013/014/015; `validation/design-review.md` (3 new entries: first cross-check, re-verification, referenced by this ECO); REQ-403 (traceability-matrix Notes enriched, disposition unchanged — still Pending). |

| ECO-021 | 2026-09-13 | No document revision bump — new SS8.1 analysis appended to `bench-imu-01-dimensional-spec.md`, no `.scad` geometry changed; new HIGH finding logged, not resolved | **MISS-011 closure attempt surfaces materially significant new information about REQ-403's core containment adequacy.** MISS-011 (MEDIUM, non-gating rigor gap: no impact-energy/pull-out calculation existed for the containment structure) was dispatched to Mechanical Lead as legitimate non-blocking work while REQ-403's own disposition remained paused for the human. Mechanical Lead produced a bounded, hand-derived engineering estimate (new SS8.1, `bench-imu-01-dimensional-spec.md`) — no FEA/physical-test tool exists in this environment, re-confirmed this session. **Result, independently verified by both Hardware Lead and Mechanical Reviewer before logging** (arithmetic hand-recomputed from scratch by both, Reviewer's own verification script bug caught and disclosed before concluding; the primary CNC Kitchen fastener-pull-out citation independently re-fetched and confirmed exact; `.scad` geometry constants spot-checked): two independent methods (Charpy/fracture-toughness energy density; yield-strength-limited plastic work), swept across a physically-anchored engagement-length range, both conclude the 4.0mm containment wall's local material cannot plausibly absorb the disclosed 121.60J/69.74m/s hazard energy — best case (largest engagement, most favorable method) ~30-40% of budget (~2.5-3x short of the full energy budget); typical/localized-engagement case as low as ~0.5% (1 to 3+ orders of magnitude short). Fastener pull-out estimate (6xM3 heat-set inserts, real CNC Kitchen PETG pull-out data, DS-FAST-002) is more favorable for the cap's realistic secondary/attenuated loading role (the wall, not the cap, is the primary containment surface per the already-established MISS-013 determination), marginal only for a deliberately pessimistic full-direct-hit hypothetical the cap was never intended to be the primary defense against. Mechanical Reviewer independently re-derived all arithmetic from scratch, spot-checked the material-property citations not already checked by the Hardware Lead, specifically hunted for a missing favorable global-structural-response argument and did not find one that changes the picture, and gave an unhedged direct answer when asked: "real, not overstated." **MISS-011 marked RESOLVED** (its own ask was that a defensible calculation exist, not that the result be reassuring — a genuine, honest, independently-verified calculation now exists). **New finding logged: MISS-016 (HIGH)** — the quantified wall-margin shortfall itself, classified HIGH not CRITICAL because it is a bounded `ESTIMATE` with genuine two-directional uncertainty (high-strain-rate and FDM-print-vs-TDS-coupon effects likely make it worse; an oblique-impact-angle sub-case argument would make it better for that specific release geometry) and no physical/global-structural proof either way — mirrors the MISS-012 severity-reasoning precedent exactly. Disposition left entirely to the human: present the quantified margin transparently alongside the rest of REQ-403's material, commission physical drop/impact testing before relying on the design, or revisit `containment_wall_t`/topology. **MISS-017 (MEDIUM)** also logged: SS8.1's own confidence ledger mislabeled several geometry/kinematic values CONFIRMED that SS4.1/SS4.4 already establish as ASSUMPTION/ESTIMATE — the same pattern MISS-014 already fixed in the sibling manufacturing-spec document, non-gating. `tools/check_open_issues.py` now correctly reports the gate FAILED (MISS-016 HIGH open) — this is expected, correct behavior: real new safety-relevant information should reopen/reinforce the gate, not be silently absorbed. Mechanical Lead explicitly did not modify any `.scad` geometry in response to this finding — left as a decision for the Hardware Lead/human. | MISS-011 (MEDIUM, now RESOLVED); MISS-016 (HIGH, new, OPEN — blocks Design Complete); MISS-017 (MEDIUM, new, OPEN, non-gating); REQ-403 (new, materially significant information, disposition itself still Pending). | Estimate-production and independent-review work itself: no separate human approval needed — this is ordinary Mechanical-discipline analysis-and-review work attempting to close an already-tracked, non-gating finding, not itself a new architecture/component/safety-critical decision. **MISS-016's own disposition (what to do about the quantified shortfall) is explicitly NOT decided by this ECO** — it is new information for the still-outstanding Chief Engineer REQ-403 sign-off, per `docs/architecture.md` §10, and per this repository's repeatedly-reaffirmed HITL discipline throughout this design cycle. | `hardware/mechanical/bench-imu-01-dimensional-spec.md` new SS8.1 (SS8.1.1-SS8.1.7); `validation/open-issues.md` MISS-011/016/017; `validation/design-review.md` (new Mechanical Reviewer entry); `datasheets/evidence-log.md` + 6 new metadata records (DS-MTL-001/002/003, DS-FAST-001/002/003); REQ-403 (traceability-matrix Notes updated to reflect this materially significant new information, disposition unchanged — still Pending). |

| ECO-022 | 2026-09-13 | No document revision bump — citation-label correction (`datasheets/evidence-log.md`) + new §7.5.13 derivation appended to `hardware/schematic/bench-imu-01-design.md`; no schematic/BOM/component change, no `.scad` geometry touched | **DS-MTR-018 voltage-label error found and corrected; a real, higher, credible-worst-case M1 no-load speed independently re-derived; flagged as a required new input for Mechanical Lead's flywheel physics table (SS8) — not itself applied there.** While independently verifying the Firmware Engineer's REQ-405 command-ceiling derivation (which correctly cites this design's own 13.0V worst-case VM_MOTOR envelope, SS7.5.9), the Firmware Reviewer flagged that DS-MTR-018's own "~22,200 RPM at full-charge 3S (11.1V)" label looked wrong. Per this agent's own "Process" step 1 (never proceed on an unconfirmed datasheet/evidence claim) and explicit instruction not to just accept another role's report, every figure was independently re-derived from primary sources rather than taken on trust. **Confirmed, 3 independent ways**: 11.1V is 3S's nominal voltage (3.7V/cell), not its full-charge voltage (12.6V, 4.2V/cell) — (1) this evidence log's own very next row, DS-MTR-017, already states the correct nominal/full-charge split and was simply never cross-checked against DS-MTR-018 above it; (2) the T-Motor metadata record's own "Known gaps" section independently states "3S full charge (~12.6V)"; (3) a fresh web search plus a second independent retailer re-fetch (new DS-MTR-079) both confirm the standard LiPo per-cell convention. The 22,200 RPM figure was never arithmetically wrong (exactly KV×11.1V) — only its label was. **Fixed**: DS-MTR-018 annotated in place in `datasheets/evidence-log.md` (original figure kept, not deleted, mirroring this file's own DS-MCU-062 annotation precedent — see also this repo's identical-in-kind ECO-016 correction). **Carried further, because a label fix alone is not this design's real answer**: a naive "label-fix-only" recompute (KV×12.6V=25,200 RPM) still ignores both (a) this design's own wider, binding 13.0V J4 envelope ceiling (12.6V plus ~3% bench-supply headroom, SS7.5.9) and (b) the real series voltage drops between J4 and U5's actual VCC pin through F1/D2/U6 (SS7.5.2/SS7.5.9/SS7.5.10) — components that did not exist yet when DS-MTR-018 was originally written. New SS7.5.13 (`hardware/schematic/bench-imu-01-design.md`) derives the true credible-worst-case VCC(U5) using the *opposite* bound from SS7.5.2/SS7.5.10's own existing drop analyses (those ask "could voltage dip too low/heat rise too high" using max-VF/max-R at worst-case 3A operating current; this asks "what is the most voltage that could credibly reach M1" using min-R (F1 Rmin=0.010Ω DS-PROT-006/033; U6 R(ON)min=0.026Ω DS-PROT-031), the lowest credible D2 VF at the motor's actual (not 3A) no-load current (ESTIMATE ≈0.35–0.45V, reasoned from Schottky low-current physics + a re-fetched STPS3L60 datasheet, new DS-PROT-034, whose Figure 13 low-current curve was confirmed to exist but could not be numerically extracted this session — disclosed honestly, not guessed past), and D3 contributing 0V (shunt topology). **Result**: V_VCC(U5)≈12.54–12.64V (point ≈12.59V); RPM≈25,060–25,280 (point ≈25,180) — **≈12.9–13.9% higher** (point ≈13.4%) than the (correctly-labeled) 22,200 RPM figure, and, because stored kinetic energy scales with ω², **≈27–30% more stored energy** (point ≈29%) at the credible worst case, not a merely-proportional increase. Sensitivity-checked: doubling the assumed no-load current (0.3A CONFIRMED T-Motor Io, DS-MTR-018/079 → 0.6A ASSUMPTION bound) changes the RPM answer by under 1% — the result is dominated by D2's VF uncertainty, not the current assumption. Every step is confidence-tagged (CONFIRMED/ASSUMPTION/ESTIMATE, this project's convention) in SS7.5.13 itself. Explicitly not a claim that Firmware's own REQ-405 work (DS-MTR-078) needs a fix — that figure's own use of the unadjusted 13.0V as VCC is conservative in its own, separately-scoped command-ceiling context, the opposite direction of error from this analysis; `firmware/bench-imu-01/` is untouched. | Firmware Reviewer's REQ-405 cross-check flagged the DS-MTR-018 label as suspicious, triggering this agent's own independent, from-primary-sources re-derivation obligation (`.github/agents/circuit-engineer.agent.md` Process step 1) rather than accepting the flag's own analysis as sufficient on its own. | No human approval needed for the citation correction and derivation itself — routine evidence-hygiene correction plus a same-role analysis extension, directly mirroring the already-accepted ECO-016 (citation-hygiene) precedent; no component, schematic topology, or requirement changed. **This ECO does not disposition anything for Mechanical Lead** — the corrected RPM figure is flagged in SS7.5.13 as a required new input for `hardware/mechanical/bench-imu-01-dimensional-spec.md` §8's flywheel physics table (out of this agent's own edit scope, per its "Out of scope" instructions — not touched by this ECO), which in turn is the physics basis for the still-open REQ-403 disposition and the still-open MISS-016 (HIGH) containment-wall-margin finding: a higher credible RPM/energy figure can only widen, not narrow, MISS-016's already-quantified shortfall once Mechanical Lead recomputes against it — flagged for that follow-up, not pre-empted or redispositioned here, and `validation/open-issues.md` is left untouched by this ECO. | `datasheets/evidence-log.md` DS-MTR-018 (annotated/corrected), DS-MTR-079 (new), DS-PROT-034 (new), DS-MTR-080 (new — the aggregated derivation citation); `datasheets/tmotor_mn2206-13-2000kv_rev-unknown.md`, `datasheets/stmicroelectronics_stps3l60_ds2134-rev7.md` (Evidence-ID cross-reference lists updated); `hardware/schematic/bench-imu-01-design.md` new SS7.5.13; MISS-016 (context only, not resolved/redispositioned — see Approved-by column); `hardware/mechanical/bench-imu-01-dimensional-spec.md` §8 (flagged as required-input consumer, not itself edited by this ECO). |
| ECO-023 | 2026-09-13 | Rev 3.2 → Rev 3.3 (`hardware/mechanical/bench-imu-01-dimensional-spec.md`, §8 only) — no `.scad` geometry changed, §8.1 unchanged | **Mechanical Lead applies ECO-022's corrected credible-worst-case motor RPM to §8's REQ-403 physics table and safety-disposition prose; independently re-verified ECO-022's own derivation first, not accepted on report alone.** ECO-022 flagged (but explicitly did not itself apply) a corrected M1 credible-worst-case no-load speed, RPM≈25,060–25,280 (point ≈25,180), superseding the old, correctly-arithmetic-but-mislabeled 22,200 RPM figure §8's physics table was built on. Per this agent's own explicit instruction not to just accept another role's report, this Mechanical Lead independently re-read DS-MTR-017/018/079/080 and DS-PROT-005/006/031/033/034 directly in `datasheets/evidence-log.md`, hand-reproduced the KV(2000 RPM/V)×V_VCC arithmetic and the F1/U6/D2 voltage-drop chain from those primary citations, and independently reconfirmed the 11.1V-nominal-vs-12.6V-full-charge LiPo convention from a fourth, separate source (web search). **Verdict: agree** — real Evidence IDs, sound arithmetic, correct conclusion. One immaterial (~20 RPM, ~0.08%) internal-consistency wrinkle found in ECO-022's own stated range (exact recompute puts the true low bound at ≈25,078–25,080 RPM, not exactly 25,060, since only the former is consistent with the stated point estimate being the range's exact midpoint) — disclosed in §8, changes no conclusion, well inside D2's own VF ESTIMATE band. **Recomputed §8's physics table entirely from this Mechanical Lead's own rotor-inertia/radius data** (I=4.5×10⁻⁵ kg·m², r=0.030m, §4.1/§4.4, unchanged this revision) — not a reuse of ECO-022's own illustrative scaling figures, which independently and closely match this recomputation but are not this section's authoritative source for mechanical inputs. New point-estimate results: ω≈2636.8 rad/s (was 2324.8), KE≈156.44 J (≈70.4× the 3000 RPM baseline, was 121.60J/54.76×), rim tip speed≈79.11 m/s/~285 km/h (was 69.74 m/s/~250 km/h), peak centrifugal stress≈20.20 MPa (was 15.70 MPa) — full range disclosed too (RPM 25,060–25,280 → KE≈154.95–157.69J, rim speed≈78.73–79.42 m/s, stress≈20.01–20.36 MPa). **Disk-burst safety factor re-checked, not assumed unchanged**: yield(250 MPa)/peak-stress is now ≈12.3–12.5× (point ≈12.4×), down from the superseded ~15.9× as expected (stress rose ≈28.65% at the point estimate) but still comfortably non-binding — disk-burst was never, and remains not, the governing failure mode; the discrete hub-collar-release/projectile hazard is unaffected in kind, only in magnitude (now up to ≈79.4 m/s/≈157.7 J credible worst case, not ≈69.7 m/s/≈121.6 J). Every surrounding REQ-403 disposition sentence built on the old figures (the "~20,000–22,200 RPM class"/"~250 km/h" framing, the "~122 J dropped from ~12.4m" illustrative comparison, and the now-false "44–55× matches ISS-020" cross-check) was updated to match. **Explicit MISS-016 (HIGH, OPEN) staleness flag added at the end of §8** (not resolved, not recomputed): MISS-016's own §8.1.2 Method 1 (~0.5–2.4% of budget)/Method 2 (~30–40% of budget) wall-impact-energy-absorption shortfall figures were computed against the now-superseded 121.60J/69.74 m/s/22,200 RPM inputs and are now stale; because the containment wall's absorption capacity is unchanged (no `.scad` geometry touched) while the demanded energy rose ≈27.4–29.7%, the shortfall can only widen, not narrow, once §8.1 is recomputed — that recompute is explicitly **not** performed by this ECO (separate, already-flagged scope) and is left as required follow-up work for Mechanical Lead's own next pass or the Hardware Lead's disposition. §15 item 6's own "Rev 3.2 note" (also quoting the superseded 121.60J figure) and `hardware/schematic/bench-imu-01-design.md` §7.5.11's own now-stale "45–55×" ISS-020 framing were both identified and are both flagged as observations only, not fixed by this ECO (outside its scope). No `.scad` geometry, schematic, BOM, firmware, or `validation/open-issues.md` content was touched. | ECO-022 (DS-MTR-018 correction) explicitly flagged this corrected RPM as "a required new input for Mechanical Lead's flywheel physics table (§8) — not itself applied there" and explicitly deferred the actual application to this ECO; this agent's own core instruction to independently re-derive/re-verify what another role hands off before relying on it, rather than just relaying it forward. | Impact analysis: Mechanical-domain-only change (see `validation/change-impact-matrix.md` ECO-023 entry) — no schematic, BOM, component, or requirement change; REQ-403's own disposition remains an unresolved proposal (status unchanged by this ECO, only its supporting numbers corrected), still awaiting Independent Mechanical Review and the human HITL gate. Directly widens the already-quantified MISS-016 (HIGH) shortfall once §8.1 is eventually recomputed (flagged, not performed here) — re-review of the updated REQ-403 disposition by Independent Mechanical Review is needed before the human gate, and a follow-on §8.1/MISS-016 recompute is needed as a separate, distinct task. **No human approval needed for this analysis-only correction itself** — propagating an already-independently-verified corrected input into an existing physics table/disposition prose is a same-class routine analysis correction, no geometry/topology/component decision made, directly mirroring ECO-022's and ECO-016's own "no human approval needed for a citation/analysis correction" precedent; the REQ-403 disposition's own pending-human-HITL-gate status is unchanged and not itself being approved or closed by this ECO. | `hardware/mechanical/bench-imu-01-dimensional-spec.md` §8 (Rev 3.3, this revision's edits) and its own new "Rev. 3.3 changelog" block (top of file); `hardware/schematic/bench-imu-01-design.md` §7.5.13 (DS-MTR-080, the consumed input); `datasheets/evidence-log.md` DS-MTR-017/018/079/080, DS-PROT-005/006/031/033/034 (independently re-verified, not newly created by this ECO); ECO-022 (the upstream correction this ECO consumes); MISS-016 (HIGH, OPEN, `validation/open-issues.md` — staleness flag only, not resolved/redispositioned by this ECO); `validation/change-impact-matrix.md` ECO-023 entry. |

| ECO-024 | 2026-09-13 | Rev 3.3 → Rev 3.4 (`hardware/mechanical/bench-imu-01-dimensional-spec.md` §8.1 only) — no `.scad` geometry changed, §8's own main physics table (Rev 3.3) unchanged | **Mechanical Lead recomputes MISS-016's own Method 1/Method 2 wall-impact-energy-absorption analysis (§8.1) against ECO-023's corrected 156.44J/79.11 m/s credible-worst-case, closing the staleness flag ECO-023 itself raised but explicitly deferred.** Re-ran both hand-calc methods (Charpy/fracture-toughness energy density; yield-strength-limited plastic work), both reverse cross-checks, the fastener pull-out section, the confidence ledger, and the escalation flag against the corrected energy/rim-speed figures — material property inputs unchanged (still the same real, cited Charpy/yield/pull-out data), only the denominator (disclosed hazard energy) changed. Independently spot-verified by the Hardware Lead before this ECO was logged (arithmetic reproduced exactly, not accepted on report alone): Method 1 now 0.028–1.84% of budget across the 4.5–60mm engagement sweep (was 0.036–2.37% against the smaller superseded budget); reverse cross-check 3,259–16,296mm (1,313–6,566% of the 248.19mm wall circumference), up from 2,533–12,667mm (1,021–5,104%). Method 2 now 1.7–30.7% of budget (was 2.2–39.5%); reverse cross-check 196–258mm (79–104% of circumference) — the upper bound now **exceeds 100%**, a new qualitative fact that did not hold before. **Overall shortfall widened, exactly as ECO-023 predicted, never narrowed**: ≈3.26–4.30× short at best case (was ≈2.53×, per Method 2's most favorable engagement), ≈1.7–3.6 orders of magnitude short at typical/localized engagement (was ≈1.6–3.4 orders, per Method 1's sweep). Fastener pull-out estimate: still plausible for its realistic/secondary loading role; the hypothetical full-direct-hit crush-distance-for-adequacy grew from ≳20mm to ≳26mm. Baseball-KE illustrative comparable: was "within 5%" of the old budget, now only ≈74% of the corrected one. Every remaining "121.60J"/"69.74 m/s" text in the document was confirmed (repo-wide grep) to be an explicitly-labeled historical "(was ...)" comparison, not a live uncorrected citation. Same-cycle repo-wide propagation of the ECO-022/023 correction to every other citing document, closing MISS-019 (partial)/MISS-020/MISS-021: `validation/fmea.md` FMEA-009 (the risk register's highest-RPN entry) updated to the corrected hazard magnitude (RPN scoring/qualitative framing preserved unchanged); `hardware/mechanical/bench-imu-01-manufacturing-spec.md` updated at 7 locations (Manufacturing Engineer's own fix, independently cross-checked and confirmed RESOLVED by a fresh Mechanical Reviewer pass — 100%-infill/gyroid/6-perimeter/nylon-or-PETG process recommendation and the "FDM not validated without physical testing" disclaimer both confirmed unchanged in substance, the corrected higher figures reinforcing rather than undercutting the existing conservatism); `hardware/schematic/bench-imu-01-design.md` §1 changelog bullet and §7.5.11's own "45–55×"/"6–7×" ISS-020 framing corrected to "44–70×"/"6.7–8.4×"; `requirements/requirements.md` REQ-405 row and `requirements/traceability-matrix.md` REQ-405/REQ-403 rows corrected; `bom/component-selection.md`'s motor-comparison table annotated (not rewritten, preserving the original Component-Selection-era decision record) with a correction note. `firmware/bench-imu-01/` source-code comment citations (MISS-019's remaining scope) deliberately deferred to a future Firmware Engineer dispatch — low priority, non-safety-degrading (understates rather than overstates the true margin), does not block any gate. | ECO-023's own explicit staleness flag at the end of §8, naming MISS-016's own Method 1/Method 2 figures as stale and requiring this exact follow-up recompute before MISS-016 could be considered ready for the human's REQ-403 review with accurate figures. | Recompute/propagation work itself: no separate human approval needed — a same-class analysis correction propagating an already-independently-verified corrected input (ECO-022/023) into MISS-016's own already-tracked estimate and its citing documents, no new geometry/topology/component decision made, mirroring ECO-023's own "no human approval needed" precedent. MISS-016's own disposition (what to do about the now-final, corrected shortfall figures) remained, at the time of this ECO, still explicitly un-decided — new information for the still-outstanding Chief Engineer REQ-403 sign-off, not a resolution of it (see ECO-025 for that sign-off, received the same day). | `hardware/mechanical/bench-imu-01-dimensional-spec.md` §8.1 (Rev 3.4); `hardware/mechanical/bench-imu-01-manufacturing-spec.md` (7 locations); `validation/fmea.md` FMEA-009; `hardware/schematic/bench-imu-01-design.md` §1/§7.5.11; `requirements/requirements.md`, `requirements/traceability-matrix.md`; `bom/component-selection.md`; `validation/open-issues.md` MISS-016 (Issue/Rationale/Notes updated to the Rev 3.4 figures, status unchanged by this ECO — remained OPEN), MISS-019 (partial)/MISS-020/MISS-021 (RESOLVED). |

| ECO-025 | 2026-09-13 | Bench-IMU-01 Rev 3 (Electronics/Mechanical/Firmware) — **DESIGN COMPLETE GATE GRANTED** | **Human Chief Engineer's REQ-403 safety disposition received and recorded — the first physical-hazard subsystem in this project's history to reach a human safety disposition.** Kyosuke reviewed the full REQ-403 material as one integrated package: the containment topology (2 Independent Mechanical Review cycles), the manufacturing-process adequacy assessment (Manufacturing Engineer's spec, 2 independent Mechanical Reviewer cross-checks, honest "cannot be presented as adequate without physical testing" conclusion), and the corrected, final load case (ECO-022/023/024: 156.44J/79.11 m/s, MISS-016's own quantified ≈3.26–4.30×/≈1.7–3.6-orders-of-magnitude wall-margin shortfall). **Disposition: ACCEPTED-RISK.** The containment structure is approved as a **defense-in-depth mitigation** — explicitly on the basis that "something is better than nothing," not on a claim that the calculated shortfall has been resolved or that containment adequacy has been proven. Real physical drop/impact destructive testing is explicitly deferred as a tracked future task (new finding **MISS-022**, LOW, `Source: human`, non-gating), not silently folded into "resolved." `validation/open-issues.md` MISS-016 changed `OPEN` → `ACCEPTED-RISK` with the full sign-off recorded in its own Notes; `requirements/traceability-matrix.md` REQ-403 changed `Pending` → `Verified — human-accepted disposition` (same wording precedent as REQ-402/ECO-005). With this, **all 5 Design Complete conditions (`docs/architecture.md` §8) are now met for Rev 3**: (1) zero unresolved CRITICAL — confirmed, `tools/check_open_issues.py` reports 0 CRITICAL findings across the revision; (2) every HIGH is `RESOLVED` or `ACCEPTED-RISK` with named human sign-off — confirmed, MISS-016 is the only HIGH that was OPEN and is now ACCEPTED-RISK with this entry's own recorded sign-off, joining the already-signed ISS-002/ISS-020/ISS-021; (3) `requirements/traceability-matrix.md` shows 100% `Verified`/`Waived` across all 45 rows — confirmed by direct count, zero `Pending` rows remain; (4) `validation/fmea.md` reviewed for this revision — satisfied by the 2026-09-11 closeout pass (this file's own §"Condition 4...is satisfied by this pass" note), with FMEA-009's own cited magnitude figures kept current via ECO-024's propagation, not requiring a fresh review cycle; (5) this entry, human-signed. **`tools/check_open_issues.py` and `tools/check_id_uniqueness.py` both re-run and confirmed clean before this entry was written** (0 unresolved CRITICAL/unsigned-off HIGH across 49 findings; 0 duplicate IDs across 400). | REQ-403's own explicit human safety-critical-change HITL gate (`docs/architecture.md` §10), presented as an explicit decision package alongside MISS-016's corrected figures — not self-approved by any AI role at any point in this design cycle, consistent with this project's repeatedly-reaffirmed HITL discipline throughout Rev 3 (a delegate explicitly and repeatedly declined to substitute their own judgment for this exact reserved decision until Kyosuke's own direct decision arrived). | **Human Chief Engineer (Kyosuke), 2026-09-13**, received directly (not a delegate's own call) and relayed via the cross-session HITL channel: "ACCEPTED-RISK... approved as a defense-in-depth mitigation... explicitly on the basis that 'something is better than nothing'... Real physical drop/impact testing is deferred as a future task, not a blocker for this revision." This single decision both dispositions MISS-016/REQ-403 and constitutes the human sign-off required by Design Complete condition 5. | `validation/open-issues.md` MISS-016 (ACCEPTED-RISK), MISS-022 (new, LOW, `Source: human`, tracked future physical-testing task); `requirements/traceability-matrix.md` REQ-403 (Verified — human-accepted disposition); supersedes ECO-024's "still-pending"/"un-decided" notes — **Design Complete is now GRANTED for Rev 3**. |
| ECO-026 | 2026-09-01 | N/A (Requirements Rev 4 + new Component Selection section — planning/comparison artifacts only; no Mechanical Design/CAD, no schematic revision, Rev 3 itself untouched) | **New task: free-rotation support mechanism for the 1-axis attitude-control roadmap stage** (`docs/architecture.md` §11/§14), narrowly scoped to Requirements Engineering + Component Selection per explicit human direction (relayed via the creator/"General Chat" session). Confirmed via repo-wide search that Rev 3's mechanical design has zero pivot/gimbal/bearing mechanism — the board+motor+flywheel assembly is mounted rigidly to the bench, so no reaction-wheel torque could ever produce an observable attitude change. Added: `requirements/requirements.md` new §1b (Rev 4 scope note), 12 new requirements (REQ-011–014 Functional, REQ-113 Electrical, REQ-205 Environmental, REQ-310–311 Mechanical, REQ-407–408 Safety, REQ-505–506 Non-functional), §9d (2 new Open Questions: angular-travel target, mechanism BOM ceiling), §9e (creator-session physics-finding cross-check record), §10b (pending approval table); `requirements/traceability-matrix.md` — 12 new rows, `Pending` except 3 directly-checkable anti-scope/non-extension facts marked `Verified` (mirrors REQ-006/009's own precedent); `bom/component-selection.md` new "Free-Rotation Support Mechanism" section comparing 4 real candidates (lazy-susan turntable bearing, dual-608ZZ ball-bearing pivot, torsion-wire suspension, air bearing) with a worked mass ESTIMATE (≈280–320g total rotating assembly, since Rev 3 never estimated the enclosure's own plastic mass) and a worked platform-angular-rate physics finding (I_wheel/I_platform≈1:15, ≈12–1170°/s illustrative range) — recommending Candidate B (dual-608ZZ pivot), A as fallback; `datasheets/` — 6 new metadata records (DS-BRG-001–006: BC Precision lazy-susan + 608ZZ product pages, SKF bearing-friction reference, Charles' Labs reaction-wheel-demo precedent, EyasSat + Tensor Tech air-bearing cost context) plus 1 updated (`prusament_petg_tds-2021-10.md`, new DS-MTL-004 density citation); `datasheets/evidence-log.md` — 8 new Evidence ID rows. | Human Chief Engineer's own project roadmap (`requirements/requirements.md` §1, `docs/architecture.md` §11) explicitly names "1-axis attitude control" as the next stage after Rev 3's Reaction Wheel milestone; this ECO documents the first concrete step toward it — the physical precondition (free rotation), not the control loop itself (REQ-014's explicit anti-scope statement, mirrors REQ-009). | **Pending human Chief Engineer decision** on (a) §9d's two Open Questions (angular-travel target, mechanism BOM ceiling) and (b) the Free-Rotation Support Mechanism candidate choice (`bom/component-selection.md`) — an architecture-level decision per `docs/architecture.md` §10, mirroring the Power Architecture HITL gate (ECO-007's own precedent: "Pending human Chief Engineer decision..., but no architecture is approved by this ECO alone"). Hardware Lead relays the requirements and recommendation via cross-session message; **no Mechanical Design/CAD work starts until that decision arrives.** | REQ-011 through REQ-506 (Rev 4 rows, `requirements/requirements.md` §2–§10b); DS-BRG-001–006, DS-MTL-004 (`datasheets/evidence-log.md`); does **not** touch or reopen REQ-403/MISS-016 (`validation/open-issues.md` unchanged) or any Rev 1/2/3 content. |
| ECO-027 | 2026-09-01 | N/A (same-day revision of ECO-026's own recommendation — no Mechanical Design/CAD, no schematic revision, still Requirements/Component Selection planning artifacts only) | **Recommendation swap following direct human priority clarification** (`requirements/requirements.md` §9f, relayed via the creator/"General Chat" session): the actual driving goal for this stage is speed to a real, physical, demonstrable 1-axis result — 1-axis was deliberately chosen over 3-axis specifically because it's the *faster* path, not just the more careful one; favor the cheapest/simplest-to-source mechanism over anything needing custom fabrication or long lead times; keep the deliverable lean; do not reopen Rev 3's motor+flywheel selection. Since ECO-026's own Candidate B (dual-608ZZ pivot) recommendation explicitly required a **future custom shaft/mount design** step before being buildable — slower to an actual physical result than Candidate A (bolt-on-ready, zero custom design work) — the recommendation is revised: **`bom/component-selection.md`'s Free-Rotation Support Mechanism section now recommends Candidate A primary, Candidate B as the documented alternative** (Success-probability ranking, Recommendation, and Approval table all updated in place; Escalation flags and the underlying comparison arithmetic/evidence citations are unchanged — the facts didn't change, only which fact is weighted highest). Candidate D's (air bearing) table exposition was also condensed, per the same "keep it lean" instruction. `requirements/traceability-matrix.md` REQ-011's row updated to match. `requirements/requirements.md` §9f records the human's clarification verbatim/close-paraphrase; §10b gained 2 new rows recording receipt of the clarification and the Hardware Lead's resulting revision. | Human Chief Engineer's own explicit priority steer, received after ECO-026 was already committed — recorded as an honest revision (superseding ECO-026's recommendation, not silently overwriting it), consistent with this project's own practice of never editing a past ECO's record. | **Still Pending human Chief Engineer decision** on §9d's two Open Questions and the actual mechanism choice — this ECO changes *which candidate is recommended*, not who decides; no Mechanical Design/CAD work starts until that decision arrives. | REQ-011 (`requirements/traceability-matrix.md`, updated); `bom/component-selection.md` Free-Rotation Support Mechanism section (Success-probability ranking, Recommendation, Approval); does **not** touch or reopen REQ-403/MISS-016, the Motor/Motor Driver Component Selection sections, or any Rev 1/2/3 content. Supersedes ECO-026's "recommending Candidate B... A as fallback" line — see this entry for the current recommendation. |
| ECO-028 | 2026-09-01 | Bench-IMU-01 Rev 4 requirements — **REAL HUMAN SIGN-OFF RECEIVED, both Open Questions resolved + architecture decision made** | **Kyosuke's actual decisions on §9d's two Open Questions and the Free-Rotation Support Mechanism candidate choice, received directly and relayed via the creator/"General Chat" session.** (1) REQ-012 (angular-travel target): approved as proposed, ≥180°/ideally continuous, no wording change. (2) REQ-505 (mechanism BOM ceiling): **waived** — no ceiling imposed this cycle, cost is not a gating factor; REQ-505's own row changed `Should` → `Won't (this cycle)`, recorded as an explicit human waiver (not a silent drop), REQ-506's Notes corrected to no longer describe Mechanical Design as blocked. (3) Mechanism choice: **Candidate A (BC Precision 4LS-3 lazy-susan turntable bearing, $13) confirmed** — matches the Hardware Lead's own ECO-027 revised recommendation. `requirements/requirements.md` §9g records all 3 decisions verbatim/close-paraphrase; §10b's Approval table closed out from "awaiting review" to the real signed decision; `bom/component-selection.md`'s own Free-Rotation Support Mechanism Approval table likewise closed out. `requirements/traceability-matrix.md` REQ-011/012/505 rows updated to reflect the resolved status. **Kyosuke also authorized the next phase: Mechanical Design** — integrating Candidate A into a new Bench-IMU-01 enclosure revision is now in scope (previously explicitly out of scope, §1b/§9f) — via Mechanical Lead + Independent Mechanical Review, same rigor as Rev 3. Other scope fences (no control loop/PID/attitude estimation, no 2nd/3rd reaction wheel, no Control Engineer introduction) remain explicitly active and unchanged. | REQ-012/REQ-403's own Human-in-the-loop pattern for architecture-level and requirements-sign-off decisions (`docs/architecture.md` §10) — the mechanism choice is a major/architecture-defining component decision, exactly the class of decision this framework reserves for the human. | **Human Chief Engineer (Kyosuke), 2026-09-01**, received directly and relayed via the cross-session HITL channel — see `requirements/requirements.md` §9g and §10b, and `bom/component-selection.md`'s Free-Rotation Support Mechanism Approval table, for the full verbatim/close-paraphrase record. | REQ-011, REQ-012, REQ-505 (`requirements/traceability-matrix.md`, all updated); `requirements/requirements.md` §9g/§10b; `bom/component-selection.md` Free-Rotation Support Mechanism Approval table; does **not** touch or reopen REQ-403/MISS-016 or any Rev 1/2/3 content. Mechanical Design work (Rev 4 of the enclosure) begins as its own next phase/ECO, not part of this entry. |
| ECO-029 | 2026-09-14 | Bench-IMU-01 Rev 3 → Rev 4 (mechanical only — `bench-imu-01-enclosure.scad`, `bench-imu-01-dimensional-spec.md` §18, `hardware/mechanical-interface.md` Part C) | **First Mechanical Design pass integrating the human-approved Candidate A (BC Precision 4LS-3 lazy-susan bearing, ECO-028) into the enclosure — additive only, per REQ-311.** Delegated to a `mechanical-lead` sub-agent (independent invocation, full brief including a Hardware-Lead-computed preliminary CG estimate and a real finding that the flywheel bay's own Z=0 floor footprint (≈87mm dia) is smaller than the bearing (101.6mm dia)). New: `bmount_flange()` (a new solid ring fused below the existing base's floor, bolting to the bearing's rotating top plate), `stand_plate()` (a genuinely new 4th printed piece, bolting to the bearing's stationary bottom plate, sized via a real computed CG/tip-over analysis rather than the bearing's own generic 12–25in furniture-scale suggestion — chosen 120mm diameter, 6.2× static margin against a computed ≈601.8g total system mass and ≈14.3mm CG-to-axis offset), `reference_bearing()` (visual-only). Self-disclosed a real, honestly-characterized discrepancy: the Mechanical Lead's own analytic plastic-mass computation (≈207.9g) came out materially higher than the Hardware Lead's earlier bounding-shell-surface-area ESTIMATE (130–170g) — presented as a finding for review, not silently reconciled. Self-disclosed a genuine manufacturability finding (an internal, hidden ≈56mm unsupported overhang at the base/flange transition, needing slicer-generated internal support material — 3 alternatives considered and rejected before accepting this with a disclosed caveat). Verified via direct `openscad --backend=manifold` render (both `show_mode`s, manifold/NoError). **Independently reviewed by a separate `mechanical-reviewer` sub-agent invocation (Cycle 5, `validation/design-review.md`)**: verdict **CONDITIONAL** — 0 CRITICAL, 2 HIGH (MISS-023: REQ-407(b) pinch-point/rotating-overhang hazard never assessed, independently derived a ≈115.9mm swept radius vs. the 60.0mm stand plate; MISS-024: REQ-407(c)/REQ-113 cable-entanglement hazard never assessed for now-rotating J1/J4 connectors), 1 MEDIUM (MISS-025, no fastener-load calc), 3 LOW (MISS-026/027/028, minor documentation/arithmetic gaps) — all newly opened in `validation/open-issues.md`, none resolved this ECO. | REQ-011/012/113/310/311 (`requirements/requirements.md`) require the mechanical integration this ECO delivers; `docs/architecture.md` §8/§10's loop-back rule (any CRITICAL/HIGH finding routes back to the design role, then requires a fresh Reviewer verdict) governs the CONDITIONAL outcome — not yet eligible for Design Complete. | Not yet human-approved — this ECO records design + independent review work, not a completion gate. Design Complete for this revision remains blocked pending MISS-023/024's resolution (see ECO-030) and, ultimately, human sign-off. | `hardware/mechanical/bench-imu-01-enclosure.scad`, `hardware/mechanical/bench-imu-01-dimensional-spec.md` §18.0–§18.11, `hardware/mechanical-interface.md` Part C1–C8, `datasheets/evidence-log.md` (new `DS-BRG-007`), `datasheets/lily-bearing_lazy-susan-hardware-kit_web-article.md` (new); `validation/open-issues.md` MISS-023 through MISS-028 (new, opened by the independent Reviewer, not this ECO's own author); `validation/design-review.md` "Mechanical Reviewer — Cycle 5" entry. Does **not** touch `requirements/traceability-matrix.md` or Rev 1/2/3 electronics/firmware content. |
| ECO-030 | 2026-09-15 | Bench-IMU-01 Rev 4 → Rev 4.1 (mechanical only, same files as ECO-029, additive) | **Loop-back fix pass addressing ECO-029's 2 open HIGH findings (MISS-023, MISS-024), per the Design Complete Gate's own loop-back rule** — delegated to a fresh `mechanical-lead` sub-agent invocation with the full Cycle-5 finding text as its brief. For MISS-023: independently re-derived the swept radius at **126.424mm** (correcting the Reviewer's own 115.9mm — a real, disclosed correction, not a dispute), added `pinch_guard()` (a new stationary, 4-quadrant annular guard ring around the hazard zone, 60–115mm radius, covering a computed 77.7% of the true hazard band, backstopped by an operational keep-clear warning for the disclosed 11.4mm residual gap) — **self-assessed as "partially closed, not fully closed,"** explicitly declining to self-declare RESOLVED. For MISS-024: added a 3-turn limit before mandatory manual re-centering (mirrors REQ-113's own existing language), a 2.5m minimum external service-loop spec, `rotation_index_pointer()` (a visual turn-counting witness mark), and `cable_anchor_tab()`×2 (zip-tie strain relief) — self-caught and fixed its own wall-thickness defect along the way (disclosed, not hidden) — **self-assessed as RESOLVED for REQ-113's own explicit scope, not for REQ-012's fuller "ideally continuous" aspiration** (a standing, previously-understood limitation, not a new gap). Self-disclosed the new `pinch_guard()` feature's own mass (≈570.6g) as larger than every other Rev 4 addition combined, bringing total system mass to ≈1173.4g — flagged, not silently absorbed. **Independently re-reviewed by a fresh `mechanical-reviewer` sub-agent invocation (Cycle 6, `validation/design-review.md`)**, which re-derived essentially everything from raw geometry rather than trusting the Lead's own report (confirmed the 126.424mm correction was itself right; independently computed `pinch_guard()`'s exact mass/coverage/collision-clearance; independently re-read REQ-407(c)/REQ-113's exact text to judge the Lead's own "REQ-113 scope, not REQ-012" framing as honest, not a dodge): verdict **CONDITIONAL** — **MISS-024 → RESOLVED**; **MISS-023 → stays OPEN/HIGH** (judged the residual 11.4mm gap + an unfastened, driftable guard an inadequate closure for a HIGH safety finding, on top of the Mechanical Lead's own already-disclosed hedging) — this is the one finding still blocking Design Complete. The Reviewer also independently discovered **MISS-029** (LOW, not mentioned in either Mechanical Lead report): `bom/component-selection.md`'s Candidate A/B friction-torque margin citations (comparison table, Recommendation prose, and success-probability ranking) were stale, computed against a placeholder ≈300g mass that pre-dates Rev 4/4.1 entirely, understating the true load by ≈35%; recomputed margin ≈21.5× (Candidate A) / ≈242× (Candidate B), both still enormously non-blocking against REQ-007's ≥5 mN·m budget — **fixed by the Hardware Lead this same pass** (all 3 stale citations in `bom/component-selection.md` updated to the recomputed figures, independently re-verified by hand; MISS-029 marked RESOLVED). `tools/check_id_uniqueness.py` (415 IDs, 0 duplicates) and `tools/check_open_issues.py` re-run — the latter fails **only** on MISS-023, exactly matching the Reviewer's own verdict (an independent cross-check that the two files are internally consistent). | Same loop-back rule as ECO-029; MISS-023's continued OPEN/HIGH status is exactly the kind of safety-critical residual-risk situation this project's own REQ-403/MISS-016 precedent (Rev 3, ECO-024/025) already established a resolution pattern for — present the real, quantified trade-off to the human Chief Engineer for an explicit ACCEPTED-RISK-or-further-mitigation decision, do not resolve it unilaterally. | **Not yet human-approved.** MISS-023 is reported to Kyosuke (via cross-session message) as an explicit safety-critical decision point, mirroring the REQ-403/MISS-016 HITL gate precedent — pending his choice between (a) accepting the current `pinch_guard()` mitigation's disclosed residual risk as ACCEPTED-RISK (a named human sign-off, which no AI role in this project can supply), or (b) directing a further Mechanical Lead iteration to close the residual gap more completely. Design Complete for this revision remains blocked until that decision is made and, if (a), signed. | `hardware/mechanical/bench-imu-01-enclosure.scad` (Rev 4.1 additions), `hardware/mechanical/bench-imu-01-dimensional-spec.md` §18.12–§18.17, `hardware/mechanical-interface.md` C9 + Open items 20–27; `validation/open-issues.md` MISS-023 (Notes appended, status unchanged/OPEN), MISS-024 (→RESOLVED), MISS-029 (new, →RESOLVED same pass); `validation/design-review.md` "Mechanical Reviewer — Cycle 6" entry; `bom/component-selection.md` (3 stale friction-margin citations corrected, MISS-029's own fix). Does **not** touch `requirements/traceability-matrix.md` (see separate update this same session) or Rev 1/2/3 electronics/firmware content. |
| ECO-031 | 2026-09-15 | Bench-IMU-01 Rev 4 (Mechanical: free-rotation support mechanism) — **DESIGN COMPLETE GATE GRANTED (mechanical scope)** | **Human Chief Engineer's MISS-023 safety disposition received and recorded — the second physical-hazard subsystem in this project's history to reach a human safety disposition (first: REQ-403/MISS-016, Rev 3, ECO-024/025).** Kyosuke reviewed the full Cycle-6 material (`pinch_guard()`'s 77.7%-by-area coverage of the true 126.424mm-radius hazard band, the disclosed 11.4mm residual gap concentrated at the outer/highest-velocity edge, the guard's own disclosed lack of a positive retention/locating feature) as one integrated package. **Disposition: ACCEPTED-RISK.** `pinch_guard()`'s current design is approved as a **defense-in-depth mitigation** — explicitly on the basis of real, disclosed, partial risk reduction, **not** on a claim that the residual gap has been closed or that the guard's real-world durability has been proven — the same disposition class as MISS-016/REQ-403, not a new pattern. Further geometric closure (extending `pinch_guard_or` toward 126.424mm, adding a retention feature) is recorded as an available future refinement, not a requirement of this disposition. `validation/open-issues.md` MISS-023 changed `OPEN` → `ACCEPTED-RISK`, with the full sign-off recorded in its own Notes; `requirements/traceability-matrix.md` REQ-407 changed `Pending` → `Verified — human-accepted disposition` (same wording precedent as REQ-403), and REQ-011/REQ-205 (both blocked on MISS-023) updated to match. `validation/fmea.md` received its own Rev 4 pass this same cycle: 4 new systemic-risk entries (FMEA-013 through 016) covering `pinch_guard()` positional drift, the cable turn-limit human-factors risk, dynamic-disturbance tip-over (an honest analysis gap, not a confirmed problem), and the bearing-mount fastener-load gap (MISS-025's own systemic counterpart) — satisfying Design Complete Gate condition 4. With this, **4 of the 5 Design Complete conditions (`docs/architecture.md` §8) are met for this Rev 4 mechanical scope**: (1) zero unresolved CRITICAL — confirmed, `tools/check_open_issues.py` reports 0 CRITICAL across 56 findings; (2) every HIGH is `RESOLVED` or `ACCEPTED-RISK` with named human sign-off — confirmed, MISS-023/024 (the only 2 HIGH findings opened this cycle) are now ACCEPTED-RISK/RESOLVED respectively; (3) `requirements/traceability-matrix.md` — **99% Verified/Waived, one disclosed exception**: REQ-013 remains `Pending` because its own firmware-behavioral component (speed-increment/FSR-range-selection logic) has not been implemented — Firmware Engineer has not been engaged for this Rev 4 work at all. Per this framework's own established precedent (Firmware Bring-up, Phase 2/`docs/workflow.md` Phase 11, is a separate phase that does not feed back into the same Design Complete Gate Electronics/Mechanical share), this is judged **not to block a mechanical-scope Design Complete determination** — but is flagged here explicitly, not silently interpreted in this cycle's own favor without disclosure; (4) `validation/fmea.md` reviewed for this revision — satisfied by this cycle's own pass (above); (5) this entry, human-signed. **`tools/check_open_issues.py` and `tools/check_id_uniqueness.py` both re-run and confirmed clean before this entry was written** (0 unresolved CRITICAL/unsigned-off HIGH across 56 findings; 0 duplicate IDs across 415). | MISS-023's own explicit human safety-critical-change HITL gate (`docs/architecture.md` §10), presented as an explicit decision package (Cycle 6's full findings) — not self-approved by any AI role at any point in this design cycle, consistent with this project's repeatedly-reaffirmed HITL discipline (mirrors REQ-403/MISS-016's own precedent exactly, down to the "defense-in-depth, not proven-adequate" framing). | **Human Chief Engineer (Kyosuke), 2026-09-15**, received directly and relayed via the cross-session HITL channel: "MISS-023 → ACCEPTED-RISK. Proceed with the current `pinch_guard()` design as the disclosed, partial mitigation. Same disposition class as MISS-016/REQ-403." This single decision both dispositions MISS-023/REQ-407(b) and constitutes the human sign-off required by Design Complete condition 2. | `validation/open-issues.md` MISS-023 (ACCEPTED-RISK); `validation/fmea.md` FMEA-013 through 016 (new); `requirements/traceability-matrix.md` REQ-011, REQ-205, REQ-407 (all → `Verified — human-accepted disposition`); REQ-013 remains `Pending` — explicitly disclosed as a future-Firmware-phase item, not silently folded into this scope. Supersedes ECO-030's "still Pending human Chief Engineer decision" note — **Design Complete is now GRANTED for Bench-IMU-01 Rev 4's mechanical scope**; a full Rev 4 (Electronics/Mechanical/Firmware)-wide Design Complete claim, mirroring Rev 3's own ECO-025 framing, awaits a future Firmware Bring-up phase for REQ-013. |
| ECO-032 | 2026-09-15 | N/A (post-Design-Complete artifact export — no `.scad` geometry, dimension, or module body changed) | **Print-ready STL export of the complete current mechanical assembly**, per Kyosuke's own request following ECO-031's Design Complete grant. 5 distinct printed pieces exported to new `hardware/mechanical/stl/` (binary STL, `--export-format binstl`): (1) `bench-imu-01-base-assembly.stl` — Rev 3's `base()` fused with Rev 4's `bmount_flange()`/`rotation_index_pointer()`/`cable_anchor_tab()`×2, printed as one combined job; (2) `bench-imu-01-pcb-lid.stl` — Rev 3 `pcb_lid()`, unchanged; (3) `bench-imu-01-containment-cap.stl` — Rev 3 `containment_cap()`, unchanged; (4) `bench-imu-01-stand-plate.stl` — Rev 4 `stand_plate()`; (5) `bench-imu-01-pinch-guard-quadrant.stl` — Rev 4.1 `pinch_guard(0)`, one quadrant (print ×4 — all 4 quadrants confirmed geometrically identical by direct module-body inspection, not assumed). Each exported via a small, committed wrapper script (`hardware/mechanical/stl/export/*.scad`) that isolates the exact same module call(s) the parent file's own `print_layout` show_mode already uses, without editing that file — verified via `openscad --backend=manifold` (all 5: `Status: NoError`, manifold, Genus/vertex/facet counts independently re-checked) and `trimesh` bounding-box cross-checks against the source file's own named variables (all match exactly, e.g. `stand_plate.stl` 120.0×120.0mm = `stand_plate_or`×2). `bench-imu-01-base-assembly.stl` shows `trimesh`-reported `watertight=False` — the same already-diagnosed, benign coincident-edge-seam tessellation artifact the Mechanical Lead found during Rev 4's own render verification (not a real manifold defect; OpenSCAD's own CGAL check independently confirms `NoError`). All caveats already on record (internal-overhang support-material need, MISS-023/024/025's own dispositions) still apply and are cross-referenced in the new `hardware/mechanical/stl/README.md`, not silently dropped by this export. | Kyosuke's own explicit deliverable request following Design Complete (ECO-031) — a tangible, print-ready output for the mechanical revision this Design Complete grant covers. | Not a design decision requiring separate sign-off — a derived-artifact export of already-approved, already-Design-Complete geometry (Kyosuke's own request constitutes authorization). | `hardware/mechanical/stl/` (new: 5 `.stl` files + `README.md` + `export/*.scad` wrapper scripts, 5 files); does **not** touch `hardware/mechanical/bench-imu-01-enclosure.scad`'s own module bodies, `requirements/traceability-matrix.md`, or `validation/open-issues.md`. |
| ECO-033 | 2026-09-16 | N/A (post-Design-Complete visual-documentation generation — no `.scad` geometry, dimension, or module body changed) | **Assembly instructions, 2D orthographic drawings, and an exploded assembly view for the complete current mechanical assembly**, per Kyosuke's own request for real, buildable visual documentation of the already-Design-Complete Rev 3 + Rev 4/4.1 design. New: `hardware/mechanical/assembly-instructions.md` (a numbered build procedure extending the existing §14/§18.9 assembly-order analyses with fastener call-outs cross-referenced from the `.scad` file, `datasheets/evidence-log.md` `DS-BRG-001`/`DS-BRG-007`/`DS-FAST-001`, and the still-open PCB branch's `bom/bench-imu-01-fab-bom.csv` MH1 line item, read-only; restates the existing REQ-403/`MISS-016` and REQ-407(b)/`MISS-023` ACCEPTED-RISK dispositions honestly rather than re-litigating them; flags the PCB-install step as an explicit placeholder since no fabricated board exists yet, `ISS-036` open; independently cross-referenced the PCB-lid fastener count and found the existing dimensional-spec §14 step 2 text ("6× M2.5") does not match the 4 corner tab positions actually modeled in `.scad` — flagged transparently as a minor, non-gating documentation-precision note in the new document, not silently "corrected" in the source spec, since resolving it is a documentation call, not a new engineering finding). New `hardware/mechanical/drawings/` directory: `scad/` (6 new wrapper scripts isolating each printed piece + the bearing reference in its real assembled-frame position, `include`-only against the parent file, mirroring `stl/export/*.scad`'s own convention), `2d/` (19 PNGs: top/front/side per printed piece + top/front/side/iso of the full assembled unit, via OpenSCAD's own `--projection=ortho --camera=...` CLI rendering — every image visually inspected before inclusion), and `exploded/` (a Blender-built exploded view, `build_exploded_view.py` + `bench-imu-01-exploded-view.png`, using the `blender-*` MCP tools — confirmed connected this session, v5.1.1 — since OpenSCAD has no native "explode" primitive; parts' assembled-frame relationship was independently verified against `hardware/mechanical/stl/README.md`'s own bounding-box table, exact match on all 6 pieces, before any explode offset was applied). Small honest addendum to `hardware/mechanical/README.md` noting Blender's per-session-verified connection, without overwriting the prior session's own accurate "not connected" historical record. | Kyosuke's own explicit deliverable request for assembly instructions, 2D drawings, and an exploded view of the already-Design-Complete Rev 3/Rev 4/4.1 mechanical design — a documentation gap this project had not previously addressed (its first 2D drawings and first exploded view). | Not a design decision requiring separate sign-off — a derived-documentation generation pass over already-approved, already-Design-Complete geometry (Kyosuke's own request constitutes authorization), mirroring ECO-032's own precedent exactly. | `hardware/mechanical/assembly-instructions.md` (new); `hardware/mechanical/drawings/` (new: `README.md`, `scad/` 6 files, `2d/` 19 files, `exploded/` 2 files); `hardware/mechanical/README.md` (addendum only); `validation/change-log.md` (this entry). Does **not** touch `hardware/mechanical/bench-imu-01-enclosure.scad`'s own module bodies, `hardware/schematic/**`, `hardware/pcb/**`, `firmware/**`, `requirements/traceability-matrix.md`, or `validation/open-issues.md`. |

## Notes

- Also use this log to record the outcome of a Conflict Resolution /
  Deadlock Escalation (`docs/workflow.md` §3) whenever it changes something
  already designed.
- `docs/evaluation.md` uses ECO count per revision cycle as a "Change
  Churn" proxy for design stability/rework — keep entries granular enough
  for that to be meaningful (don't bundle unrelated changes into one ECO).
