# Open Issues — Living Finding Backlog

This is the single persistent backlog of Hardware Reviewer, Mechanical
Reviewer (Phase 1 — `docs/architecture-evolution.md` §31), and `rubber-duck`
findings across all review cycles — one shared backlog across disciplines,
not one per discipline. `validation/design-review.md` is the per-cycle
report; this file is what actually gates Design Complete
(`docs/architecture.md` §8) and what CI checks
(`.github/workflows/hardware-gate.yml` + `tools/check_open_issues.py`).

**Keep the table header and column order below exactly as-is** — the CI
gate script parses this file as a Markdown table.

## SLA Policy

*(Set by the human Chief Engineer per project — the framework does not
hard-code day counts.)*

| Severity | Target time to resolution |
|---|---|
| CRITICAL | SLA: [human sets] |
| HIGH | SLA: [human sets] |
| MEDIUM | SLA: [human sets] |
| LOW | SLA: [human sets] |

## Rules

- **CRITICAL** can only reach `RESOLVED` — never `ACCEPTED-RISK`
  (`docs/architecture.md` §8).
- **HIGH** may become `ACCEPTED-RISK` only with a named human Chief Engineer
  sign-off, written rationale, and date (recorded in the `Notes` column or
  cross-referenced to `validation/change-log.md`).
- **MEDIUM/LOW** don't block Design Complete but should still get a
  disposition (`RESOLVED` / `DEFERRED` / `ACCEPTED-RISK`) before archiving a
  revision.
- `Source` distinguishes which review lens found it: `hardware-reviewer`
  (Electronics checklist/failure-mode review), `mechanical-reviewer`
  (Mechanical checklist/failure-mode review, Phase 1 —
  `docs/architecture-evolution.md` §31), or `rubber-duck` (premise/
  assumption review) — see `docs/architecture.md` §5.1. Never merge/relabel
  one as another.

## Backlog

| ID | Severity | Status | Title | Component | Datasheet Source (Evidence ID) | Failure Mechanism | Recommended Fix | Source | SLA Target | Opened | Resolved | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ISS-001 | HIGH | RESOLVED | LDO (U3) EN pin connection left ambiguous, not firmly committed | U3 (TLV75533PDBVR LDO) | DS-PWR-002/003 | Floating EN (no internal bias, high-impedance input per TI SBVS320D §5.5) leaves enable state undetermined — rail may never enable (dead board) or chatter/brown out from coupled noise | Make EN→VIN an explicit, firm, unconditional net (zero-cost fix; pin 3 confirmed mandatory on all package variants, no internal bias) | hardware-reviewer | [human sets] | 2026-08-31 | 2026-09-01 | Cross-ref design-review.md ISS-001; contradicts CE's own hedge that EN "might not need connecting". **Cycle 2 (2026-09-01) independent re-verification: CONFIRMED FIXED.** Hardware Reviewer re-derived directly from TI's TLV755P/SBVS320D datasheet (not merely re-citing the design doc or evidence-log) that EN (pin 3, DBV/SOT-23-5, the ordered package) is mandatory with no internal pull-up/pull-down bias — a floating EN leaves enable state genuinely undefined. Rev 2 §3.4/§12/§13 now show a firm, unconditional `EN_VIN` net (U3 EN→IN) with zero remaining hedge language anywhere in the document (confirmed by full-document text search, not spot-check). Independently confirmed tying EN directly to VIN needs no added decoupling cap, so checklist item 5 is unaffected by this net addition. See `validation/design-review.md` Cycle 2 entry. |
| ISS-002 | HIGH | ACCEPTED-RISK | LDO Vin ROC top-end margin against real-world USB-C vSafe5V ceiling is effectively zero | U3 (TLV75533PDBVR LDO); J1 (USB-C connector) | DS-PWR-002/003 | Operating above ROC (5.5V) but below AMR (6.0V) risks reduced functionality/reliability/lifetime per TI's own ROC-exceedance footnote; real USB-C vSafe5V ceiling (4.75-5.5V) is wider than REQ-101's stated 4.75-5.25V | Explicitly scope REQ-101 to intended sources, or add real circuit-level margin given bench-use (REQ-201) source diversity | hardware-reviewer | [human sets] | 2026-08-31 | 2026-09-01 | Cross-ref design-review.md ISS-002; CE's arithmetic against REQ-101 is correct, but REQ-101 itself may not capture true USB-C input range. **Cycle 2 (2026-09-01) independent assessment: technical analysis SOUND** — both governing numbers (vSafe5V=4.75–5.5V; LDO AMR=6.0V/ROC=1.45–5.5V) independently re-derived and confirmed to match Rev 2 §3.5's arithmetic exactly. The AMR-vs-ROC distinction is correctly characterized: this is a possible-reduced-functionality-at-a-worst-case-edge concern, **not a damage/destruction risk** (0.5V/9% of real headroom remains under the 6.0V AMR even at the worst-case 5.5V input). **ACCEPTED-RISK — human Chief Engineer sign-off recorded 2026-09-01** (via `ask_user`, session record): "Accept the risk as-is — no damage risk, bench-use context", selected over the alternative of routing to Component Engineer for an LDO with a wider Vin ROC ceiling. Rationale: this is a first-cycle, bench/prototype design (REQ-201 desk/lab use, REQ-502 paper-design exercise this cycle, no mass production), the failure mode is bounded (reduced functionality/reliability at a rare worst-case input, not device destruction), and re-selecting the approved LDO would reopen an already-approved Component Selection decision for a non-damage-risk edge case. If this design is ever mass-produced or its input-source population becomes less controlled, this disposition should be revisited. See `validation/change-log.md` ECO-003. |
| ISS-003 | MEDIUM | RESOLVED | LDO thermal margin claim disputed as using wrong package's θJA — independently reverified, original figure was correct | U3 (TLV75533PDBVR LDO) | DS-PWR-006/008 | None. Hardware Lead independently re-verified via TI's own datasheet (SLVSDV4, Thermal Information table) that θJA=60.3°C/W IS the correct value for the DBV/SOT-23-5 package this part orders as ("PDBV" suffix = DBV package code, confirmed via TI's own product page). The disputed 231.1°C/W figure is real but belongs to a *different* package in the same family (DQN/X2SON-4, a 1mm×1mm micro-package) — the Hardware Reviewer's finding conflated the two rows of the same datasheet table. Original design's thermal analysis (~71°C TJ at 40°C ambient/300mA, ~79°C headroom) stands correct, no design change needed. | N/A — no fix needed; reviewer finding itself was in error | hardware-reviewer | [human sets] | 2026-08-31 | 2026-08-31 | RESOLVED by Hardware Lead mediation (`docs/workflow.md` §3) after independent re-verification against TI's primary datasheet; see chat record for full research trail. This is a rare case of the *reviewer's* claim being wrong, not the original design — recorded transparently rather than silently correcting the backlog |
| ISS-004 | MEDIUM | ACCEPTED-RISK | REQ-402 reverse-polarity protection satisfied only via USB-C mechanical keying, no discrete component | J1 (USB-C connector); U3 (LDO) IN pin | DS-CONN-001 | Miswired/damaged/off-brand cable presenting reverse polarity or swapped VBUS/GND has no discrete component to block/clamp it; keying prevents reversal but not miswiring | Add low-cost reverse-polarity mitigation (series Schottky diode or ideal-diode PFET) sized for ≈16.2mA worst-case draw | hardware-reviewer | N/A (MEDIUM; ACCEPTED-RISK sign-off not strictly required by `docs/architecture.md` §8 condition 2, recorded anyway for completeness) | 2026-08-31 | 2026-09-03 | Cross-ref design-review.md ISS-004; elevated risk given bench-use (REQ-201) cable diversity. See also ISS-012 (rubber-duck): the underlying requirement wording itself may be ambiguous about which threat model it intends. **ACCEPTED-RISK — human Chief Engineer sign-off recorded 2026-09-03** (via `ask_user`, Design Complete Gate review): "accept REQ-402's mechanical-keying-only protection as-is." Mechanical keying does prevent the common/normal-use plug-reversal case; the residual risk is specifically a miswired/off-spec/damaged cable, judged acceptable for a bench/desk-use prototype (REQ-201, REQ-502). Recommended fix (series Schottky/ideal-diode) remains logged above for a future revision if this design's use context changes (e.g. less-controlled cable population, mass production). See `validation/change-log.md` ECO-005 |
| ISS-005 | MEDIUM | OPEN | No onboard overcurrent protection upstream of the LDO | J1 (USB-C connector) to U3 (LDO) IN pin span | DS-PWR-002/003 | Fault/short upstream of LDO's own current limit is bounded only by source output impedance/trace capacity if source lacks host-side OCP (e.g. bench PSU, non-compliant charger) | Add small resettable PTC fuse, or explicitly document accepted-risk restriction to USB-IF-compliant sources with Chief Engineer sign-off | hardware-reviewer | [human sets] | 2026-08-31 | | Cross-ref design-review.md ISS-005; downstream span already implicitly protected by LDO's own ICL (560/720/865mA) |
| ISS-006 | MEDIUM | RESOLVED | DS-MCU-046 (BOOT0/PB8 pin-bonding) citation is factually incorrect — PB8 does physically exist | U1 (STM32G031K8T6 MCU) | DS-MCU-044/045/046/049 | None for current design (correct mechanism confirmed by independent re-verification: BOOT0 is actually muxed onto **PA14** — not PB8 — on the STM32G0x0 sub-family per the correct reference manual RM0454; PA14 is already committed to SWCLK in this design, so no physical BOOT0 circuit is possible either way and the design's decision is correct, just for a different reason than originally stated); documentation/evidence-trail defect only; nBOOT_SEL factory-default only corroborated via sibling-part community source, still MODERATE confidence | Correct DS-MCU-046 evidence-log entry (PB8 DOES exist at pin 32 and IS an alternate I2C1_SCL pin per Q5 below — it is simply not the BOOT0 pin); correct DS-MCU-045 and design-doc §4.2 to state BOOT0 is muxed on **PA14** (confirmed via RM0454, ST Community thread "Clarification Needed on PA14-BOOT0 SWCLK BOOT0 pin functionality", and AN5096/AN2606), not PB8; verify FLASH_OPTR nBOOT_SEL via SWD at first-article bring-up | hardware-reviewer | [human sets] | 2026-08-31 | 2026-09-01 | Cross-ref design-review.md ISS-006; NOT a brick risk — SWD recovery independently confirmed available regardless of nBOOT_SEL state, and independently reconfirmed by Hardware Lead research this cycle (see ISS-011 research trail). **Cycle 2 (2026-09-01) independent re-verification: CONFIRMED FIXED.** Hardware Reviewer independently re-derived (2 separate web searches, one citing the STM32G031x4/x6/x8 errata sheet es0487) that BOOT0 is genuinely muxed onto PA14, and confirmed PB8 physically exists with real alternate function I2C1_SCL (AF6), unrelated to BOOT0 — Rev 2 §4.1/§4.2/§11/§13 correctly reflect this, and the underlying evidence-log entries (DS-MCU-050/051) were already corrected during Cycle 1 mediation, prior to this rework. One residual, pre-existing, non-blocking gap remains and was independently reproduced during this cycle's research: PB8's *exact* pin number is still genuinely ambiguous even under fresh independent search (conflicting secondary sources) — matches Rev 2's own honest disclosure at §16 item 3; immaterial to this design since nothing is wired to PB8 regardless of its exact pin number. See `validation/design-review.md` Cycle 2 entry. |
| ISS-011 | HIGH | RESOLVED | MCU pins PB10/PB11 are wired and documented throughout as "I2C1", but the STM32G031K8T6's real alternate-function table maps PB10/PB11 to **I2C2**, not I2C1 | U1 (STM32G031K8T6 MCU) pins 13/14 (PB10/PB11); U2 (BMI270 IMU) I2C connection; net names `I2C1_SCL`/`I2C1_SDA` throughout the design document | DS-MCU-052 (Hardware Lead independent research, confirms PB10/PB11=I2C2 not I2C1; STM32G031 genuinely has 2 separate I2C peripherals) | If firmware initializes the I2C1 peripheral register block while these physical pins are only wired for the I2C2 alternate function, the bus will not respond at all — a full, deterministic failure of the IMU interface (REQ-001/REQ-104) under normal operation as documented. The underlying physical wiring (MCU pins to IMU SCL/SDA, with pull-ups) is electrically correct and needs NO hardware change — this is a peripheral-instance labeling defect, not a wiring defect, caught by the parallel rubber-duck premise-review pass (a check the Hardware Reviewer's own checklist pass did not independently catch) | Relabel every reference to this bus throughout `hardware/schematic/bench-imu-01-design.md` (net names, pin table, checklist item 13, §5) from "I2C1" to "I2C2", and note firmware must configure the I2C2 peripheral, not I2C1 — zero PCB/wiring change required. Alternative (not required): reroute to true I2C1 pins (PB6/PB7 or PB8/PB9, both exist on this LQFP-32 package per DS-MCU-053) if a future reason to keep the I2C1 label specifically ever arises | rubber-duck | [human sets] | 2026-08-31 | 2026-09-01 | Verified independently by Hardware Lead via real-time web research this cycle (STM32G031 datasheet DS12992 AF/pinout tables) after the Hardware Reviewer and rubber-duck passes disagreed on this exact point; classified HIGH not CRITICAL because the fix requires zero hardware rework (pure documentation/firmware-target correction) and the failure mode, while total, is neither destructive nor subtle (would surface immediately at firmware bring-up). **Cycle 2 (2026-09-01) independent re-verification: CONFIRMED FIXED.** Hardware Reviewer independently re-derived (2 separate web searches, not merely re-citing DS-MCU-052) that PB10/PB11 map to I2C2 (AF1) per ST's own DS12992 datasheet Table 13/16 and RM0454 reference manual, consistently, with no contradicting source found. Full-document text search (all 28 "I2C1" + 21 "I2C2" occurrences across §2.3/§5.2/§5.3/§6/§11/§12/§13/§14/§16) confirms every reference in Rev 2 is now consistently and correctly relabeled — no stray leftover "I2C1" mislabel found anywhere, and "I2C1 is now free" is stated correctly (true I2C1 pins PB6/PB7 or PB8/PB9 per DS-MCU-053, independently confirmed unused elsewhere in this design). Independently confirmed the relabeling changes zero electrical characteristics (same physical pins PB10/PB11, same R3/R4=4.7kΩ pull-ups, same bus-capacitance/timing analysis — purely a peripheral-instance/firmware-target correction as claimed, no new interface-timing finding). See `validation/design-review.md` Cycle 2 entry. |
| ISS-012 | MEDIUM | OPEN | REQ-402's "reverse-polarity protection" wording does not specify which threat model it intends (reversed connector insertion vs. a miswired/damaged upstream cable) | `requirements/requirements.md` REQ-402 | N/A — requirements-wording ambiguity, not a datasheet fact | The same requirement can be read as fully satisfied (by USB-C's mechanical keying alone, ISS-004's circuit-level fix) or as unsatisfied (no protection against a miswired/faulty source), depending on which threat model is intended — an ambiguous Must requirement is not properly testable | Sharpen REQ-402's wording in a future Requirements Engineering pass to state explicitly which threat model it covers (recommend: scope it to "compliant-cable, correct-insertion reverse-polarity" only, since that is what the mechanical-keying argument actually addresses, and treat miswired/damaged-cable protection as a separate, explicitly-optional hardening item) | rubber-duck | [human sets] | 2026-08-31 | | Raises the same underlying concern as ISS-004 from a requirements-clarity angle rather than a circuit-design angle; both should be dispositioned together |
| ISS-013 | LOW | OPEN | The "possible future attitude-control roadmap" mentioned in `requirements/requirements.md`'s own project objective has no defined acceptance boundary against today's design choices (e.g. I2C polling vs. interrupt-driven IMU acquisition) | `requirements/requirements.md` §1 objective; design document §5.3 (IMU INT1/INT2 left NC) | N/A — scope/premise question, not a datasheet fact | Not a current defect: REQ-001 (≥100Hz) is easily met by polling, and REQ-006 already explicitly prohibits over-scoping for speculative future use. Risk is purely schedule-related: if a future revision needs interrupt-driven acquisition or tighter latency, discovering that only after enclosure/mechanical work is complete would cost a PCB revision rather than a firmware-only change | No action required for this design cycle. If a future cycle extends toward attitude-control work, revisit whether INT1/INT2 (currently NC but on a part with spare, unallocated MCU GPIOs per design-doc §11) should be wired proactively | rubber-duck | [human sets] | 2026-08-31 | | Explicitly NOT a current blocker; recorded for future-cycle awareness per the rubber-duck's own scope-assessment conclusion that MCU+IMU+Power is an appropriately-sized benchmark scope |
| ISS-007 | LOW | OPEN | DS-IMU-004/006 (BMI270 VDD/VDDIO Absolute Maximum Rating) citation understates actual ceiling (3.6V cited vs 4V actual) | U2 (BMI270 IMU) | DS-IMU-004/006 | None — benign-direction citation error; corrected AMR gives more margin (0.7V) than previously believed (0.3V), not less | Correct evidence-log entries to VDD/VDDIO AMR −0.3 to 4V per primary Bosch datasheet Table 5; re-source to primary PDF | hardware-reviewer | [human sets] | 2026-08-31 | | Cross-ref design-review.md ISS-007 |
| ISS-008 | LOW | OPEN | I2C pull-up sizing (4.7kΩ) compliant only up to ≈75pF bus capacitance; real Cb not yet known pre-layout | R3, R4 (I2C pull-ups); SCL/SDA bus | DS-IFACE-001 | Real Cb above ≈75pF would push Fast-mode rise time beyond 300ns budget, degrading I2C signal integrity between U1 and U2 at 400kHz | Confirm actual bus capacitance once PCB layout exists; apply documented fallback (3.3kΩ/2.2kΩ) if measured Cb exceeds ≈75pF | hardware-reviewer | [human sets] | 2026-08-31 | | Cross-ref design-review.md ISS-008; math/formula independently verified correct, healthy nominal margin (34% headroom at 50pF) |
| ISS-009 | LOW | OPEN | LDO output capacitor (0.47µF) below TI's own PSRR/noise characterization condition (1µF) | C2 (LDO output capacitor); U2 (BMI270 IMU) | DS-PWR-002/003 | None currently demonstrated; PSRR/noise at 0.47µF not separately characterized and may be modestly worse than published 1µF-characterized curves at frequencies relevant to gyroscope noise floor | Consider increasing C2 to 1µF (matches TI's characterization condition, negligible cost/area impact) or bench-verify IMU noise at 0.47µF | hardware-reviewer | [human sets] | 2026-08-31 | | Cross-ref design-review.md ISS-009; precautionary margin note, not a datasheet violation |
| ISS-010 | LOW | RESOLVED | evidence-log.md DS-MCU-012 entry still says UNKNOWN for MCU VDD AMR lower bound despite being resolvable | datasheets/evidence-log.md DS-MCU-012 entry | DS-MCU-012/013 | None — pure documentation-currency issue; 3.3V already well within confirmed ROC regardless | Update DS-MCU-012 entry to VDD AMR Min −0.3V / Max 4.0V per ST DS12992 Rev4 Table 18; mark design-doc §16 UNKNOWN #2 resolved | hardware-reviewer | [human sets] | 2026-08-31 | 2026-09-01 | Cross-ref design-review.md ISS-010. **Cycle 2 (2026-09-01) independent re-verification: underlying FACT confirmed correct; ORIGINAL RECOMMENDED FIX found incomplete (design-doc updated, evidence-log.md was not) — kept OPEN by the Cycle 2 reviewer pending that specific gap.** **Hardware Lead (2026-09-01): closed the gap directly** — updated `datasheets/evidence-log.md` DS-MCU-012's Value column from "UNKNOWN" to "−0.3 V to +4.0 V" (Table 18 reference added), since this file is outside the Circuit Engineer's edit scope and outside the Hardware Reviewer's own `design-review.md`/`open-issues.md` output scope for this cycle, per the reviewer's own note that it is "owned by Hardware Reviewer/Hardware Lead." Now marked RESOLVED — the evidence trail is fully closed, no remaining action. |
| MISS-001 | HIGH | RESOLVED | Header/button bay (J2/J3/SW1) clearance margin is measured from connector centerlines, not the design's own assumed footprints — real margins as low as 0.0mm | Lid header/button bay cutout (bay_x_min/bay_x_max/bay_y_min in bench-imu-01-enclosure.scad); J2 (UART header), J3 (SWD header), SW1 (reset button) | hardware/mechanical-interface.md Connectors/Switches/LEDs table (J2/J3/SW1 rows, point positions only); bench-imu-01-enclosure.scad lines 233-238 (bay_x_min/bay_x_max/bay_y_min) cross-referenced against lines 445-451 (reference_pcba()'s own J2/J3/SW1 footprints) | If real J2/J3 headers or SW1's real package occupy anywhere close to the footprint already assumed elsewhere in the same file, the lid bay does not actually provide the "6mm" clearance its own comments/self-check claim — recomputed real margins are 0.0mm (J2/J3 far edge to bay_y_min), 1.0mm (J2 near edge to bay_x_min), and 3.5mm (SW1 near edge to bay_x_max), not ~6mm — risking a shrouded header body or switch bezel contacting/overlapping the printed cutout edge, requiring filing/rework to actually mate a cable or press the button | Recompute bay_x_min/bay_x_max/bay_y_min from each connector's own assumed (or, once available, real) footprint edge, not its centerline/point position, and re-add genuine spare margin beyond that edge — matching the edge-to-edge convention already correctly used for the tab-vs-bay clearance (dimensional-spec Open Item #8) | mechanical-reviewer | [human sets] | 2026-09-02 | 2026-09-03 | Cross-ref design-review.md Mechanical Reviewer Cycle 1, MISS-001. Contradicts dimensional-spec §11 self-check item 2 ("PASS... all comfortably inside with margin"). Fully derivable from values already committed to the .scad file (does not require confirming real J2/J3 hardware first, though compounded by that unconfirmed-hardware risk per the spec's own Open Item #1). **Cycle 2 (2026-09-03) independent re-verification: RESOLVED, CONFIRMED FIXED.** Independently re-derived J2/J3/SW1 footprint edges directly from `reference_pcba()`'s own `cube()`/`cylinder()` primitives (not re-read from the spec's own arithmetic) and recomputed all three connector-to-bay margins from the new `bay_edge_margin`(1.5mm)-derived `bay_x_min`(9.5)/`bay_x_max`(48.0)/`bay_y_min`(32.5): J2 near edge 1.5mm, J2/J3 far edge 1.5mm, SW1 near edge 1.5mm — genuinely uniform and positive at every connector, not just one representative point. Also independently discovered and logged a NEW side-effect finding this cycle — see MISS-007 — where this same fix narrows the D1-to-bay isthmus to 1.0mm, below the design's own 2.0mm `min_wall_t`; the Mechanical Lead's spec discloses the 1.0mm number (Open Item 8a) but only checked it as a clearance margin, not against the wall-thickness checklist item. |
| MISS-002 | HIGH | RESOLVED | Base corner-tab "45-deg self-supporting chamfer/gusset" is geometrically non-functional — hull() of two 0.01mm-thin cubes produces a degenerate sliver, not a real wedge | All 4 base corner tabs (base_tab()/base_tabs() in bench-imu-01-enclosure.scad) — the entire base-side half of the lid's fastening mechanism | bench-imu-01-enclosure.scad lines 267-277 (chamfer rationale comment) and lines 310-335 (base_tab() implementation, hull() construct at lines 329-334); bench-imu-01-dimensional-spec.md §9.2 ("without printed support material") and §11 self-check item 9 ("PASS") | The base tab's overhanging ~6mm x 8mm x 5mm volume (a genuine 90-deg horizontal overhang roughly 3/4 of the way up the base's 18.6mm print height, not bed-adjacent) has no real supporting structure in the model as coded — both hull() inputs are 0.01mm thin in Y AND Z, yielding a diagonal sliver far below any FDM printer's minimum feature size, not a load-bearing wedge; sliced without a human manually adding support material (which the design claims is unnecessary), the tab is likely to droop or print with degraded surface quality exactly where the M2.5 pilot hole for the lid-fastening screw is threaded, risking stripped threads or a lid corner that cannot be reliably fastened, at all 4 base tabs | Replace the degenerate hull() construct with a genuine solid wedge (e.g. linear_extrude of a right-triangle polygon() with real rise/run matching tab_chamfer_run, swept along tab_w), or explicitly require sliced-in support material for the base tabs until the gusset geometry is corrected | mechanical-reviewer | [human sets] | 2026-09-02 | 2026-09-03 | Cross-ref design-review.md Mechanical Reviewer Cycle 1, MISS-002 — the most significant finding this cycle. Directly contradicts dimensional-spec §9.2's explicit claim and self-check item 9's PASS. The .scad file's own comment at lines 321-325 already partially hedges this ("not a load-bearing dimension... a human should feel free to refine the exact profile") — a caveat that did not propagate into the spec's more confident claim. **Cycle 2 (2026-09-03) independent re-verification: RESOLVED, CONFIRMED FIXED — most rigorously verified finding this cycle.** Independently re-derived the `linear_extrude(polygon())`+`rotate([0,90,0])` construct by hand (confirmed the code comment's own Ry(90) claim via direct matrix substitution) AND empirically, via actual OpenSCAD 2021.01 renders of the real repo file (confirmed genuinely installed this cycle): rendered both front-tab (dy=-1) and rear-tab (dy=+1) instances standalone, confirmed `Simple: yes` fully-manifold geometry for each, measured the wedge's own volume via a custom STL-parsing divergence-theorem calculation at exactly 144.0000mm^3 (matching the hand-derived 0.5x6x6x8=144mm^3 to 4 decimal places) and the combined tab+wedge volume at 398.3899mm^3 (matching the hand-calc 398.349mm^3 to within cylinder-facet-discretization tolerance), and confirmed via actual boolean `intersection()` tests (not bounding-box comparison) that the wedge has ZERO overlap with either the interior cavity or the fastener pilot hole, for both tab orientations (all 4 intersection tests returned "Current top level object is empty"). This is a genuine, correctly-positioned, load-bearing solid wedge, not a degenerate artifact. |
| MISS-003 | MEDIUM | RESOLVED | Lid tab's clearance-hole annular wall (1.6mm) is below the design's own stated 2.0mm minimum wall thickness | All 4 lid corner tabs (lid_tab()/lid_tabs() in bench-imu-01-enclosure.scad) | bench-imu-01-enclosure.scad line 112 (min_wall_t=2.0), line 251 (tab_w=8.0), line 252 (tab_project=6.0), line 261 (tab_clear_dia=2.8); bench-imu-01-dimensional-spec.md §11 self-check item 5 ("adequate surrounding material at every boss") | (tab_project − tab_clear_dia)/2 = (6.0−2.8)/2 = 1.6mm wall around the lid tab's clearance hole, below the stated 2.0mm minimum (the equivalent base-tab calc, using the smaller 2.0mm pilot dia in the same footprint, gives exactly 2.0mm); the lid tab experiences compressive bearing stress from the screw head rather than thread-forming stress, so the risk is bounded, but an elevated crack/deformation risk exists at all 4 lid tabs under repeated assembly/disassembly or over-torquing | Widen tab_w/tab_project for the lid tab specifically (a lid-side-only footprint override) to restore ≥2.0mm margin around the larger clearance hole, or reduce tab_clear_dia to the minimum practical M2.5 clearance size that still fits the existing footprint with ≥2.0mm margin | mechanical-reviewer | [human sets] | 2026-09-02 | 2026-09-03 | Cross-ref design-review.md Mechanical Reviewer Cycle 1, MISS-003. Directly computable from named variables already in the file; not dependent on unconfirmed real-hardware facts. Contradicts self-check item 5's claim. **Cycle 2 (2026-09-03) independent re-verification: RESOLVED, CONFIRMED FIXED.** Independently recomputed the lid tab's annular wall from the new lid-only `lid_tab_project`(6.8mm = 2x min_wall_t + tab_clear_dia) override: Y-margin (the constrained axis) = (6.8-2.8)/2 = 2.0mm exactly, X-margin = (8.0-2.8)/2 = 2.6mm — both now meet or exceed the 2.0mm minimum. Confirmed the fastener axis (`hole_yc`) still derives from the unchanged, shared `tab_project`, so the lid clearance hole remains coaxial with the base tab's pilot hole beneath it — widening the lid footprint did not introduce a new misalignment. Confirmed the resulting 0.4mm/side footprint growth lands entirely within regions already solid (interior cavity, header bay, lid roof) or on the lid's flat, bed-adjacent first-print-layer (lid prints roof-down), so it introduces no new interference or overhang risk. |
| MISS-004 | MEDIUM | RESOLVED | Lid/base-tab fastener pair has zero engagement-depth spare margin (4.0mm into a 4.0mm pilot), unlike the PCB standoff's deliberate 0.6mm spare | All 4 lid-to-base corner-tab fastener joints (base_tab()/lid_tab() in bench-imu-01-enclosure.scad) | bench-imu-01-enclosure.scad lines 151-158 (screw_len comment, explicitly stating both engagement figures) and line 256 (tab_pilot_depth=4.0); bench-imu-01-dimensional-spec.md §8 fastener-placement table (PCB-standoff row: "4.4mm engaged"; lid/base-tab row: "full engagement") | screw_len(6.0) − tab_lid_t(2.0) = 4.0mm available engagement, exactly equal to the 4.0mm tab_pilot_depth (0.0mm spare), vs. the PCB standoff's 0.6mm spare using the same screw; any positive real-world tolerance (a longer-than-nominal screw or a shallower-than-nominal printed pilot hole, a common ±0.2-0.3mm variance for M2.5 self-tapping fasteners) causes the screw to bottom out before the lid tab is fully clamped, leaving that corner slightly proud/loose rather than fully seated, potentially recurring at any/all of the 4 lid corners | Deepen tab_pilot_depth slightly (trading against the tab's own "1.0mm solid floor remaining" margin) or shorten the lid-side clearance-hole thickness assumption, to restore a small positive spare-engagement margin analogous to the PCB standoff's 0.6mm; re-characterize the fastener-placement table's "full engagement" wording to explicitly flag the margin, the way the standoff row already does | mechanical-reviewer | [human sets] | 2026-09-02 | 2026-09-03 | Cross-ref design-review.md Mechanical Reviewer Cycle 1, MISS-004. Raw numbers are not hidden — the .scad file's own comment at lines 156-158 already discloses them — but are framed positively ("full engagement") in the spec's fastener table rather than flagged as zero-tolerance, unlike the PCB standoff row one line above it in the same table. **Cycle 2 (2026-09-03) independent re-verification: RESOLVED, CONFIRMED FIXED.** Independently re-derived the full Z-stack from raw values: base tab now spans global Z=[13.0,18.6] (`z0=base_total_h-tab_base_t=18.6-5.6=13.0`), pilot hole bottom at Z=14.0 (`=18.6-tab_pilot_depth(4.6)`), and the screw tip — after passing through the lid's `tab_lid_t`(2.0mm) clearance thickness — reaches Z=14.6: a genuine +0.6mm positive engagement spare (matching the PCB standoff's own precedent spare exactly), not the prior 0.0mm. Independently confirmed `tab_base_t` and `tab_pilot_depth` were both increased by the same +0.6mm in lockstep, so the tab's solid-floor-below-the-pilot-hole margin (`tab_base_t - tab_pilot_depth`) is preserved at exactly 1.0mm, unchanged from before the fix — the fix restored one margin without silently trading away the other. |
| MISS-005 | LOW | OPEN | Design's claim that fit_clearance governs "all fastener clearance-hole sizing" does not match the implemented code | fit_clearance documentation/self-check claim; mount_holes[i][2] and tab_clear_dia (bench-imu-01-enclosure.scad) | bench-imu-01-enclosure.scad lines 101-104 (fit_clearance header comment), lines 186-187 (its only two actual uses), line 63 (mount_holes clearance-dia comment, independent ISO 273 convention), line 261 (tab_clear_dia comment); bench-imu-01-dimensional-spec.md §11 self-check item 8 ("applied at... fastener clearance holes") | None physical — fit_clearance (0.2mm/side) is used only at the lid-skirt/base-wall joint (lines 186-187); the fastener clearance-hole diameters (2.8mm) are independent hardcoded literals that do not mathematically derive from fit_clearance (a strict derivation would give 2.5+2×0.2=2.9mm, not 2.8mm); the risk is purely to future maintainability — a future edit to fit_clearance would silently not propagate to the fastener holes despite the code/self-check claiming it would | Either express the fastener clearance-hole diameters as an actual function of fit_clearance, or correct the header comment and self-check item 8's wording to state fastener clearance holes follow a separate, standard screw-clearance convention, mirroring how board_xy_keepout and z_margin are already explicitly distinguished from fit_clearance elsewhere in the same file | mechanical-reviewer | [human sets] | 2026-09-02 | | Cross-ref design-review.md Mechanical Reviewer Cycle 1, MISS-005. The actual 2.8mm dimension chosen is independently reasonable (ISO 273 M2.5 clearance convention per the .scad file's own line-63 comment) — this is a documentation/self-check-accuracy defect only, no physical consequence. |
| MISS-006 | LOW | OPEN | Two array fields (mount_holes[i][2], tab_positions[i][1]) are defined but never consumed by the geometry code (dead data) | mount_holes array, tab_positions array (bench-imu-01-enclosure.scad) | bench-imu-01-enclosure.scad lines 62-69 (mount_holes) vs. lines 294-308 (standoff()/base_standoffs(), only consuming m[0]/m[1]); lines 279-288 (tab_positions) vs. lines 310-335 (base_tab(), only consuming pos[0]/pos[2]) | None currently manifest — a latent documentation/maintainability risk only: a future edit to either field would silently have no geometric effect, and no comment currently flags either field as unused/vestigial | Either wire mount_holes[i][2] into a future PCB-side clearance-hole model and remove/comment tab_positions[i][1] as intentionally unused (since the tab's Y-position is fully determined by the direction flag alone), or add a one-line comment at each declaration noting the field is currently unused | mechanical-reviewer | [human sets] | 2026-09-02 | | Cross-ref design-review.md Mechanical Reviewer Cycle 1, MISS-006. Style/traceability nitpick, no functional or manufacturability consequence. |
| MISS-007 | MEDIUM | OPEN | D1 (LED) viewing-hole-to-header-bay isthmus narrows to 1.0mm, below the design's own stated 2.0mm minimum wall thickness — a side effect of the MISS-001 fix that was disclosed as a margin item but never cross-checked against the wall-thickness rule | D1 LED viewing hole and header/button bay cutout, lid roof (`lid_shell()` in bench-imu-01-enclosure.scad) | bench-imu-01-enclosure.scad line 112 (`min_wall_t`=2.0), lines 269-283 (`bay_edge_margin`/`bay_x_min`/`bay_x_max`/`bay_y_min` derivation, including the Mechanical Lead's own inline side-effect-disclosure comment), D1's `d1_x`/`d1_y` definition and `d1_hole_dia`=3.0 (ESTIMATE per hardware/mechanical-interface.md), lines 545-556 (bay cube cutout and D1 cylinder cutout in `lid_shell()`); bench-imu-01-dimensional-spec.md §12 Open Item 8a | D1's hole center (global (13.5, 33.5), r=1.5mm) and the bay cutout's near edge (global Y=36.0, spanning X=[13.0, 51.5], which contains D1's X=13.5) leave only a 1.0mm-wide, full-roof-thickness (2.0mm tall) isthmus of solid material between the two full-depth cutouts — independently confirmed both by hand distance geometry AND by an OpenSCAD binary-search growing-circle probe this cycle (2D probe centered on D1, empty intersection with the bay footprint at probe radius 2.50mm, non-empty at 2.51mm, pinning the true gap at 1.00mm), below this design's own stated `min_wall_t`=2.0mm; a thin, isolated fin like this is a plausible FDM under-extrusion/warping/cracking point, risking the D1 hole and bay notch merging into one opening or the fin snapping off during printing, support removal, or handling | Either (a) reduce `d1_hole_dia` from its current 3.0mm ESTIMATE toward a smaller confirmed-adequate value to restore >=2.0mm (needs Electronics/LED-datasheet input, since the diameter itself is unconfirmed), or (b) add a small local reinforcement (fillet/rib) bridging the isthmus without moving either cutout, or (c) explicitly re-open and disposition this as an accepted, monitored exception to `min_wall_t` (e.g. a first-article-print watch item, analogous to the existing J1-bridge-span Open Item 9) rather than leaving it addressed only under the clearance-margin framing of Open Item 8a | mechanical-reviewer | [human sets] | 2026-09-03 | | Found independently this cycle while re-verifying MISS-001's fix per the review task's own prompt (does the wider bay 'come too close to anything else'). The Mechanical Lead's own `.scad` file already discloses this exact 1.0mm number in an inline comment at the `bay_y_min` derivation, and dimensional-spec.md §12 Open Item 8a explicitly flags it as "the single item this Mechanical Lead would most want the Reviewer to look at again" — full credit given for that proactive disclosure; this finding's contribution is reclassifying it from a clearance-margin judgment call (checklist item 4, "is it positive? yes, barely") into a formal wall-thickness/manufacturability finding (checklist items 6/9, "does it meet the design's own stated print-safe minimum? no") that had not yet been checked against that specific rule. Not a functional-fit blocker (both the D1 hole and the bay opening remain independently usable even in the worst case); classified MEDIUM by direct analogy to MISS-003 (same class of defect: a below-minimum wall/isthmus around a hole feature, minimum at one point, widening away from it). Does not block this cycle's PASS verdict per this file's own MISS-003/MISS-004 precedent (open MEDIUM findings are non-gating; only open CRITICAL/HIGH findings gate the verdict), but should be dispositioned by the Mechanical Lead before Design Complete. |
| ISS-014 | HIGH | RESOLVED | New reverse-polarity diode D2 (STPS3L60) pushes a 2S LiPo source below the DRV10983 (U5) UVLO threshold at typical and worst-case corners — 3S-only is the real practical operating constraint, not propagated to the BOM or requirements | J4 (Same Sky PJ-102AH) -> D2 (STPS3L60) -> D3 (SMBJ16A) -> U5 (DRV10983) VCC; M1 (T-Motor MN2206-13 KV2000, rated 2S-3S in isolation) | DS-PROT-005 (STPS3L60 forward-voltage electrical characteristics); DS-MTR-057 (DRV10983 UVLO rising/falling thresholds) | 2S nominal (7.4V) minus D2's typical forward drop (0.53V) = 6.87V, below VUVLO_R's own typical threshold (7.4V) -- a typical, not merely worst-case, 2S pack is expected to fail to power U5 up at all; 2S full-charge (8.4V) minus D2's max forward drop (0.62V) = 7.78V, below VUVLO_R's own max threshold (8V) -- even a freshly-charged 2S pack can fail to clear UVLO at the pessimistic corner; a 2S pack that does start can also drop out mid-discharge (7.0V - 0.53V = 6.47V, below VUVLO_F min 6.7V). 3S remains robust throughout its full practical discharge range at every corner (9.0V near-cutoff - 0.62V max drop = 8.38V, clears both VUVLO_R max and VUVLO_F max) | (1) Formally propagate a documented 3S-only operational constraint into `bom/component-selection.md` and `requirements/requirements.md` (zero hardware cost); and/or (2) if 2S support is genuinely required, have the Component Engineer evaluate a lower-voltage-drop reverse-polarity topology (e.g. an ideal-diode/ORing-FET circuit), since simply swapping to a different Schottky trades VF against reverse-leakage/voltage rating in the same component class | hardware-reviewer | [human sets] | 2026-09-05 | 2026-09-08 | Independent re-derivation of a finding the design document itself self-flagged (`bench-imu-01-design.md` §7.5.2, §16 item 17) without self-assigning severity -- this review's contribution is the severity assignment (HIGH, per `docs/architecture.md` §7.1: a realistic-corner likely malfunction with a non-destructive, auto-recovering UVLO shutdown and a zero-cost mitigation available, not CRITICAL, which requires failure under the design's own as-designed/intended condition -- here, 3S), independently reproducing and in fact sharpening the design document's own headline number (the typical-corner 2S-nominal failure is a likely, not rare-corner, case). Also surfaces two things neither source document states alone: (a) a real, if narrow, cross-document prose-optimism inconsistency -- the design document's own §7.5.2 states a freshly-charged 2S pack "clears UVLO with any margin" (true only against VUVLO_R's typical value) while `hardware/power-budget.md`'s own Rail Margin Summary, addressing the identical scenario, concludes the opposite ("sits under UVLO_R's max (8V)"); and (b) via independent `git show --stat 3e2c529`, confirmed zero propagation of this constraint into `bom/component-selection.md` (whose only Rev 3 change was the unrelated OCP/Lock Detection fix, ISS unrelated) or `requirements/requirements.md` (grepped in full across every Rev 3 requirement -- no 2S/3S/cell-count constraint exists anywhere). Cross-ref `validation/design-review.md` Cycle 3. **Cycle 4 (2026-09-08) independent re-verification: CONFIRMED RESOLVED.** Independently re-derived the full §7.5.2 corner arithmetic from scratch (unchanged since Cycle 3 — Rev 4 only sharpened the framing) and independently located the actual propagated text in both target files: `bom/component-selection.md` lines 749–766 (Motor Driver IC recommendation, an explicit 3S-only binding note) and `requirements/requirements.md` REQ-108's own Notes column (line 105) — both confirmed present, not merely claimed. Used `git show --stat`/`git log` to independently confirm this propagation is attributable to the Hardware Lead's `9410a9d` mediation commit, not to Circuit Engineer's Rev 4 commit (`7774d4f`) directly, which never touched either target file — a correct division of labor, not a process gap. See `validation/design-review.md` Cycle 4. |
| ISS-015 | HIGH | RESOLVED | DRV10983 (U5) SPEED pin's factory-default EEPROM state is active analog mode (not inert), has no hardware-enforced cross-domain power-up sequencing, and its cited partial mitigant (internal SPEED pulldown) is documented only for the sleep-capable Z-variant, not the plain DRV10983 actually specified -- uncommanded-motion risk on M1 tied to REQ-403 | U5 (DRV10983) SPEED pin 13 / DIR pin 14; M1 (T-Motor MN2206-13 KV2000 reaction wheel); J1 (MCU/USB domain) vs. J4 (independently-sourced motor domain) under the Option A dual-power architecture | DS-MTR-068 (qualified this cycle), DS-MTR-069, DS-MTR-070, DS-MTR-071 (new this cycle) -- DRV10983 Table 8 EEPROM defaults, §8.4.5.2 analog-mode transfer function, RPD_SPEED_SL's own "SLEEP MODE (DRV10983Z)" Electrical Characteristics section header | Register 0x2B (SysOpt9) factory-default value 0x0C decodes to SpdCtrlMd=0 = analog mode, not the PWM mode this design intends (which requires an EEPROM write firmware has not yet performed in any uncommissioned window); §8.4.5.2 confirms analog mode is fully active out of reset (SPEED<100mV=stop, 100mV<=SPEED<~V3P3x0.9=proportional non-zero speed command, SPEED>=~V3P3x0.9=max). No EN/FAULT pin exists on DRV10983 at all -- SPEED is the sole on/off control surface. No supervisory switch gates U5's VCC on the MCU domain's power state (D2/D3 are both passive) -- the uncommissioned window is not bounded to be transient if the motor-rail connector (J4) is powered before or independent of the MCU-rail connector (J1). Any leakage/coupling/stray voltage on floating SPEED exceeding 100mV during this window is interpreted as a real, non-zero commanded speed, commutating M1 on generic (non-T-Motor-matched) EEPROM Rm/Kt defaults | (1) Add a real external SPEED pulldown sized to dominate realistic leakage/coupling -- a deliberate, justified deviation from TI's own Table 11 reference circuit, which implicitly assumes a single always-alive host and does not anticipate this design's dual-independent-power-domain architecture; (2) add a supervisory load switch gating U5's VCC, enabled only once the MCU domain's own rail is confirmed alive, directly enforcing the missing cross-domain sequencing (design document's own §16 item 14); or (3), as a weaker documentation/process-only fallback, mandate a specific operator power-up order and have firmware drive SPEED to a safe state at the earliest possible init step. Given REQ-403's explicit human-review gate, route for human safety sign-off regardless of which technical path is chosen | hardware-reviewer | [human sets] | 2026-09-05 | 2026-09-08 | Independently discovered this cycle -- not one of the two items the design document itself flagged for Hardware Reviewer attention -- while investigating checklist items 6 (floating pin) and 10 (power sequencing) together, building on the design document's own honest self-flagged §16 items 13-14. This review's contribution beyond the design document's own analysis: (a) independently confirmed via Table 8's own EEPROM-default decode that the factory-default state is analog mode, not inert, correcting an inaccurate "functionally inert" claim in the pre-existing `datasheets/evidence-log.md` DS-MTR-068 entry (qualified this cycle -- see `validation/design-review.md` Cycle 3 for the correction detail, plus the new DS-MTR-071 row); (b) independently found that RPD_SPEED_SL -- the design document's own cited partial mitigant -- is documented under a datasheet section header explicitly scoped to the DRV10983Z sleep-capable variant, not confirmed/guaranteed for the plain (Standby-only) DRV10983 actually specified as U5, a variant-specificity nuance beyond the design document's own (already honest) sleep-mode-vs-power-on-state caveat. Severity HIGH not CRITICAL: the failure path is probabilistic/multi-factor-dependent (a specific power-up order plus an unmeasured real stray voltage exceeding 100mV), not deterministic-every-time, and any resulting motion is current-limited/ramped (StAccel/OpenLCurr, Table 6) and running on non-matched commutation parameters, bounding (not eliminating) worst-case outcome -- consistent with this file's own HIGH-not-CRITICAL precedent (e.g. ISS-011, a deterministic 100%-certain failure under stated conditions, still classified HIGH not CRITICAL). Cross-ref `validation/design-review.md` Cycle 3; ties directly to REQ-403 (safety-critical, human-review-gated) and checklist items 6/10/15. **Cycle 4 (2026-09-08) independent re-verification: CONFIRMED RESOLVED.** R10=1kΩ (unchanged since Rev 4) independently confirmed present. New U6 (TI TPS26631PWPR) load switch's R11=10kΩ SHDN pulldown independently re-derived from TI's own guaranteed ≥10µA SHDN-sink instruction (not the design document's restated figure) — reproduces the claimed ≈8×/≈20× margins against V(SHUTF)=0.8V/V(SHUTR)=2V exactly. Independently investigated this cycle's specific new question — PA9's own state before MCU GPIO init completes — via `web_search` (secondary source, disclosed in `validation/design-review.md` Cycle 4): confirmed STM32G0 GPIOs default to high-impedance Analog mode (no internal pull) after any reset (now formally logged as DS-MCU-067, MODERATE confidence), so R11 dominates the SHDN node with full margin for the entire pre-init boot window; PA9 driving SHDN high draws only ≈330µA through R11, confirming the enable direction also works correctly. No new floating-pin risk found on PA9. See `validation/design-review.md` Cycle 4 for the full derivation. |
| ISS-016 | LOW | OPEN | DRV10983 (U5) DIR pin has zero internal bias of any kind and no external pull -- direction selection is indeterminate if U5 begins commutating before MCU GPIO init completes | U5 (DRV10983) DIR pin 14; U1 (STM32G031K8T6) PB1 | DS-MTR-069 (DRV10983 Pin Functions table + "DIGITAL I/O (DIR INPUT AND FG OUTPUT)" Electrical Characteristics section) | Floating CMOS-style digital input settles to an indeterminate logic level under leakage/coupling; if commutation begins before PB1 is initialized, the rotation direction on that specific spin-up is unpredictable | TI's own Table 11 reference circuit does not bias DIR either, so no datasheet-recommended component exists to add; best addressed in firmware (initialize PB1 to a known state as early as possible in boot) or accepted as a low-severity residual given the narrower consequence (direction ambiguity only) relative to ISS-015 (speed/motion ambiguity) | hardware-reviewer | [human sets] | 2026-09-05 | | Independently confirmed via the primary DRV10983 Pin Functions table and Electrical Characteristics table (VDIR_H/VDIR_L logic thresholds only, no pulldown/pullup parameter documented anywhere for DIR, unlike SPEED's own caveated RPD_SPEED_SL) -- matches and independently verifies the design document's own self-flagged §16 item 13. Cross-ref `validation/design-review.md` Cycle 3, ISS-015 (same root architectural cause -- no hardware-enforced cross-domain sequencing -- but a narrower consequence). |
| ISS-017 | LOW | OPEN | J4 (Same Sky PJ-102AH) barrel jack's third terminal function (switch contact vs. sleeve/GND identity) is unconfirmed from the datasheet page fetched to date | J4 (Same Sky PJ-102AH) | DS-CONN-005 (Same Sky PJ-102AH metadata record, "Known gaps" section) | None under current as-designed use (the unidentified terminal is left unpopulated, a safe default); risk is limited to a future footprint/pad assignment being wrong if revisited without re-confirming against the connector's own internal schematic | Obtain and read the connector's own internal schematic diagram (not just its external dimensional drawing) before PCB layout to confirm terminal identity with certainty | hardware-reviewer | [human sets] | 2026-09-05 | | Matches and independently confirms the design document's own self-flagged §16 item 15 -- no contradicting or clarifying information found elsewhere in this design's own citations. Pre-layout confirmation item only; not a current functional or safety concern. Cross-ref `validation/design-review.md` Cycle 3. |
| ISS-018 | LOW | OPEN | `datasheets/evidence-log.md` DS-MTR-059 entry is internally inconsistent: prose states Lock Detection has "5" independently-maskable sub-schemes, but its own enumeration lists 6 (Lock0/1/2/3-Fault3/4/5) and cites a 6-bit field (LockEn[5:0]) | N/A (`datasheets/evidence-log.md` documentation only) | DS-MTR-059 (its own text, self-inconsistent) | None -- a documentation/proofreading defect only; the design document's own §7.5.6 protection table already states the correct count (6) and is unaffected, so no design decision in this revision relies on the evidence-log's own miscounted prose | Correct DS-MTR-059's prose from "5" to "6" independently-maskable sub-schemes to match its own enumeration and cited 6-bit field | hardware-reviewer | [human sets] | 2026-09-05 | | Purely internal to one evidence-log row; zero functional or design-decision impact since the design document itself (the artifact actually used for engineering decisions) already states the correct count. Cross-ref `validation/design-review.md` Cycle 3. |
| ISS-019 | HIGH | RESOLVED | New motor input (J4) has no bounded source envelope or coordinated upstream fault containment -- every component in its path tolerates far more voltage/current than the recommended motor (M1) is actually qualified for | J4 (Same Sky PJ-102AH); D2 (STPS3L60); D3 (Littelfuse SMBJ16A); U5 (DRV10983); M1 (T-Motor MN2206-13 KV2000) | DS-MTR-017 (M1 qualified 2S-3S only); DS-CONN-005 (J4 rated 24V/5.0A); DS-PROT-004 (D3 does not clamp below ~16V, may clamp as high as 26V); DS-MTR-053 (U5 tolerates up to 30V) | `hardware/power-architecture.md`'s own Option A rationale explicitly called for independent enable/inrush/fault containment on the new motor rail, but the actual as-designed path has no coordinated input over-voltage protection or current limiting upstream of U5's own internal OCP -- a physically-compatible but out-of-spec source (e.g. a 4S pack, an unregulated 12-24V adapter, or a bench supply set incorrectly) can run M1 outside its qualified voltage range, or deliver fault current through J4/D2 that U5's own phase-level OCP cannot see until it reaches U5 itself | Define a binding J4 input envelope (explicit maximum voltage, not just the "~12V-class" framing) and add coordinated input protection (OVP clamp sized to the envelope, and/or an input eFuse/PTC/fuse) at the J4/D2 stage rather than relying solely on U5's own internal, downstream protection; record the envelope in `validation/bring-up-procedure.md`'s future motor-rail section | rubber-duck | [human sets] | 2026-09-05 | 2026-09-08 | Independently discovered this cycle, distinct from ISS-014 (which is about the input sagging too LOW for 2S) -- this is the complementary upper-bound/source-fault-envelope gap Hardware Reviewer's own checklist pass did not separately surface. Cross-ref `validation/design-review.md` Cycle 3 rubber-duck section. **Cycle 4 (2026-09-08) independent re-verification: CONFIRMED RESOLVED.** Independently re-extracted TI's own UVLO/OVP divider equations (Equations 9–10) directly from the primary TPS2663x datasheet (SLVSE94G) and recomputed all six corner values from scratch using the design's actual R12=887kΩ/R13=60.4kΩ/R14=88.7kΩ — reproduces UVLO=8.172/8.339/8.506V and OVP=13.737/14.017/14.298V (min/typ/max) exactly, matching the design document's own claimed 8.17–8.51V/13.74–14.30V window to 3+ significant figures at every corner. F1's own role (upstream fault-current containment, not continuous-overvoltage regulation) independently reconfirmed unchanged and distinct from the specific gap this finding is about; U6's OVP function is what closes it. See `validation/design-review.md` Cycle 4. |
| ISS-020 | HIGH | ACCEPTED-RISK | The approved "≥3000 RPM" reaction-wheel target has only ever been treated as a functional floor, never converted into a bounded maximum/safety envelope for commanded speed, acceleration, or mechanical containment | M1 (T-Motor MN2206-13 KV2000) and its eventual flywheel/mount (Mechanical Lead scope); U5 SPEED command path; motor firmware (future) | DS-MTR-018 (M1's own derived no-load speed is ≈20,000-22,200 RPM, 6-7× the 3000 RPM target); DS-MTR-020/DS-MTR-021 (torque/mass basis); REQ-007, REQ-306, REQ-403 | REQ-007 only specifies a commanded range "including at least" the target -- nothing in this design or its requirements bounds the maximum command, ramp rate, or overspeed response. For the 100g/30mm flywheel basis, stored rotational energy at the motor's own derived no-load speed is roughly 45-55x the energy at the 3000 RPM target (energy scales with omega^2) -- a legitimate full-duty command, a firmware bug, or a lost/reduced mechanical load could drive the wheel far beyond the value every other requirement/mechanical-design assumption in this cycle was sized against, with no interlock to catch it; this directly compounds ISS-015's uncommissioned-motion finding (that finding is about *unintended* motion at power-up, this finding is about *unbounded* motion even during normal, intended operation) | Define an explicit maximum operating/fault speed (not just the minimum target), a commanded ramp-rate limit, and a tach-supervised (FG-based) overspeed shutdown as a firmware safety interlock -- this is a bounded safety cutoff, not the prohibited closed-loop attitude-control logic REQ-009 excludes, the same way a supply's overcurrent shutdown is not "control" in the disallowed sense. Feed the chosen maximum into Mechanical Lead's flywheel/mount/containment design as a real input, not an afterthought | rubber-duck | [human sets] | 2026-09-05 | 2026-09-09 | A genuine premise-challenge finding -- neither the requirements pass, Component Selection, nor Circuit Design ever asked "what is the MAXIMUM speed this design must tolerate/survive," only "what is the minimum it must reach." Ties to REQ-403's safety-critical human-review gate. Cross-ref `validation/design-review.md` Cycle 3 rubber-duck section. **Cycle 4 (2026-09-08) independent re-verification: correctly remains OPEN, not hardware-fixable.** Independently confirmed via direct read of new design-doc §7.5.11 that Circuit Engineer honestly disclosed "No circuit-level fix exists for this finding, and none is invented here," and independently confirmed REQ-405 exists in `requirements/requirements.md` (line 142), is well-formed (Priority: Must), and is correctly attributed to this finding — added by the Hardware Lead's own mediation commit (`9410a9d`), not silently invented by Circuit Engineer. My own independent assessment agrees no circuit-level mitigation exists for this firmware/control-policy question. Recommend a human Chief Engineer consider an explicit disposition note (e.g. "OPEN, tracked for Firmware Bring-up phase") for the record, though the underlying finding itself must remain OPEN (not ACCEPTED-RISK) until the firmware policy is actually implemented — per this file's own Rules, only a named human Chief Engineer may make an ACCEPTED-RISK disposition change, and that has not occurred this cycle. See `validation/design-review.md` Cycle 4. **ACCEPTED-RISK — human Chief Engineer sign-off recorded 2026-09-09** (via cross-session HITL channel, hybrid disposition covering ISS-020 and ISS-021 together): explicitly conditioned on Firmware Bring-up actually implementing REQ-405 (this finding's governing requirement) — this is not a bare accept-and-move-on, the underlying finding is not truly RESOLVED until that firmware exists and is independently confirmed against REQ-405 in a future review cycle. Rationale distinguishing this from Bench-IMU-01's own precedent (Firmware Bring-up not gating Design Complete): that precedent was safe specifically because Rev 1/2 firmware only *read* sensors and never drove physical motion; this subsystem has real stored rotational energy, so the same blanket precedent cannot be applied without a stronger compensating safeguard. That safeguard: the "before first power-on" HITL gate (`docs/architecture.md` §10) is strengthened with a new named, mandatory pre-condition in `validation/bring-up-procedure.md` requiring confirmed REQ-405 implementation, cross-referencing this exact finding ID, before the motor subsystem is ever energized — see that file's pre-power-on section. This lets hardware/mechanical Design Complete proceed now while keeping the actual physical risk hard-blocked at the one gate that matters. See `validation/change-log.md` for the corresponding ECO entry. |
| ISS-021 | HIGH | ACCEPTED-RISK | REQ-404's "shutdown behavior to prevent sustained overheating" is not actually satisfied by DRV10983's Lock Detection alone -- Lock Detection auto-retries after a fixed delay, it does not latch off | U5 (DRV10983); M1; D2 (thermal dissipation under repeated fault current) | DS-MTR-058 (OCP clears once the fault condition itself clears); DS-MTR-059 (Lock Detection auto-retries after tLOCK_OFF=5s, not a latch); DS-MTR-060 (Thermal Shutdown recovers after cooling); REQ-404 | All three of U5's own protection mechanisms (OCP, Lock Detection, Thermal Shutdown) are auto-recovering/auto-retrying by design, not latching -- none of them, alone or together, actually implements "prevent sustained overheating" in the sense of a shutdown that stays off until an operator acknowledges it. A persistent jam (e.g. a mechanical bind on the flywheel) would cause repeated current pulses and repeated restart attempts indefinitely, each one dissipating real heat in U5, M1, and D2 (D2 alone can dissipate approximately 1.86W at a 3A/0.62V worst-case fault current), with no design-level cutoff that ever actually stops trying | Define an explicit latched-fault policy: e.g., firmware counts consecutive Lock Detection retries within a time window and forces SPEED to a safe/stopped state (and/or a supervisory switch cuts U5's VCC) after a threshold, requiring a deliberate re-arm before resuming -- do not treat "the driver retries automatically" as equivalent to "the system shuts down," those are different behaviors and REQ-404 asks for the latter | rubber-duck | [human sets] | 2026-09-05 | 2026-09-09 | Directly challenges whether REQ-404 (as currently implemented) is actually satisfied by U5's own datasheet-confirmed mechanisms, rather than checking whether those mechanisms exist (which Hardware Reviewer already confirmed via DS-MTR-058/059/060) -- a premise-level distinction between "protection exists" and "the specific behavior REQ-404 asks for exists." Cross-ref `validation/design-review.md` Cycle 3 rubber-duck section. **Cycle 4 (2026-09-08) independent re-verification: correctly remains OPEN (firmware policy undecided), hardware enabler independently CONFIRMED sufficient.** Independently confirmed via direct read of new design-doc §7.5.12 the same honest "no circuit-level fix" disclosure, and independently confirmed REQ-406 exists (line 143, Priority: Should), correctly attributed. Independently verified the one genuinely hardware-level sub-question this finding raises — whether U6/PA9 provides a real, sufficient physical enforcement point for a future firmware latch — and confirmed PA9 driving U6's SHDN low removes power from U5 entirely (not merely a SPEED-level override), the strongest available physical enforcement mechanism at this architecture; also independently confirmed via TI's own Table 8-1 that U6's own MODE=Open gives an independent chip-level Latch-off (TPS26631-specific), sharing but not confusing the SHDN reset line with any future firmware-declared latch (design doc's own §16 item 25 accurately discloses that U6 exposes no register letting firmware distinguish the two). Same recommendation as ISS-020: ripe for human Chief Engineer disposition consideration; remains OPEN, not unilaterally changed here. See `validation/design-review.md` Cycle 4. **ACCEPTED-RISK — human Chief Engineer sign-off recorded 2026-09-09** (via cross-session HITL channel, same hybrid disposition covering ISS-020 and ISS-021 together — see ISS-020's Notes for the full rationale): explicitly conditioned on Firmware Bring-up actually implementing REQ-406 (this finding's governing requirement) — the underlying finding is not truly RESOLVED until that firmware exists and is independently confirmed against REQ-406 in a future review cycle. Compensating safeguard: `validation/bring-up-procedure.md`'s pre-power-on section now names this exact finding ID as a mandatory pre-condition before the motor subsystem is ever energized, mirroring ISS-020's treatment. See `validation/change-log.md` for the corresponding ECO entry. |
| ISS-022 | MEDIUM | OPEN | The FG tach signal does not always represent actual rotor RPM (it can reflect commanded/drive frequency during open-loop startup), and this residual risk was not propagated into any Firmware or FMEA artifact despite ECO-007's explicit human directive to do so | U5 FG output; U1 PA6; motor firmware (future); `validation/fmea.md` | DS-MTR-062 (`FGOLsel` register), DS-MTR-063 (`Op2ClsThr` open-to-closed-loop transition threshold); REQ-008, REQ-112; `validation/change-log.md` ECO-007 | The design document itself already discloses the BEMF-detection-floor caveat (~500-1500 RPM) and the two specific configuration registers governing it -- but as of this review, neither `firmware/` (which does not yet exist for this subsystem) nor `validation/fmea.md` (unchanged this cycle) actually carries this as a tracked item, so ECO-007's directive is not yet fulfilled even though the underlying circuit-level substantiation is recorded | Carry this into the session's own tracked follow-up for Firmware Bring-up (handle/disclose degraded or absent RPM readback during the low-RPM ramp) and into `validation/fmea.md` at the next FMEA population pass -- both are already-planned future phases, this finding is a checkpoint that the directive not get silently dropped, not a new circuit-level defect | rubber-duck | [human sets] | 2026-09-05 | | Cross-references this session's own `bemf-caveat-tracking` follow-up item (recorded when the human issued the ECO-007 directive) -- confirms it has not yet been fulfilled, appropriately, since Firmware Bring-up has not run yet this cycle. Not blocking Circuit Design rework; a checkpoint for later phases. Cross-ref `validation/design-review.md` Cycle 3 rubber-duck section. |
| ISS-023 | MEDIUM | OPEN | Rev 2's "no vibration/shock, single simple rail" validation-artifact premise (bring-up procedure, FMEA) has not been updated to reflect that a real rotating body with independent power now exists on the same board | `validation/bring-up-procedure.md` (still describes a single-rail, ~50mA case with no motor-rail/sequencing content); `validation/fmea.md` (no M1/U5-related entries yet) | REQ-010, REQ-204, REQ-306, REQ-307, REQ-403, REQ-404 | If a future physical build followed only the currently-published bring-up procedure, it would have no motor-rail voltage/current acceptance criteria, no power-up sequencing guidance, and no motor-on/motor-off IMU regression check -- the exact concerns REQ-010/REQ-204 raise have no corresponding validation-procedure content yet | Update `validation/bring-up-procedure.md` and add `validation/fmea.md` entries for the motor/reaction-wheel subsystem at the next validation-artifact closeout pass (mirrors this project's own Rev 2 precedent, where FMEA/bring-up-procedure population happened at Design Complete Gate closeout, not mid-Circuit-Design) -- not a circuit-level defect, a checkpoint that this doesn't get silently skipped | rubber-duck | [human sets] | 2026-09-05 | | This is expected to be addressed at this cycle's own planned `validation-artifacts-rev3` phase (after Independent Review and Independent Mechanical Review both clear) -- flagged here so it is not forgotten, not because it should have been done already at this point in the cycle. Cross-ref `validation/design-review.md` Cycle 3 rubber-duck section. |
| ISS-024 | LOW | OPEN | Rev 5 top-of-file changelog summary states U6's thermal rise as "ΔTJ ≈ 10–16°C", inconsistent with three other internally-consistent locations in the same document and with `hardware/power-budget.md` (all state "≈7.5–13.0°C") | U6 (TPS26631PWPR) — documentation only, no schematic/BOM change needed | DS-PROT-031 (Thermal Information table; R(ON)/IQ Electrical Characteristics rows) | None — a documentation-consistency defect, not a physical/electrical failure mode. The wrong figure, if believed, overstates rather than understates thermal rise, so it carries no safety risk in itself; the risk is purely an inconsistent record confusing a future reader or reviewer | Circuit Engineer to correct the single changelog summary line (top of file, line 103) to match the three internally consistent, independently-reconfirmed occurrences ("≈7.5–13.0°C") in `bench-imu-01-design.md` §7.5.10/§14/§15 and `hardware/power-budget.md`. Trivial, single-line, no re-analysis required | hardware-reviewer | [human sets] | 2026-09-08 |  | Independently discovered this cycle while re-verifying U6's thermal margin against the primary TI datasheet's own Thermal Information table (RθJA=32.2°C/W, PWP/HTSSOP-20 package) and Electrical Characteristics R(ON) values (26/30.44/34.5mΩ min/typ/max) at the design's actual ~1.05A operating current — independently reproduces ≈7.5–13.0°C, confirming the *detailed* figure (§7.5.10/§14/§15/power-budget.md) is the technically correct one and the changelog summary line is the outlier. Cross-ref `validation/design-review.md` Cycle 4. |
| ISS-025 | LOW | OPEN | U6's OVP trip-point margin against a hypothetical 4S pack is real but narrow (≈3.4% reference-tolerance-only; ≈3.76% full-stack worst-case against the 13.0V envelope ceiling) — not a violation, but narrow enough to track rather than only leave inside a self-check paragraph | R12, R13, R14 (U6 OVP/UVLO divider) | DS-PROT-026 (Equations 9–10, divider architecture and guaranteed reference tolerance) | If a future BOM revision substitutes looser-tolerance (e.g. 5%) resistors for R12/R13/R14 without re-deriving the OVP trip point, or if resistor values drift with age/temperature beyond the ±1% assumed here, the worst-case OVP trip point could shift closer to — though based on this cycle's own arithmetic, not yet past — the 13.0V envelope ceiling or a 4S pack's nominal voltage, narrowing (not yet eliminating) the safety margin against a mis-connected battery pack | No change required for the current Rev 5 E96/1% resistor selection — the margin is real and positive. Track for future consideration: (a) if a real 4S-pack mis-connection is ever judged a credible (not just theoretical) field scenario, consider tighter-tolerance (e.g. 0.1%) resistors for R12–R14 in a future revision; (b) if R12/R13/R14 are ever re-selected for cost or availability reasons, the new tolerance must be re-run through this same margin calculation before being approved, not assumed equivalent | hardware-reviewer | [human sets] | 2026-09-08 |  | Independently recomputed both percentages from TI's own primary-datasheet equations (Equations 9–10) using the design's actual resistor values and both plausible worst-case tolerance combinations — reproduces the design document's own §16 item 28 figures (3.4%/3.75%; my own recomputation: 3.4%/3.76%) essentially exactly. Not a new arithmetic finding — a considered severity judgment (this task's own instruction to "form your own view") that this residual item, while correctly identified and disclosed by Circuit Engineer inside §16 item 28, deserves its own tracked backlog entry rather than remaining only inside a self-check narrative, given that a resistor-tolerance choice in a future revision could silently erode this margin without anyone re-deriving it unless it is separately tracked. Cross-ref `validation/design-review.md` Cycle 4. |
| ISS-026 | CRITICAL | RESOLVED | The IMU (U2, BMI270) I2C bus, documented and firmware-implemented as "I2C2 on PB10/PB11" since the original Rev 2 baseline, is wired to two MCU pins that **do not physically exist** on the STM32G031K8T6's actual LQFP-32 package | U1 (STM32G031K8T6) PB10/PB11 nets; U2 (BMI270) SCx/SDx pins; R3/R4 (I2C pull-ups); `firmware/bench-imu-01/src/{gpio.c,gpio.h,clock.c,i2c2.h,stm32g031_regs.h}` | DS12992 Rev 4 (ST's own primary datasheet, independently re-fetched and read directly this session via `r.jina.ai` text extraction — the same primary document DS-MCU-050 through DS-MCU-053/062 already cite): **Table 2** ("device features and peripheral counts") lists only 30 GPIOs for the K-suffix (LQFP32/UFQFPN32) package vs. 44 for the C-suffix (LQFP48/UFQFPN48) package; **Figure 7** ("STM32G031KxT LQFP32 pinout") lists all 32 physical pads by name and PB10/PB11 do not appear anywhere in it, while **Figure 9** ("STM32G031CxT LQFP48 pinout") explicitly does show them; **Table 12** ("Pin assignment and description," the master per-pin-per-package table) shows PB10's and PB11's own rows with a dash (not present) in the LQFP32/UFQFPN32 column and a real pin number (22, 23 respectively) only in the LQFP48/UFQFPN48 column. Independently cross-corroborated via a second, differently-sourced primary-equivalent record: ST's own CubeMX pin database as consumed by `stm32duino/Arduino_Core_STM32`'s auto-generated `PeripheralPins.c` for this exact part/package (`variants/STM32G0xx/G031K(4-6-8)(T-U)_G041K(6-8)(T-U)/PeripheralPins.c`) — PB10/PB11 appear in zero of its ADC/I2C/TIM/UART/SPI pin-map tables. New Evidence ID **DS-MCU-073** (logged as "DS-MCU-068" when this finding was first recorded 2026-09-09; renumbered 2026-09-11 during the main-branch merge, ECO-014) records this in full | The design as documented and as implemented in firmware is 100% guaranteed to fail at first power-on in exactly the way it is specified to work — this is not a corner-case/realistic-conditions failure (which would be HIGH), it is unconditional failure under the design's own normal/as-designed operating condition (`docs/architecture.md` §7.1's CRITICAL definition, first clause: "design will fail... under normal/expected operating conditions as designed"), because the two GPIO pads the schematic, net list, and firmware register-level code all reference for the IMU's SCL/SDA signals are simply not bonded out of the package — there is no physical pin for the PCB trace to connect to. The MCU's I2C2 peripheral would be correctly clocked and configured by firmware, but its AF6-routed output would have no physical pin to appear on; the IMU would never receive a valid I2C transaction, `BMI270_INIT_FAILED` would be the only possible outcome of every physical bring-up attempt, and REQ-002 (IMU functional readout, this board's core purpose) would be unconditionally unmet | Re-route the IMU I2C bus from the nonexistent PB10(SCL)/PB11(SDA) to **PA11(SCL)/PA12(SDA)** — both independently confirmed present on this exact LQFP32 package (Table 12), both carrying the same **I2C2** peripheral (not a peripheral-instance change, unlike the alternative of moving to I2C1) via the same **AF6** alternate-function selector already assumed for PB10/PB11 (Table 12: "SPI1_MISO/I2S1_MCK, USART1_CTS, TIM1_CH4, TIM1_BK2, **I2C2_SCL**" for PA11; "SPI1_MOSI/I2S1_SD, USART1_RTS_DE_CK, TIM1_ETR, I2S_CKIN, **I2C2_SDA**" for PA12), and both confirmed currently free/unused anywhere in this design through Rev 5 (`bench-imu-01-design.md` §11's own free-GPIO inventory) — no conflict with the motor subsystem's PA6/PA8/PA9/PB1/PB6/PB7 allocations. (The alternative of moving to real I2C1 pins is not available: PB6/PB7, I2C1's primary AF pair, are already consumed by the Rev 3 motor subsystem's own separate I2C1 bus to U5; PB8/PB9, I2C1's secondary AF pair, would require both an unavailable second simultaneous use of the same already-committed I2C1 hardware peripheral and a full firmware peripheral-instance change, a strictly larger blast radius than the PA11/PA12 fix.) Requires: (a) Circuit Engineer corrects the schematic net list, pin table, parts-list notes, and evidence-log citations; (b) Firmware Engineer corrects the register-level GPIO/AF/clock configuration (`gpio.c`/`gpio.h`/`clock.c` move from GPIOB pins 10/11 to GPIOA pins 11/12; `i2c2.h`/`stm32g031_regs.h` keep the I2C2 peripheral base address, which is unaffected by this fix since the peripheral instance itself does not change) — deferred to the already-planned Firmware Bring-up phase for this revision rather than a separate dispatch, tracked explicitly so it is not forgotten | human | CRITICAL: [human sets] | 2026-09-09 | 2026-09-10 | Discovered by a parallel session building a real KiCad project for the Rev 2 electronics baseline, relayed to this session by the human Chief Engineer with an explicit instruction to independently re-verify before acting. Independently re-verified beyond the human's own claim: fetched and read ST's actual primary datasheet (DS12992 Rev 4) directly this session (not merely trusting the relay or a secondary AI-summarized web search, two of which initially gave *conflicting* answers before this direct primary-source check settled it definitively), and independently cross-corroborated via a second, differently-derived primary-equivalent source (stm32duino's CubeMX-derived pin tables). This is a genuine, previously-undetected defect in the already-merged, already-Design-Complete Rev 2 baseline (Electronics PR #6, `b7ee9f1`) — it predates and is unrelated to Rev 3's own motor-subsystem work, but blocks Rev 3's own Design Complete Gate regardless, since CRITICAL findings can never be ACCEPTED-RISK (`docs/architecture.md` §8) and this finding is discovered while Rev 3 is the current revision under active review. The original Rev 2 Independent Review (which correctly caught the I2C1-vs-I2C2 *peripheral*-identity mislabeling, ISS-011/FMEA-003) did not separately check whether the specific GPIO pads referenced physically exist on the specific ordered package — a distinct, more fundamental question that FMEA-003's own "systemic lesson" (continue running both hardware-reviewer and rubber-duck passes) did not, in hindsight, fully cover either, since neither review lens's checklist explicitly included "does this named pin physically exist on this exact package variant." Recommend a new systemic-lesson entry in `validation/fmea.md` alongside FMEA-003 when the Rev 3 FMEA closeout pass runs. No mechanical-design impact: this is a pure MCU-internal net reassignment (which internal pad the IMU's SCL/SDA traces terminate at), not a change to board outline, connector positions, physical component list, or anything `hardware/mechanical-interface.md`/the Rev 3 enclosure design already produced — those remain valid and do not need rework. **Cycle 5 (2026-09-10) independent re-verification: CONFIRMED RESOLVED.** Independently re-fetched ST's own primary datasheet (DS12992 Rev 4) fresh this cycle via `r.jina.ai` — not reused from the citation chain — and re-derived the core physical fact from scratch: Table 2's GPIO/pin-count row (30 GPIOs, K-suffix/LQFP32-UFQFPN32 package vs. 44, C-suffix/LQFP48-UFQFPN48); Figure 7's full 32-pad LQFP32 pinout enumeration (PB10/PB11 absent, PA11/PA12 present); Figure 9's 48-pad LQFP48 enumeration (PB10/PB11/PB12 present); and Table 12's per-pin per-package assignment table, whose six-package-column parsing (SO8N/WLCSP18/TSSOP20/UFQFPN28/LQFP32-UFQFPN32/LQFP48-UFQFPN48 pin-position values) was independently validated against a known-good row (PC13: dash×5 then "1" = LQFP48-only, physical pin 1) before trusting it for the pins under review — reproduces the design document's own claim exactly: PB10 = dash×5 (incl. the LQFP32/UFQFPN32 column) then **22** only in the LQFP48/UFQFPN48 column; PB11 = same dash pattern then **23**; PA11 = 5/C1/16/18/**22**/dash (physical pin 22 present on LQFP32/UFQFPN32 itself); PA12 = 6/B2/17/19/**23**/dash (physical pin 23). Table 13 (Port A AF map) independently confirms PA11 AF6=I2C2_SCL and PA12 AF6=I2C2_SDA; Table 14 (Port B AF map) independently confirms PB10 AF6=I2C2_SCL and PB11 AF6=I2C2_SDA — the identical AF6 selector, exactly as the fix claims. **Completeness**: a fresh combined `grep -n "PB10"`/`"PB11"` pass (43 hits, whole document) reviewed line-by-line — every remaining hit is Rev 6/Rev 2 changelog narrative or a live table/prose entry that correctly states PA11/PA12 as the current value with PB10/PB11 present only as historical/superseded annotation (§2.3, §5.3, §5.5/§6/§7.5.4, §11's table + Rev 6 note, §12 net list, §14/§16 self-check items) — no live incorrect claim found anywhere. **Conflict check**: a fresh `grep -n "PA11"`/`"PA12"` (27 hits each) confirms sole use is the IMU's I2C2 SCL/SDA in every occurrence; cross-checked against the motor subsystem's own PA6/PA8/PA9/PB1/PB6/PB7 allocations (§7.5.4) — no overlap. Free-GPIO arithmetic independently re-added (§11: 11 enumerated free pins + PB8/PB9 = 13, down from 15 with PA11/PA12 now committed) — consistent. **§5.2 pull-up sizing independently re-read**: `Rp,min=(Vcc−VOL,max)/IOL` and `tr≈0.8473×Rp×Cb` depend only on Vcc/VOL,max/IOL/Cb, none of which reference a physical pin — and, going beyond the design document's own stated reasoning, Table 12 shows PB10/PB11 and PA11/PA12 (LQFP32/UFQFPN32 row) share the **identical I/O structure designation "FT_fa"**, which is what actually sets VOL,max/IOL in ST's electrical-characteristics tables — independently confirming R3/R4's 4.7kΩ sizing is unaffected, not merely assumed to be. **Floating pin / pull-up-pull-down / logic-voltage / interface-timing** (the four checklist items independently re-derived as relevant to a pin-reassignment specifically): no floating pin (R3/R4 pull-ups travel with the net; PB10/PB11 removed from the pin table entirely rather than left as a dangling used-but-unconnected entry); pull-up/pull-down correct (per the FT_fa-identity finding above); no logic-voltage mismatch (Port A and Port B share the same single VDD/VDDIO 3.3V I/O domain on this MCU — confirmed via the identical FT_fa I/O-structure class, not port letter, governing electrical behavior); no interface-timing impact (same I2C2 peripheral instance, same AF6 selector — alternate-function pad routing does not alter the peripheral's internal Fast-mode 400kHz timing-register configuration). **Scope discipline** independently confirmed via `git show --stat`/`--name-status ef5edc4` (not merely the commit message's own self-report): exactly one file touched, `hardware/schematic/bench-imu-01-design.md` (201 insertions/29 deletions) — `firmware/bench-imu-01/`, `bom/component-selection.md`, `requirements/requirements.md`, `hardware/mechanical-interface.md`, and `hardware/mechanical/` all confirmed untouched; `git status` clean, HEAD=`ef5edc4`, no further drift. See `validation/design-review.md` Cycle 5. **Superseded by a more complete upstream fix, 2026-09-11**: a parallel session building a real KiCad project for the Rev 2 baseline independently discovered this exact same physical-pin-nonexistence defect class (its own **ISS-027**, PR #9, merged to `main`) — its fix additionally corrected 3 further pin-identity errors this branch's Rev 6 fix did not catch (VDD/VDDA combined at pin 4, no separate VBAT pin, NRST shares pin 6 with PF2), verified via a real KiCad project's own ERC/netlist-export tooling rather than Markdown-only review. Upon merging `main` into this branch, main's more complete fix was adopted as authoritative for the shared MCU pin-table/power-pin content; this entry's own Rev 6 fix (PA11/PA12 re-route) remains correctly RESOLVED in its own right — it was independently correct, just incomplete relative to the fuller defect set ISS-027 covers. See ISS-027 for the complete, adopted fix. |
| MISS-008 | CRITICAL | RESOLVED | Flywheel disk Z-stack formula omits hub-collar height, producing an empirically-confirmed ~145-151mm³ solid-solid interpenetration between the disk and its own mounting collar — the flywheel cannot physically be assembled as the design's own assembly sequence describes | Flywheel disk (`fw_disk_bottom`/`fw_disk_top`); hub collar (`fw_hub_collar_od`/`fw_hub_collar_h`); every parameter downstream of `fw_disk_top` (`fw_clearance_top`, containment wall height, REQ-306 vertical-clearance claim, REQ-403 containment envelope sizing) in bench-imu-01-enclosure.scad | bench-imu-01-enclosure.scad lines 384-392 (`fw_hub_standoff`=3.0mm DERIVED, `fw_hub_collar_h`=6.0mm ASSUMPTION, and the file's own `fw_shaft_exposed_len_needed = fw_hub_collar_h + fw_hub_standoff` = 9.0mm formula, which additively includes collar height) cross-referenced against lines 561-574 (`fw_disk_bottom = fw_motor_bell_top + fw_hub_standoff`, which omits `fw_hub_collar_h` entirely); bench-imu-01-dimensional-spec.md §14 Assembly Step 5 ("slide the flywheel disk onto the hub collar") and §7 (REQ-306 vertical clearance claim, which depends on the now-shown-incorrect fw_disk_top/fw_clearance_top values) | The design's own `fw_shaft_exposed_len_needed` formula (line 392) treats the collar as sitting above the standoff and the disk as sitting above the collar (additive: standoff + collar), yet `fw_disk_bottom` (line 565) positions the disk directly on top of the standoff, skipping the collar's own 6.0mm height entirely — an internal self-contradiction within the same file. Empirically confirmed via a standalone rendered `intersection()` of the two literal solids at their modeled coordinates (collar Z=[28.5,34.5], disk Z=[31.5,36.0]): a genuine, fully-manifold overlap solid, volume ~145.0mm³ via trimesh (hand-calc 150.8mm³, within normal STL-facet-discretization tolerance), Z-bounds exactly [31.5,34.5] — the bottom 3.0mm of the disk's modeled position is literally inside the collar's modeled volume. A "collar embedded within disk" alternative reading is independently ruled out: collar height (6.0mm) exceeds disk thickness (4.5mm), making that reading geometrically impossible. As modeled, and as the assembly sequence's own wording requires (disk slides onto/over the collar's full height before resting on top), the disk cannot occupy the position the file assigns it | Correct `fw_disk_bottom` to `fw_motor_bell_top + fw_hub_standoff + fw_hub_collar_h` (=37.5mm, not 31.5mm) and propagate through `fw_disk_top` (=42.0mm, not 36.0mm) and `fw_clearance_top` (=45.0mm, not 39.0mm). Because corrected `fw_clearance_top` (45.0mm) exceeds current `fw_cap_outer_top` (43.0mm) by 2.0mm, the cap height/overall Z-stack must also grow by at least 2.0mm plus wall thickness to keep the disk enclosed — re-verify REQ-306 vertical clearance, REQ-403 containment sizing, and the §14 assembly sequence end-to-end against corrected geometry, not just patch the single point of the original error | mechanical-reviewer | [human sets] | 2026-09-11 | 2026-09-12 | Independent Mechanical Review Cycle 1 (Rev 3), first Mechanical review of this scope — cross-ref `validation/design-review.md` "Mechanical Reviewer — Cycle 3," Finding 1. Discovered by re-deriving the Z-stack formula chain against the file's own internally-inconsistent formulas, not from the dimensional-spec document's own self-check (§15 reports 10/10 ✅ and §16's open-items table does not list this defect — confirmed via full-document review this is a genuinely new catch). Blocks PASS. Also undermines the specific numeric claims made in support of the REQ-403 safety disposition (§8) — the flywheel does not fit within the modeled/verified clearance envelope at all under corrected geometry, so REQ-403's specific containment-cap dimensions cannot yet be considered verified, though the disposition's separate topological claim (continuous containment wall, no rotation-plane opening) is independently confirmed true and unaffected by this specific bug. Per `docs/architecture.md` §8, a CRITICAL finding can only reach RESOLVED, never ACCEPTED-RISK — Status intentionally left OPEN, which will correctly cause `tools/check_open_issues.py` to report a gate failure until this is fixed and re-verified. **Cycle 4 (2026-09-12) independent re-verification: RESOLVED, CONFIRMED FIXED.** Cross-ref `validation/design-review.md` "Mechanical Reviewer — Cycle 4," re-verification of Finding 1. Independently re-derived `fw_disk_bottom`=37.5 / `fw_disk_top`=42.0 / `fw_clearance_top`=45.0 / `fw_cap_outer_top`=49.0mm directly from the live `.scad` source via `include` (not transcribed from the spec document), then independently confirmed zero overlap two ways: an isolated CGAL `intersection()` of the corrected disk and hub-collar solids renders empty, and a `union()` of the same two solids splits into 2 disjoint watertight components with an exact measured 3.0mm gap. Also independently confirmed the +6.0mm cascade correctly propagated to REQ-306's own dependent figures (§7) and did not silently break REQ-308's X/Y envelope (formula-independence check) or the heat-set insert engagement margins (both re-derived, confirmed shift-invariant, 2.2mm radial / 2.3mm axial). No regression found. |
| MISS-009 | HIGH | RESOLVED | Motor-wire duct void is never subtracted from the enclosure's outer solids — the documented cable-routing path from the flywheel bay to the PCB bay is solid material, not a passage | `motor_wire_bridge()` (local duct cutout only), `fw_bay_wall()` (containment-wall annulus), `pcb_bay_base()` south wall, `base()` (top-level union) in bench-imu-01-enclosure.scad; MC-1 motor-wire connector routing (interface's Connectors/Switches/LEDs table) | bench-imu-01-enclosure.scad lines 770-857 (`fw_bay_wall()`, `motor_wire_bridge()`, `base()` — confirmed `base()` is a flat `union()` of 4 solids with no top-level `difference()` applied for the duct void); bench-imu-01-dimensional-spec.md §10 (connector accessibility table, MC-1 row, "42mm interior wire run" claim) and §14 Assembly Step 4 (routing the motor/encoder wiring through this duct before closing the PCB lid) | `motor_wire_bridge()`'s duct void is only subtracted from that module's own local bridging cube — never differenced against `fw_bay_wall()`'s solid annulus or `pcb_bay_base()`'s south wall, both of which independently occupy the same duct footprint. Empirically confirmed via a rendered `base()`-only geometry and a trimesh point-containment sweep along the duct's intended air path (Y=92-95mm, r=39.5-42.5mm): every sampled point along this path returns solid, not void, at two independent locations — the containment-wall annulus itself, and (a second, independent blockage) the PCB bay's own south wall a short distance further along the same path. The motor/encoder wiring the design's own assembly instructions direct through this "duct" has no physical passage to travel through — the wire would have to pass through solid printed plastic | Apply the duct-void subtraction at the top level of `base()`'s own union (wrap the relevant portion of `base()` in a `difference()` that removes the duct cylinder from `fw_bay_wall()` and `pcb_bay_base()` as well as `motor_wire_bridge()`'s own local cube), verifying via a render + containment sweep (as performed independently in this review) that the full Y=92-95mm/r=39.5-42.5mm path is void end-to-end after the fix, not just locally within `motor_wire_bridge()`'s own solid | mechanical-reviewer | [human sets] | 2026-09-11 | 2026-09-12 | Independent Mechanical Review Cycle 1 (Rev 3) finding — cross-ref `validation/design-review.md` "Mechanical Reviewer — Cycle 3," Finding 2. Not self-disclosed anywhere in the dimensional-spec document — §10's MC-1 row and §14 Step 4 both describe the duct as functional. Classified HIGH not CRITICAL per `docs/architecture.md` §7.1: a real, likely-to-occur defect under normal assembly, but not an unconditional first-power-on failure of the board's core function nor a safety hazard, and a practical (if inferior) workaround exists (routing the wiring external to the enclosure). Found in the same general area as Error #3 (wire-bridge/motor collision) and Error #4 (missing floor disc), both already caught and correctly fixed by the Mechanical Lead this revision — good technique, just not extended to this third, separate bug. **Cycle 4 (2026-09-12) independent re-verification: RESOLVED, CONFIRMED FIXED.** Cross-ref `validation/design-review.md` "Mechanical Reviewer — Cycle 4," re-verification of Finding 2. Independently re-verified the duct is open along its full documented Y=[91,108] path via two methods exceeding the Mechanical Lead's own disclosed self-check: a 2,516-point dense `trimesh` containment sweep (0 hits) and a 13-point OpenSCAD CGAL-exact `intersection()` cross-check via a different computational path (all 13 empty). Confirmed unrelated non-manifold artifacts elsewhere in the mesh (PCB standoff boss tops) do not affect the duct-region sweep result. |
| MISS-010 | HIGH | RESOLVED | Pre-existing Rev 2-origin base-tab/lid-skirt solid interference (~190.06mm³ across all 4 corners) was disclosed in the Rev 3 design document but never entered into the tracked backlog — carries forward untracked into Rev 3 | `base_tabs()` and `lid_shell()`'s skirt band, all 4 corners (bench-imu-01-enclosure.scad) — the same base/lid mating-face joint MISS-002/003/004 already govern | bench-imu-01-dimensional-spec.md §11.C (full root-cause narrative, self-disclosed this revision, reporting 190.06mm³ overlap); independently reproduced by hand: 2.0mm Y-overlap x 3.0mm Z-overlap x 8mm tab width x 4 corners = 192mm³ (matches the disclosed 190.06mm³ within normal fillet/rounding tolerance); overlap sits between `base_tabs()`'s Z=[15.5,21.1] (global) and `lid_shell()`'s skirt band Z=[18.1,21.1] (global) | At each of the 4 corners, the base tab's own solid volume and the lid skirt's own solid volume occupy an overlapping 2.0mm-deep x 3.0mm-tall region — two solids the design intends to be separate, mating parts cannot occupy the same space; if sliced and printed exactly as modeled, the lid cannot seat fully onto the base at any of the 4 corners without either forcibly deforming/crushing ~190mm³ of printed material or the corner remaining proud/unseated, likely surfacing as a visibly non-flush lid-to-base fit and/or interference with the corner fastener's own clamping engagement (the same fastener MISS-004 already covers, RESOLVED in Rev 2) | Apply the same fix pattern already proven on this exact joint this project (per the MISS-002/003/004 resolution): resize/reposition either `base_tabs()` or `lid_shell()`'s skirt band Z-extent/Y-position so the two solids no longer share the same 2.0mm x 3.0mm region at any of the 4 corners, then re-verify via a full render + boolean `intersection()` check (zero-volume expected) rather than a hand estimate alone, matching the rigor already applied to close out MISS-002 | mechanical-reviewer | [human sets] | 2026-09-11 | 2026-09-12 | Independent Mechanical Review Cycle 1 (Rev 3) finding — cross-ref `validation/design-review.md` "Mechanical Reviewer — Cycle 3," Finding 3. This defect is NOT new to Rev 3 — it is explicitly disclosed as a pre-existing, Rev-2-origin defect in bench-imu-01-dimensional-spec.md §11.C, described there as out of scope for the Rev 3 motor/flywheel task and flagged for this review's attention. Independent verification confirms the Mechanical Lead's own root-cause narrative and volume figure are accurate; this finding's contribution is not discovering the defect (already self-disclosed) but confirming, via a full re-read of this file's entire existing backlog (ISS-001 through ISS-026, MISS-001 through MISS-007), that no entry currently tracks it. Rated HIGH by direct analogy to the already-RESOLVED MISS-002 precedent (same class: solid-solid interference at a load-bearing mating/fastening feature), not the MEDIUM MISS-007 precedent (a thin-but-usable isthmus, not an actual overlap). Left OPEN rather than silently treated as already-accepted, since a HIGH finding can only be dispositioned RESOLVED or ACCEPTED-RISK with a named human Chief Engineer sign-off per this file's own Rules section — neither has happened for this specific defect. **Cycle 4 (2026-09-12) independent re-verification: RESOLVED, CONFIRMED FIXED.** Cross-ref `validation/design-review.md` "Mechanical Reviewer — Cycle 4," re-verification of Finding 3. Independently confirmed the real interference is gone via a Z-restricted CGAL `intersection()` (empty when the exact flush parting-plane is excluded by 0.05mm) after independently diagnosing — via a different method (trimesh volume / Z-thickness check) before cross-reading the Mechanical Lead's own §11.C explanation — that the raw, unrestricted intersection's non-empty result is a benign zero-volume coincident-face artifact, not residual overlap; two independently-arrived-at diagnoses of the same artifact agree. Independently confirmed the new relief notches introduce no new wall-thickness, fragmentation, or print-safety violation (notch X-ranges do not overlap the side-skirt walls; roof connectivity preserved; unaffected skirt segments keep their full 2.0mm thickness). |
| MISS-011 | MEDIUM | OPEN | REQ-403 containment disposition's specific wall-thickness (4mm) and fastener-retention adequacy claims rest on qualitative/heuristic reasoning, not any impact-energy or pull-out-under-shock calculation tied to the disposition's own stated ~122J/100g hazard figure | Containment wall (`containment_wall_t`=4.0mm) and 6xM3 heat-set-insert cap fasteners (bench-imu-01-enclosure.scad); REQ-403 safety disposition (bench-imu-01-dimensional-spec.md §8) | bench-imu-01-dimensional-spec.md §8 (REQ-403 disposition narrative and physics table — independently reproduced this cycle and confirmed arithmetically correct as far as it goes), §12 (fastener-placement table, explicit "no fastener-load calculation performed" self-disclosure), §13.3 (explicit self-disclosed lack of print-orientation-vs-impact-load analysis for the cap) | The disposition's reasoning for why a 4mm wall (=2x the design's own general-purpose print-safety minimum) and 6xM3 heat-set inserts are adequate specifically against a detached-flywheel impact is stated qualitatively ("substantially thicker than structural minimum," "multiple fasteners provide redundancy") rather than derived from the disposition's own stated ~122J/100g kinetic-energy hazard figure via any impact-energy-absorption, wall-deflection-under-impact, or fastener pull-out-under-shock-load calculation — honestly self-disclosed 3 times (§8/§12/§13.3) rather than silently asserted; not a proven defect (no calculation has been done showing the wall or fasteners are actually inadequate), but a verification-rigor gap on a safety-critical claim | Before this proposal is brought to the human safety reviewer, either (a) perform a basic impact-energy/wall-deflection or fastener-pull-out-under-shock estimate tied to the ~122J/100g figure, or (b) explicitly carry the qualitative-only status forward as a named, disclosed limitation in the material presented to the human reviewer, so the human's decision is made with full awareness that the specific numbers are not independently load-verified, only the general topology (continuous wall, no rotation-plane opening) is | mechanical-reviewer | [human sets] | 2026-09-11 | | Independent Mechanical Review Cycle 1 (Rev 3) finding — cross-ref `validation/design-review.md` "Mechanical Reviewer — Cycle 3," Finding 4. Non-gating (MEDIUM) per `docs/architecture.md` §7.1: a disclosed rigor/traceability gap on a proposal still pending human review, not an independently-demonstrated physical defect — no confirmed malfunction or hazard has been shown, only an absence of the calculation that would rule one out. Must still be explicitly surfaced to the human REQ-403 safety-review gate rather than silently dropped. Distinguished from MISS-008 (CRITICAL, an independently-proven geometric defect) — this reviewer's assessment is that the REQ-403 disposition should not be presented to the human gate until MISS-008 is fixed and the containment envelope is re-verified against corrected geometry, since a containment proposal built on a Z-stack that cannot physically hold the flywheel in the position the design assumes is not yet a stable target for a rigor-gap assessment either. |
| MISS-012 | HIGH | RESOLVED | REQ-403 containment disposition's `containment_wall_t`=4.0mm claim is a **CAD-geometric solid-wall claim only** — whether the physically 3D-printed part actually contains 4.0mm of solid material at that location, or a sparse internal lattice (typical FDM slicer defaults are 15-25% infill, mostly air), depends entirely on manufacturing/print-process parameters (infill %, infill pattern, wall/perimeter loop count, print orientation relative to expected impact-load direction) that are specified downstream of CAD, at slicing time — and are **never mentioned anywhere in this repository** | Containment wall/cap (`containment_wall_t`=4.0mm, `bench-imu-01-enclosure.scad`); REQ-403 safety disposition (`bench-imu-01-dimensional-spec.md` §8, §12) | `hardware/mechanical/bench-imu-01-dimensional-spec.md` line 372 (`containment_wall_t` row: "Deliberately thicker than the print-safe minimum -- this wall's job is containment (§8), not just enclosure, so it is sized above the manufacturability floor **on purpose**" -- reasoning is entirely about nominal solid-geometry wall thickness, with no reference to infill/perimeter/print-orientation anywhere); independently confirmed via a repo-wide `grep -rn -i "infill"` returning zero hits anywhere in the repository | For a part whose entire stated purpose is impact/fragment containment (not merely enclosure fit/dust-proofing, where a lower infill would be a normal, harmless cost/weight optimization), a solid-wall CAD dimension is not itself a manufacturing instruction -- if this part were sliced with a typical/default FDM setting (15-25% sparse infill, commonly a slicer's own out-of-the-box default) without anyone deliberately overriding it toward a containment-appropriate value, the as-printed wall would be mostly air at the exact location the design's entire safety argument depends on being solid, silently invalidating the ~15.9x-yield-safety-factor and containment-topology reasoning in §8 without changing a single number in the CAD file or this repository's own documentation -- a realistic, not remote-corner-case, failure mode given infill is a real parameter every FDM print requires *some* value for, and the repository-wide silence on it means the default would apply unless a human operator happens to know to override it | Do not attempt an ad hoc fix to this finding directly (no reasoned infill%/perimeter-count/print-orientation spec exists yet, and inventing one without a reviewed process would repeat the exact "improvised, ungoverned parameter" problem this finding is about). Correct path: this project is introducing a new **Manufacturing Engineer** discipline (agent + skill + a corresponding addition to Mechanical Reviewer's own checklist requiring a process spec for safety-critical/structural parts) as a standalone framework PR against `main`, mirroring the Power Engineer precedent (framework merged and audited before being exercised on a real design). Once that framework PR lands, get a Manufacturing Engineer pass on this containment cap specifically (infill %, wall/perimeter count, print orientation, each reasoned against the disposition's own disclosed ~99-122J credible-worst-case load), independently cross-checked by Mechanical Reviewer per the new checklist item, **before** REQ-403 sign-off is considered ready -- this is an additional prerequisite on top of, not instead of, the human Chief Engineer's own separate safety sign-off | human | HIGH: [human sets] | 2026-09-11 | 2026-09-11 | Surfaced by the human Chief Engineer's own direct question while reviewing the containment-cap proposal (relayed via the cross-session HITL channel) -- neither Mechanical Lead's own self-check, nor 2 Independent Mechanical Review cycles, caught this; a genuine premise-challenge finding in the same spirit as this project's own rubber-duck lens, though sourced directly from the human this time, not an AI review pass. Classified HIGH, not CRITICAL: this is not a demonstrated-certain failure of the current, complete design as documented (the "design" is genuinely incomplete on this specific parameter, not wrong), but a realistic-corner-case-that-is-actually-the-likely-default risk if the gap is left unaddressed through to physical manufacture -- distinguished from MISS-011 (MEDIUM, a rigor gap on an already-fully-specified geometry) because this finding is more fundamental: it questions whether the CAD geometry itself will correspond to the as-built part at all, not merely whether an already-agreed geometry has been load-verified. A separate, standalone framework PR (Manufacturing Engineer discipline) is already in motion against `main`, not yet merged or audited -- explicitly NOT to be improvised around ad hoc; this finding stays OPEN, tracked, and blocking a *complete* REQ-403 sign-off (though not blocking the human's own topology/general-approach review, which may still proceed) until that framework lands and is applied. **UPDATE 2026-09-11**: the Manufacturing Engineer framework has now landed and produced its first real output, `hardware/mechanical/bench-imu-01-manufacturing-spec.md`; Mechanical Reviewer has independently cross-checked it per the new checklist item 11 (see `validation/design-review.md`, "Mechanical Reviewer — Manufacturing Process Cross-Check (Containment Cap)"). Real, substantive progress, but **this finding remains OPEN, not RESOLVED**: the produced spec covers `containment_cap()` only; it does not specify any process for `fw_bay_wall()` (part of `module base()`), which shares this exact `containment_wall_t` claim and is independently confirmed (direct `.scad` geometry re-derivation) to be the actual primary radial containment surface in the disclosed threat's path -- the cap's own skirt sits ~9-11mm further out, radially behind the flange band. See new finding MISS-013 (HIGH) for the scope-gap specifics -- this MISS-012 finding should be considered resolved only once a process spec exists (and is independently cross-checked) for `fw_bay_wall()`/`base()` as well, not just the cap. Two further rigor findings (MISS-014, MISS-015; both MEDIUM, non-gating) were also surfaced during the same cross-check. **Re-verification (2026-09-11): CONFIRMED FIXED, on this MISS-012 finding's own specific closure bar.** Mechanical Reviewer independently re-verified MISS-013 (see `validation/design-review.md`, "Mechanical Reviewer — Manufacturing Process Cross-Check Re-Verification (MISS-013/014/015 Closure)") and confirmed a genuine, geometry-specific manufacturing process for `fw_bay_wall()`/`base()` now exists — independently re-derived from the live `.scad` source (not copy-pasted from the cap's own numbers), including a print-orientation rationale that genuinely grapples with the part's multi-feature bundling (PCB bay, motor platform, wall, wire bridge, all in one print) and an independently-agreed primary/secondary containment-surface determination (§4.1). This meets MISS-012's own stated closure bar verbatim: "a process spec exists (and is independently cross-checked) for `fw_bay_wall()`/`base()` as well, not just the cap." **RESOLVED** on that independent basis. MISS-014 (MEDIUM, non-gating) remains separately OPEN — 2 of its 5 flagged confidence-label rows were found still uncorrected during this same re-verification cycle — but per this finding's own prior text, MISS-014 was always tracked as a distinct, lower-severity rigor finding, not a sub-condition of MISS-012's own closure bar; it does not reopen or block this finding. |
| ISS-027 | CRITICAL | RESOLVED | STM32G031K8T6 (U1) LQFP-32 package physically has no PB10/PB11 pins at all — the IMU I2C2 bus, already "corrected" once by ISS-011 (I2C1→I2C2 relabeling), is documented on pins that do not exist on the real, already-Design-Complete Rev 2 part | U1 (STM32G031K8T6 MCU); U2 (BMI270 IMU) I2C connection; R3/R4 I2C pull-ups; also affects VDD/VDDA/VBAT/NRST pin numbering on the same MCU (see Notes) | DS-MCU-064/065/066/067 (Hardware Lead independent research, ST's own official machine-readable CubeMX pin database, `STMicroelectronics/STM32_open_pin_data` GitHub repo — primary-source-equivalent) | The IMU I2C bus as previously documented (PB10/PB11) cannot be physically wired at all — no PCB trace can connect to a pin that does not exist on the chosen package. This is a total, certain, 100%-reproducible failure of REQ-001/REQ-104 (the core sensor interface) if built exactly as the already-Design-Complete Rev 2 document specified, not a corner-case/probabilistic risk — discoverable only at PCB-footprint-placement time under the prior (Markdown-only) review process, which cannot check physical pin bonding the way a real KiCad symbol/footprint can | Rewire the IMU I2C2 bus to the real, physically-existing pins: **PA11 (I2C2_SCL, physical pin 22) / PA12 (I2C2_SDA, physical pin 23)** — the default (no SYSCFG remap needed) state of this package's documented "dual-pad" pin-sharing feature; keeps the I2C2 peripheral instance unchanged (minimizes blast radius on the already-merged Firmware Bring-up code, PR #7), only the GPIO pin/AF configuration needs a follow-up firmware fix (not performed here) | hardware-lead (independent discovery during first-ever KiCad-project tooling verification for this repository — not a checklist/premise review pass; see Notes) | [human sets] | 2026-08-31 | 2026-09-04 | **Fix applied 2026-08-31** to `hardware/schematic/bench-imu-01-design.md` (§0, §2.3, §4.1, §4.3, §5.2, §5.3, §6, §11, §12, §13, §14, §16 — see that document's "Rev 2, corrected" changelog entry and new §19 handoff) by the Hardware Lead acting under Circuit Engineer rigor (full Evidence ID citations, no self-declared "reviewed"/"complete" status). Same primary source also corrected 3 related, lower-severity pin-identity facts on the same MCU: no separate VDDA pin (combined with VDD at physical pin 4), no separate VBAT pin at all (not bonded out on this package — the original "tie to 3V3" ASSUMPTION was moot, not just unconfirmed), only one VSS pin not two (physical pin 5, combined VSS/VSSA — this also resolves this document's own previously-disclosed §16 item 3 moderate-confidence VDD/VSS pin-count discrepancy), and NRST shares a pad with GPIO PF2 (physical pin 6, not a dedicated pin at 4). Cross-ref: this repository's first real KiCad project (`hardware/schematic/bench-imu-01/`) is what surfaced this finding — built using the corrected pins directly, independently tool-verified (`kicad-extract_schematic_netlist`/`analyze_schematic_connections`/`find_component_connections`/`identify_circuit_patterns`/`analyze_bom` — see `validation/design-review.md` for the real tool output). Firmware Bring-up (`firmware/bench-imu-01/`, PR #7, already merged) initializes GPIOB pins 10/11 for I2C2 (DS-MCU-062) and will need a matching follow-up fix (GPIOA pins 11/12) — flagged here, not fixed by this entry (Firmware does not gate Design Complete, `docs/architecture.md` §14/`docs/workflow.md` Phase 11). **Severity recommendation (CRITICAL) is the Hardware Lead's own, pending the independent Hardware Reviewer's own classification per `docs/architecture.md` §7.1** — more severe than ISS-011's HIGH classification because no hardware rework can make the *previously documented* pins work at all (a certain, total failure "as designed", not a corner-case/reliability risk). Pending: independent Hardware Reviewer confirmation that the fix is correct and complete, and their own severity determination. **Cycle 3 (2026-09-04) independent Hardware Reviewer re-verification: CONFIRMED FIXED AND CORRECT; severity independently CONFIRMED CRITICAL.** Fidelity-scoped review (not a full Design Complete re-litigation, per this cycle's task framing) independently re-derived, not merely re-read, every claim: (1) `kicad-cli sch erc --severity-all` re-run directly — 0 errors, 1 benign `lib_symbol_mismatch` warning on U3 (expected `extends`-flattening artifact, also generically true of U1/U4), matching README.md exactly; (2) `kicad-cli sch export netlist` re-run directly, parsed with an independently-written S-expression parser — **exhaustive, line-by-line cross-check of all 32 MCU pins (§11) and all 19 net-list rows (§12), 100% match, no exceptions**, including I2C2_SCL=U1 pin22(PA9/PA11)+R3+U2 pin13, I2C2_SDA=U1 pin23(PA10/PA12)+R4+U2 pin14, 3V3 includes U1 pin4, GND includes U1 pin5, no VBAT pin exists, NRST=U1 pin6 (dual PF2/NRST); (3) independently parsed KiCad's own official bundled `MCU_ST_STM32G0.kicad_sym` library as a **second, independent primary source** (distinct from ST's GitHub XML) — confirms zero PB10/PB11 pins and pins 22/23 dual-named PA9/PA11 and PA10/PA12, corroborating DS-MCU-064–067; (4) all 16 `kicad-*` MCP tools independently re-tested directly against this exact project (not re-reading README.md's characterization) — confirms 9 fail with the exact ctx-bug text, 5 work as README.md claims, and **corrects README.md's own two explicitly-hedged ("very likely") guesses**: `run_drc_check` does not hit the ctx bug at all (legitimately reports `{"success":false,"error":"PCB file not found in project"}`) and `generate_project_thumbnail` fails with a different bug (`'FunctionTool' object is not callable`), not the ctx-bug text — **this Notes cell's own prior tool-attribution (naming `extract_schematic_netlist`/`analyze_schematic_connections`/`find_component_connections`/`identify_circuit_patterns`/`analyze_bom` as having "independently tool-verified" the fix) is corrected here: all 5 of those exact tools are confirmed broken in this environment and produced no real output — the actual working verification method throughout was `kicad-cli` directly, as this note now correctly attributes**; (5) `kicad-cli sch export bom` re-run directly, all 4 primary ICs (STM32G031K8T6, BMI270, TLV75533PDBV, USBLC6-2SC6) cross-checked against `bom/component-selection.md`/evidence-log, all matched; (6) 3 symbol/footprint claims independently spot-checked directly against KiCad's official library files (Bosch_LGA-14 footprint body = exactly 3.000×2.500mm; USBLC6-2SC6 and TLV75533PDBV symbols/pinouts both confirmed and cross-checked against the real netlist) — all CONFIRMED. **Independent severity classification**: CRITICAL confirmed, independently re-derived (not deferred) — per `docs/architecture.md` §7.1, this is a deterministic, 100%-reproducible physical/topological impossibility ("design will fail... as designed"), not a probabilistic/corner-case reliability risk (HIGH) — categorically more severe than ISS-011 (HIGH), whose pins were always physically real. **Three new LOW findings surfaced this cycle, none disclosed by README.md, none blocking**: (A) design doc §12's own `3V3`/`GND` net-summary rows erroneously reference R5 (the real netlist proves R5 sits on neither net — it is exclusively on `/LED_CTRL` and `/LED_A`, matching §7's prose and §12's own separate `LED_CTRL` row); (B) this Notes cell's own tool-attribution inaccuracy, corrected above; (C) `validation/change-log.md` ECO-006 and this design doc's matching passages are dated 2026-08-31, chronologically before ECO-005's 2026-09-03 Design Complete grant, despite repeatedly describing themselves as "post-Design-Complete" — a change-log date-label inconsistency, zero functional impact. Full real tool output, per-item checklist, and all three findings' full Issue/Rationale/Datasheet Source/Failure Mechanism/Recommended Fix/Severity records: `validation/design-review.md` Hardware Reviewer — Cycle 3 entry. **Firmware follow-up CLOSED (2026-09-08)**: `firmware/bench-imu-01/` (PR #7, previously flagged above as "will need a matching follow-up fix ... not fixed by this entry") now correctly targets **GPIOA pins 11/12** for I2C2 (`src/gpio.c`/`src/gpio.h`/`src/clock.c`/`src/clock.h`/`src/i2c2.h`/`src/stm32g031_regs.h`), matching this schematic's own PA11/PA12 assignment — GPIOB pins 10/11 no longer appear anywhere in the firmware source or its compiled output. Independently re-verified before fixing (not trusting this prompt's own prior "GPIOA pins 11/12" recommendation without checking): re-confirmed PA11/PA12=I2C2_SCL/SDA directly against ST's pin database (DS-MCU-067) and additionally sourced the GPIO alternate-function *number* (a fact the pin database alone doesn't encode) from two further independent official ST sources new to this repository — STM32CubeG0's own NUCLEO-G031K8 (identical MCU) I2C2 example and the `stm32g0xx-hal-driver` header's `GPIO_AF6_I2C2` numeric definition — both confirming AF6 (DS-MCU-068/077). Rebuilt with `arm-none-eabi-gcc` 16.2.0 this session: succeeds, zero warnings, identical image size to the pre-fix build (10,804/4/0 bytes); `arm-none-eabi-objdump -d` confirms the old GPIOB base address (0x50000400) no longer appears anywhere in the linked binary. Full account: `firmware/bench-imu-01/bench-imu-01-firmware-design.md` Changelog/§10; `validation/change-log.md` new ECO entry. This firmware fix does not itself reopen or alter this row's RESOLVED status or CRITICAL severity (both were, and remain, about the schematic/hardware pin assignment, which ECO-006 already fixed and Cycle 3 already re-confirmed) — it closes the previously-outstanding, explicitly-out-of-scope-for-Design-Complete firmware follow-up this row's own Notes had flagged. **Independent verification of the firmware fix (2026-09-08, ad hoc pass, not a formal Firmware Reviewer gate — none exists yet): CONFIRMED CORRECT, zero findings** — re-derived the PA11/PA12/AF6/GPIOA facts from primary sources independently (not trusting the fix's own citations), independently rebuilt the firmware and confirmed the compiled binary no longer contains the GPIOB base address, and found no stale PB10/PB11 claims in the updated documentation. Full report: this session's record; summary: `firmware/bench-imu-01/bench-imu-01-firmware-design.md` §10, `validation/change-log.md` ECO-018. |
| MISS-013 | HIGH | RESOLVED | Manufacturing-process spec's own §1 scope claim ("the associated containment-wall intent") is not met by its substantive analysis, which addresses `containment_cap()` only -- `fw_bay_wall()` (part of `module base()`), the part actually first in the disclosed radial-ejection threat's path and sharing the identical `containment_wall_t`=4.0mm claim, has **zero specified manufacturing process** (infill/perimeter/orientation/material) anywhere | `fw_bay_wall()` / `module base()` (`hardware/mechanical/bench-imu-01-enclosure.scad`); `hardware/mechanical/bench-imu-01-manufacturing-spec.md` §1/§2/§3/§4 | Independently re-derived directly from `bench-imu-01-enclosure.scad`: `fw_bay_wall()` (lines 922-949) is a solid cylindrical wall from `fw_bay_inner_r`=39.5mm to `fw_bay_outer_r`=43.5mm (thickness = `containment_wall_t` = 4.0mm exactly, line 455/468), spanning `fw_wall_h`=43.0mm (line 670), unioned into `module base()` (lines 1057-1074) -- the 1st printed piece. `containment_cap()` (lines 1076-1111, the 3rd printed piece, and the only part the manufacturing spec addresses) is physically and radially separate: its own skirt is a thinner `wall_t`=2.0mm slip-fit collar sitting at r=52.7-54.7mm (derived from `fw_flange_dia`=105.0mm line 505 + `fit_clearance`=0.2mm/side line 205 + `wall_t`), over 9mm radially outside the flange band (43.5-52.5mm) and ~11.2mm outside `fw_bay_wall()`'s own outer face (43.5mm). A fragment ejected radially at the disk's rim (r=30mm, 69.74 m/s) strikes `fw_bay_wall()` first -- long before it could reach the cap's skirt | Independently re-verified via a repo-wide grep for "infill" and "perimeter" (separately) across every `.md` file that no other document specifies a manufacturing-process parameter for `base()`/`fw_bay_wall()` either -- the gap is not filled elsewhere. If this document is treated as "the containment manufacturing process is now specified," `containment_cap()` would be printed per the specified reinforced process while `base()` -- containing the actual primary radial containment wall -- would be printed under whatever default settings a slicer applies (typically 15-25% infill), since nothing overrides them for that part. The as-printed primary containment surface could be mostly air at the exact location the design's safety argument depends on being solid, while the secondary/backup surface (the cap) is solid -- the reverse of what the disclosed radial-ejection threat requires | Manufacturing Engineer to extend the manufacturing spec (or a companion document) specifying infill %/pattern, wall/perimeter count, print orientation, and material for `fw_bay_wall()` specifically, reasoned against the same disclosed 121.60J/69.74m/s radial-ejection load case -- either by broadening the existing document's substantive sections to match its own §1 claim, or by explicitly narrowing §1's scope statement and opening a distinct tracked item for the wall, so the gap is at minimum honestly disclosed rather than implicitly presented as closed. Independently cross-check the result per checklist item 11 before considering MISS-012 resolved | mechanical-reviewer | HIGH: [human sets] | 2026-09-11 | 2026-09-11 | Surfaced during the first-ever exercise of Mechanical Reviewer checklist item 11 (independent cross-check of a Manufacturing Engineer process specification) -- see `validation/design-review.md`, "Mechanical Reviewer — Manufacturing Process Cross-Check (Containment Cap)," Finding 1. This is a partial continuation of MISS-012 (HIGH, still OPEN), not a new category of concern: MISS-012 asked for a manufacturing-process pass on the containment structure broadly; this finding documents that the pass which was produced only covers half of it (the cap, not the wall). Severity classified HIGH to match MISS-012's own established precedent for this exact style of finding (a realistic, not remote-corner-case, risk that the as-printed part differs materially from its CAD-assumed solid geometry) -- not escalated to CRITICAL because, as with MISS-012, this is a documentation/specification-completeness gap in an already-incomplete area, not a demonstrated-wrong claim about a fully-specified one. Routes back to the Manufacturing Engineer via the Hardware Lead, not to the Mechanical Lead (the `.scad` geometry itself is unchanged and not at fault). **Re-verification (2026-09-11): CONFIRMED FIXED.** Mechanical Reviewer independently re-derived every §3.1 geometry claim directly from the live `.scad` source (`fw_bay_wall()` lines 922-949, `base()` lines 1057-1074) and confirmed exact agreement (radii, wall thickness, wall height, flange OD, cap-skirt clearance all recomputed by hand from source constants, not accepted from the document's prose). The `base()`/`fw_bay_wall()` process rows in §4 are substantively distinct from the cap's own rows (different geometry: floor disc, flange band, wire-bridge, vs. the cap's flat top/skirt/bolt-circle) — not a relabeled copy. The print-orientation rationale for `base()` explicitly names and rejects a considered alternative (a sideways orientation) with a specific, correct reason, genuinely grappling with the part's multi-feature bundling rather than waving it away. The new §4.1 primary/secondary containment-surface determination was independently re-derived from radial geometry (`fw_bay_wall()` at r=39.5-43.5mm vs. the cap's skirt at r=52.7-54.7mm) and is independently agreed with, not merely repeated. See `validation/design-review.md`, "Mechanical Reviewer — Manufacturing Process Cross-Check Re-Verification (MISS-013/014/015 Closure)," for the full re-derivation. **RESOLVED.** |
| MISS-014 | MEDIUM | OPEN | Manufacturing-process spec §3.2's "disclosed load case" table labels derived/computed physics figures (stored kinetic energy = 121.60 J; rim tip speed = 69.74 m/s) and a qualitative threat-mechanism hypothesis as `CONFIRMED`, despite depending on inputs the *source* document itself marks less certain | `hardware/mechanical/bench-imu-01-manufacturing-spec.md` §3.2 (confidence-labeling only, not a physics error) | Independently re-derived the arithmetic by hand: I = 0.5 × 0.100 kg × (0.030 m)² = 4.5×10⁻⁵ kg·m²; KE = 0.5 × 4.5×10⁻⁵ × (2324.8 rad/s)² ≈ 121.6 J ✓; v = 2324.8 × 0.030 = 69.74 m/s ✓ -- both match exactly. Traced the two geometric inputs to their original labels in `bench-imu-01-dimensional-spec.md`: disk mass=100g is **ESTIMATE** (line 311); disk diameter `fw_dia`=60.0mm is **ASSUMPTION** (line 365). Per `.github/instructions/mechanical-design.instructions.md`, a computed value cannot cleanly inherit CONFIRMED from a mix of inputs -- it is no more certain than its least-certain input. The "Threat mechanism" (hub-collar-release) row is itself a reasoned hypothesis, not a confirmed fact, since hub collar retention strength is independently confirmed **UNKNOWN** (dimensional-spec §16) | A future reader (including the human Chief Engineer at the REQ-403 escalation gate this document is headed toward) skimming "CONFIRMED" on 121.60 J could reasonably conclude the figure is measured/certain rather than computed from an ASSUMPTION-labeled radius and an ESTIMATE-labeled mass -- understating the actual uncertainty in the load figure the document's entire process rationale is anchored to | Re-label the two derived rows (stored kinetic energy, rim tip speed) and the threat-mechanism row as `ESTIMATE` (matching the least-certain input feeding each), or show each cell's own lineage explicitly, consistent with how §4's own process table already handles mixed-confidence reasoning correctly | mechanical-reviewer | MEDIUM: [human sets] | 2026-09-11 |  | Surfaced during the first-ever exercise of Mechanical Reviewer checklist item 11 -- see `validation/design-review.md`, "Mechanical Reviewer — Manufacturing Process Cross-Check (Containment Cap)," Finding 2. By contrast, §4's own process-parameter table was independently confirmed to correctly avoid CONFIRMED entirely (all ESTIMATE/ASSUMPTION) -- this is an internal inconsistency within the document, not a systemic failure to understand the confidence-marking convention. Does not itself change any engineering decision or introduce a physical hazard; non-gating. **Re-verification (2026-09-11): PARTIALLY FIXED — remains OPEN.** Mechanical Reviewer independently re-confirmed the 3 originally-named rows (stored kinetic energy, rim tip speed, threat mechanism) are now correctly labeled `ESTIMATE`, with correct Source-column lineage to their ASSUMPTION/ESTIMATE-labeled dimensional-spec inputs — arithmetic independently re-derived by hand and matches exactly. **However, the explicit task instruction to spot-check the rest of the document (not trust the Manufacturing Engineer's own claim that no other instances exist) found the identical overclaim persists on 2 more rows in the same §3.2 table**, neither named in this finding's original text: "Credible worst-case flywheel speed" (22,200 RPM) and "Angular velocity" (2324.8 rad/s) remain labeled `CONFIRMED`, despite the RPM figure being an explicitly project-derived, not-manufacturer-published estimate per `datasheets/evidence-log.md` line 236 (DS-MTR-018: "this project's own derived no-load speed estimate ... not directly published by the manufacturer"), corroborated by `datasheets/tmotor_mn2206-13-2000kv_rev-unknown.md` ("No formal PDF datasheet found/published for this SKU") and by `requirements/requirements.md` REQ-405's own "≈20,000-22,200 RPM" hedge. "Angular velocity" is a pure unit conversion of the same RPM figure and inherits its confidence exactly. A full-document grep confirms only 4 total `CONFIRMED` occurrences exist (lines 11, 28, 29, 82); the other 2 (safety-critical classification, escalation-required conclusion) are independently confirmed to be legitimate categorical/procedural uses, not physical-value overclaims. **Finding stays OPEN.** Recommended fix: relabel both remaining rows `ESTIMATE`, following the exact pattern already correctly applied to the other 3 rows in the same table — a small, focused, same-pattern fix. See `validation/design-review.md`, "Mechanical Reviewer — Manufacturing Process Cross-Check Re-Verification (MISS-013/014/015 Closure)," for the full re-derivation. Routes back to the Manufacturing Engineer via the Hardware Lead. |
| MISS-015 | MEDIUM | RESOLVED | Manufacturing-process spec §4's infill-percentage rationale presents "maximize infill for impact-energy absorption" as a one-sided, settled conclusion, without disclosing that the document's own cited literature source reports a non-100%-infill sample outperforming a 100%-infill solid sample for that exact goal; separately, the print-orientation rationale never addresses the cap top's own Z-axis anisotropy, a limitation the document's own cited source (reference #3) states is not fixable by infill/perimeter choices | `hardware/mechanical/bench-imu-01-manufacturing-spec.md` §4 (Infill percentage row, Print orientation row), §7 (references #3, #4) | Independently researched the manufacturing spec's own reference #4 (MDPI *Eng. Proc.* 2024, 5(1):27, "Optimizing Impact Toughness in 3D-Printed PLA Structures Using Hilbert Curve and Honeycomb Infill Patterns") via two independently-phrased web searches, both converging on the same figures: a Hilbert-curve-infill sample absorbed 11% MORE Charpy impact energy than a 100%-infill solid sample, but 20.6% LESS than a 40%-infill plain sample -- i.e. 40%-infill beat both the exotic pattern and the fully-solid sample for impact-energy absorption in this published dataset. Primary-source text was not directly accessible (`web_fetch` and a browser-user-agent `curl` both returned HTTP 403, independently confirmed) -- finding is corroborated via two independent secondary searches converging on identical figures/citation, not primary-text-confirmed. Independently verified reference #3 (3DMag) separately states FDM Z-axis strength is inherently weaker and infill/perimeter choices "won't fully compensate" for it -- a point the manufacturing spec's own print-orientation rationale never applies to the cap's own flat top | Presents a one-sided "more infill/more perimeters is always better for this goal" narrative to the human Chief Engineer at the escalation gate, when the document's own cited literature shows a more nuanced picture for the specific goal (impact-energy absorption) this part needs; weakens the rigor/credibility of the derivation chain presented as support for the parameter choices, though the choices themselves remain separately defensible on conservative-default/CAD-fidelity grounds | Add an explicit line acknowledging the more nuanced literature picture (some published FDM impact-toughness data shows infill densities below 100% can outperform fully-solid prints for energy absorption specifically; 100% infill retained here as a conservative default under deep load-case uncertainty and for CAD-fidelity, not because the literature unambiguously endorses maximum infill for this goal); add a line naming the cap top's own Z-axis exposure as a residual, infill/perimeter-independent limitation | mechanical-reviewer | MEDIUM: [human sets] | 2026-09-11 | 2026-09-11 | Surfaced during the first-ever exercise of Mechanical Reviewer checklist item 11 -- see `validation/design-review.md`, "Mechanical Reviewer — Manufacturing Process Cross-Check (Containment Cap)," Finding 3, and its dedicated "Independent literature investigation" section. Does not invalidate the document's bottom-line 100%-infill recommendation, which remains separately defensible; non-gating. **Re-verification (2026-09-11): CONFIRMED FIXED.** Mechanical Reviewer independently re-read the revised infill-percentage row and confirmed it now honestly discloses the MDPI literature nuance (a 40%-infill sample outperforming both a Hilbert-pattern sample and a 100%-solid sample for Charpy impact-energy absorption — cross-checked against this Reviewer's own separately-conducted literature research from the original finding, fully consistent) while retaining the 100%-infill recommendation on the correct, disclosed grounds (conservative default under load-case uncertainty, CAD-fidelity) rather than overclaiming unanimous literature support. Independently re-read the print-orientation row's added residual-limitation sentence on the cap top's own Z-axis anisotropy and confirmed it is specific and substantive (names the flat top, explains the geometric reason the limitation is unavoidable, correctly distinguishes what is/isn't helped by orientation choice) — not a token sentence. See `validation/design-review.md`, "Mechanical Reviewer — Manufacturing Process Cross-Check Re-Verification (MISS-013/014/015 Closure)," for the full re-derivation. **RESOLVED.** |
